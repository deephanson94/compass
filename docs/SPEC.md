# compass — Product Spec

> Status: draft v0.1 · design phase · everything here is up for debate

## 1. The problem

Running many Claude Code sessions in parallel is the new normal. The failure mode is
not that sessions do bad work — it's that the human loses the plot:

- **Past** — *"what journey did this session take?"* You scroll back through 400 lines
  of tool output trying to reconstruct what happened.
- **Present** — *"is it working, or waiting on me?"* A session blocked on a permission
  prompt looks identical to one deep in thought. Minutes die silently.
- **Future** — *"what adventure is it going to do next?"* Claude has a plan (it keeps
  todos), but that plan is invisible unless you interrupt and ask.

## 2. The shape of the answer

A **sidecar panel** next to the real Claude Code CLI. Not a replacement UI, not a
wrapper — a compass you glance at. Three ideas make it work:

### 2.1 The Trail is a git graph

Sessions don't produce a flat log — they produce a **branching journey**, and the
transcript data proves it: subagents are literal sidechains on disk that fork from the
main conversation and merge back with a result. So we render the journey exactly the
way developers already read branching history: a vertical rail of nodes, branch lanes
for subagents, merges when they return. **Time flows downward**: the opening prompt
at the top, the newest work at the bottom, pinned there so the latest is always on
screen (decided — see §7, #12, which reversed the original git-log ordering after
the first dogfood).

Node vocabulary:

| Glyph | Meaning |
|-------|---------|
| `◉` | journey start — the user's prompt, quoted |
| `◆` | completed leg (colored by class) |
| `●` | HEAD — the leg in progress *right now* (breathing animation) |
| `◈` | subagent branch (forks off, merges back with its finding); `◍` on a lane whose own transcript has gone quiet past the threshold (#49) |
| `◌` | ghost waypoint — *planned* work from Claude's own todo list, dashed lane |
| `▲` | attention marker — a question, permission prompt, or failure lives here |

The ghost nodes are the compass part: the trail doesn't stop at HEAD, it fades ahead
into where the session is *going*. Past above, present at the fold, future below it in
dashed strokes — the plan is simply further down the same road.

### 2.2 Legs: the Lv1 classification

Every session's activity segments into **legs** — contiguous spans dominated by one
class of work. Seven classes, fixed, each with one verb — and the verb is written on
the row, narrated or not: colour reinforces the class, it never carries it alone (§4).

`WAIT` below is the odd one out and was never built as a leg: waiting is a session
*state*, not a span of work, and it shows in the fleet column as `▲` or `○`.

| Class | Verb | Detected from (heuristics) |
|-------|-------|---------------------------|
| `SCOUT` | scouting | Read/Grep/Glob/WebFetch dominance, Explore agents |
| `DESIGN` | designing | plan mode, ExitPlanMode, long text turns, spec/doc writes before code |
| `BUILD` | building | Edit/Write on source files, new files |
| `FIX` | fixing | edit-run-edit loops, error strings in tool results, test-fail→edit cycles |
| `TEST` | testing | test runner invocations (pytest/jest/go test/cargo test…), parsed results |
| `SHIP` | shipping | git commit/push, PR creation, CI watching |
| `DOCS` | writing | .md edits, comments, READMEs |
| `WAIT` | waiting | no activity: idle, or blocked on the human |

Deterministic, offline, instant. AI never gates rendering.

### 2.3 Four zoom levels, one key

`Tab` zooms in, `Shift+Tab` zooms out. That's the whole navigation model.

**Lv0 — Board.** Every trail that fits, side by side: one column per session, urgent first, bright while it has something unread in it (#16). The default view from 110 columns up; `Tab` opens the selected column as Lv1.

**Lv1 — Trail.** The git graph of legs. Five to twelve nodes for a typical session.
Answerable in one glance: what phases happened, in what order, where is it now, what's
planned.

**Lv2 — Waypoints.** Each leg unfolds its detail — the same graph, expanded:

```
◆ TEST  pytest tests/auth                          12m
│  ├ 18 passed · 2 failed · 1.2s
│  ├ ✗ test_refresh_expired_token   AssertionError
│  └ ✗ test_refresh_revoked_token   AssertionError
│
◆ FIX   two bugs in token refresh                  15m
│  ├ bug1  syntax error in middleware.py:88        ✓ fixed
│  ├ bug2  expiry compared in local time, not UTC  ✓ fixed
│  └ touched  middleware.py · tokens.py · conftest.py
```

Waypoints are extracted per class: test runs get parsed pass/fail counts and failing
test names; FIX legs get one line per distinct bug; SCOUT legs get the questions
answered ("how do TokenStore and SessionCache interact"); SHIP legs get commit
subjects and PR links; subagent nodes get the agent's final-report one-liner.

**Lv3 — Deep dive.** The trail area splits 50/50 (decided — see §7):

- **Left half: the reader.** The full conversation rendered like `bat` renders code —
  syntax-highlighted tool calls, folded tool outputs (unfold with `Space`), search
  with `/`. Entering Lv3 from a selected Lv2 waypoint lands scrolled to *that moment*.
- **Right half: ask the trail.** Press `a` and compass suspends its TUI and runs a
  real `claude` session in its own terminal — the way `git commit` hands you your
  editor; quitting it drops you back into compass — pre-loaded with this session's
  transcript
  and a system prompt making it the session's historian: *"why did you delete
  conftest.py?"*, *"what approaches did you try before this one?"*, *"summarize what
  a reviewer should look at."* It's Claude interrogating Claude's own journey.

### 2.4 The Fleet

The top strip of the panel is always the **fleet**: every live session, one line each.

```
1 ● api      fixing token refresh              3m
2 ● webapp   testing · 18✓ 2✗                 40s
3 ▲ infra    needs you — permission (2m!)
4 ○ docs     idle — finished "update readme"  22m
```

Session state machine (per session, derived from transcript activity):

| State | Glyph | Definition | Panel behavior |
|-------|-------|-----------|----------------|
| **working** | `●` (green) | assistant turn in flight; transcript growing | calm; live verb from current leg |
| **needs you** | `▲` (amber) | turn stopped on a question or permission prompt | rises to top of fleet; age counter; optional bell |
| **idle** | `○` (dim) | turn complete, awaiting next prompt | shows what it finished |
| **stuck** | `◍` (red) | "working" with no transcript growth beyond threshold — and, for a shell command, past the budget the harness gave it (#45) | flagged with duration; inside its budget the row says "for 2m of 10m" |
| **looping** (`circling` in the decisions before #48 and in the hook's event name) | `↻` | not a state of the machine: a session whose last three verdict runs fail the same test three legs running, whatever the machine calls it (#27, #29) | counted in the header with the loop's age; sorts with the alarms; the row says whether a turn is in flight ("looping · idle") |
| **dead on the API** | `⊘` | needs-you whose stop is an API refusal (quota, auth); nothing typed clears it (#30) | the word "quota" or "api error" on the row; its own chip; sorts under the loop; `g` skips it; the reply panel offers the remedy under its own head, and names the quota line as the turn it starts |

**needs-you is the whole game.** The single most valuable thing compass does is make a
blocked session impossible to miss. Attention is **visual-only by default** — no bells
(bells over SSH are exactly the trouble we're avoiding; decided — see §7). Four
escalating cues, all silent from compass's side:

1. **In panel** — amber sessions sort to the top of the fleet with an age counter.
2. **Tab title** — compass sets its terminal tab title via OSC escape:
   `⌂ compass ▲2` when two sessions need you, `⌂ compass` when all calm. Windows
   Terminal, iTerm, GNOME Terminal etc. all render this on the *unfocused* tab, and
   it travels fine over SSH.
3. **tmux status bar (optional)** — `compass status` is a one-shot subcommand that
   prints a compact fleet summary (`●3 ▲1`); drop `#(compass status)` into your own
   `status-right` and every tmux session shows it. compass never touches your tmux
   config itself.
4. **The event hook (optional, #27)** — a command the config names, run on the
   crossings that matter while nobody is looking. Still silent from compass's side:
   what the command does is the person's choice.

### 2.5 One mode, one binary

**The board (Lv0).** On a terminal wide enough, the deck opens on every trail
that fits, side by side — one column per session in the fleet's order, each
under its own fleet row, bright while it has something unread in it and dim
once it is history (decision #16). `Tab` opens the selected column as the single
trail below; the three-panel deck drawn here is that trail with the mirror
switched on (`m`, decision #15).

**The deck.** `compass` runs full-screen in its own terminal tab — *outside*
tmux. It sees every Claude session on the machine at once (transcripts aren't
tmux-scoped), across all your tmux sessions and even bare-shell sessions. Three
panels: fleet on the left, the **live mirror** of the selected session's pane in the
middle, its trail on the right. Lv3 splits the trail area.

```
┌ compass ──────────────────────────────────────────────────────────────────────────────────┐
│ FLEET · live          │ ⌁ dev:1.0 · live                 │ TRAIL · api             [Lv1]  │
│ dev                   │                                  │ ◉ "fix the 401 bug"       38m  │
│▸1 ● api    fixing  3m │  ● I'll fix the token refresh    │ ╷                              │
│    :1.0 · auth-fx     │    bug. Let me look at the       │ ◆ scout  auth module map  31m  │
│ 2 ● webapp 18✓ 2✗     │    middleware first…             │ │                              │
│    :2.1 · main        │                                  │ ◆ build  refresh middlware 25m │
│ ops                 ▲ │  ⏺ Read(src/auth/middleware.py)  │ ├─◈ agent scouted payments     │
│ 3 ▲ infra  needs you! │  ⏺ Bash(pytest tests/auth -x)    │ ◆ test   pytest 18✓ 2✗    12m  │
│    :0.0 · tf/vpc      │    ⎿ 18 passed, 2 failed…        │ │                              │
│ elsewhere             │                                  │ ● fix    token refresh   ← 3m  │
│ 4 ○ docs   idle   22m │  ✻ Churning… (23s · esc to       │ ┊                              │
│    no pane · main     │    interrupt)                    │ ◌ test   full suite            │
│                       │                                  │ ┊                              │
│ 268 archived · A      │  the real pane, mirrored live    │ ◌ ship   open PR               │
│ j/k move · enter attach (prefix d returns) · g grab · ? help · q quit                     │
└───────────────────────────────────────────────────────────────────────────────────────────┘
```

**The mirror is glass, not a terminal.** It streams the selected session's tmux pane
via `capture-pane` polling — the same mechanism tmux's own `choose-tree` preview
uses — so you watch the *actual* CLI exactly as it renders, colors and all, while
compass still owns no PTY and handles no input. Keystrokes always happen in the real
pane, so `Enter` hands you that pane's own terminal (§3). Sessions with no pane show
their latest transcript activity in the mirror's place. The mirror is off by default
(#15): `m` opens it on the single trail from 110 columns; below that the deck is fleet +
trail at every level but Lv3.

The **reader** is a level, not a panel (#15): Lv2 is the trail unfolded with a cursor on
it, and Lv3 opens the conversation anchored to that cursor.

Each fleet entry shows *where the session lives* (`dev:1.0` = tmux session `dev`,
window 1, pane 0) so you always know where to go.

**Beside a session.** There is no second mode, but the deck fits the pane it is
given, and a user who splits their own tmux gets the sidecar the early sketch was
reaching for: from 62 columns the mirror closes (the real CLI is next door) and the
fleet sits beside the trail; below 62 the trail has it alone, with the header
carrying the fleet's alarm and the trail's title naming the selected session. compass
still creates no split of its own — the user does, and compass renders to fit.

**compass never manages tmux.** It creates no sessions, windows, or panes — ever
(decided — see §7). You own your multiplexer; compass reads it (`list-panes`,
`capture-pane`) and performs three opt-outable write actions — attach, a quick reply typed into a pane, and the event hook the config names — the first two only on an
explicit keypress: **`Enter`** (hand the terminal to the session you asked for).
Ask-the-trail doesn't touch tmux at all — it runs in compass's own terminal (see §3).

## 3. Keymap — and the three-keypress proof

Global keys (work at every level):

| Key | Action |
|-----|--------|
| `1`–`9` | select session N — its trail renders immediately; a digit the live fleet does not use opens that row of the recent band in the archive, where `A` comes back (#47) |
| `Tab` / `Shift+Tab` | zoom in / out (Lv1 ⇄ Lv2 ⇄ Lv3) |
| `j`/`k` or `↓`/`↑` | move: the fleet at Lv1, the trail's rows at Lv2 (the conversation follows), the reader at Lv3 |
| `Enter` | **go to it**: compass hands over the terminal. Outside tmux it suspends and attaches (your own prefix + `d` returns); inside tmux the client switches. One meaning at every level (#13). |
| `g` | grab the oldest needs-you session: select it *and* go to it, one key (a session dead on the API is skipped: no keypress helps it) |
| `G` | back to the present — the newest row (#12) |
| `[` / `]` | previous / next prompt — the chapters of a trail: the viewport at Lv1, the cursor at Lv2, with a note saying which chapter and when (#17) |
| `a` | ask the trail: compass suspends its TUI and runs the historian — a real `claude` grounded in this session's transcript — in compass's own terminal; `exit`/`Ctrl-D` returns to compass |
| `A` | browse the archive |
| `Space` | Lv3 reader: fold/unfold a tool output |
| `/`, `n`/`N` | on the board, a list or the archive: search the fleet as it is typed (the footer echoes the query, `enter` keeps it, `esc` cancels); in the Lv3 reader: search the text, walk the matches |
| `r` | reply: a panel names the selected session and its pane and offers the question's options (the menu's own digit), the stock lines (typed and entered), or stop (escape); `t` types a line. On a session dead on the API (`⊘`) the panel offers the remedy (`/login`, typed into the pane) under its own head and names a stock line as the turn it would start. A tmux write, keypress-gated, off under `readonly` |
| `x` | hide the selected session from the board; the archive lists it, `x` there brings it back. What owes an alarm stays, and says so |
| `ctrl+d`/`ctrl+u` (`PgDn`/`PgUp` too) | half a page: the trail at Lv1, the reader at Lv3 (at Lv2 the cursor is what the viewport follows, so the cursor is what moves); the footer names it wherever it has the room (#45) |
| `?` | help overlay |
| `Esc` | one level out; on the board or a list, a standing search clears first — on the only session too, since there is nothing to zoom out to |
| `q` | quit compass, at every level |

Note what's *absent*: no "new session" key, no kill, no rename — compass doesn't
manage sessions or tmux (§2.5). `Enter` (and the `g` that ends in one) and `r`
(#26) are its tmux writes, behind an explicit keypress and disableable with
`-readonly` / `readonly = true`; the event hook (#27) is the one action with
nobody at the keyboard, runs only when the config names it, and is not a tmux
write, so `readonly` leaves it be.

The constraint, proven:

| "I want to…" | Keys | Count |
|--------------|------|-------|
| see what session 3 is doing | `3` | 1 |
| unblock whichever session needs me | `g` — it selects *and* hands over the terminal | 1 |
| see why webapp's tests fail | `2` `Tab` `Tab` from the board, `2` `Tab` from a trail | 2 |
| read the exact moment a bug was fixed | `2` `Tab` `Tab` `Tab` from the board (j/k on the legs aim the reader; aiming is not depth) | 3 |
| ask a session why it made a choice | `2` `a` | 2 |
| get to session 3's actual CLI | `3` `Enter` | 2 |

Rule of the constraint: **every destination is ≤3 *depth* keypresses**; selecting a
session (`1`–`9`, `g`, `j`/`k`) is aiming, not depth, and nothing may hide behind a
fourth level. From the board the trail is one `Tab`, the legs two, the reader three. Any feature that can't fit this dies or moves to config.

## 4. Visual language

- **Palette**: one hue per leg class, muted (Tailwind-500-ish saturation on dark,
  600 on light); amber and red reserved *exclusively* for needs-you and stuck. If
  everything is fine, the panel contains no warm colors at all — you can see "all
  calm" from across the room.
- **Typography**: box-drawing rails, Nerd-Font-optional (pure-unicode fallback glyphs
  built in). Right-aligned relative timestamps (`3m`, `40s`) in dim text.
- **Motion**: exactly one animation — the HEAD node breathes (500ms ease, glyph
  alternation `●`/`◐`). Everything else moves only when data moves.
- **Density**: every line answers a question; anything that doesn't is dimmed or
  dropped. Truncate with `…`, never wrap — except in the reader, on the reply panel's own lines (a line you are about to send is read whole), a question under HEAD (at its options, never inside the brackets), a lane's finding beneath its row, and the help's legend where its column has rows to spare. The trail earns its 38–44 columns; extra
  width is spent on the panels, never on chrome.
- **The header names the selection**: `⌂ compass · 2 harness · ⌁ harness:1.0` — the
  selected session's digit as its row wears it (the fleet's for life, the
  archive's own where the archive draws it, #32), its name, its tool and model
  where the fleet runs two tools (#50), and its `⌁` tag only when another
  session shares the name — on the one row that is at the same cells at every
  level and width, so a zoom never moves the name and a digit press shows its
  landing (#46). It sheds before the alarm chips, which never do: `· board`, the
  tool, the tag, the search, then the name clips around its digit. Panel titles
  keep the bare name.
- **Useful before pretty**: color and glyphs are reinforcement, never the only
  carrier of meaning. Everything must read correctly in pure monochrome — glyph
  shape (`●▲○◍`) and position carry state on their own. `NO_COLOR` and dumb-glyph
  (`* ! o x`) fallbacks are first-class, not afterthoughts (matters over SSH and in
  odd terminals).
- **Empty states are designed**: a fresh session shows `◉` and its prompt, then
  its present when the fleet knows one ("● scout  thinking…  for 40s") and
  "scouting will appear here" in dim text when it does not — never a blank panel.

## 5. What compass is not

- Not a session manager that owns your processes — tmux owns them; compass only
  observes (plus the keypress-gated actions: `Enter`, `r` — send-keys of a line, a
  menu's digit, or the escape that stop is — and ask; and the event hook, which runs
  only when the config names it).
- Not a tmux layout tool — it never creates sessions, windows, or panes for itself.
- Not a replacement chat UI — input always happens in the real CLI.
- Not a metrics dashboard — no token graphs, cost meters, or charts in v1. The trail
  is a narrative, not analytics.
- Not multi-agent-vendor — Claude-only first (subscription + Bedrock). The engine
  isolates the transcript adapter so others can come later.

## 6. Open questions (cooking together)

1. **Deck fleet density** — with many sessions (10+), does the two-line fleet entry
   (status + location) get cramped? Fallback: one-line entries with location revealed
   on selection. Decide with real data once M0 renders.

## 7. Decision log

| # | Question | Decision (2026-08-30) |
|---|----------|-----------------------|
| 1 | Trail direction | ~~**Newest on top**, like `git log`.~~ **Reversed by #12** after dogfooding. |
| 2 | Lv3 real estate | **50/50 split** of the trail area. Superseded in practice by #13: from Lv2 the reader already owns the middle panel, and Lv3 gives it focus rather than more columns. |
| 3 | Narrator budget | **On by default (opt-out)**, no hard cap — Haiku is cheap enough. Caching and batching stay (efficiency, not cost policing). `narrator = "off"` in config for those who want it. |
| 4 | Fleet scope | **Everything on the machine, one view.** compass runs standalone (own terminal tab, outside tmux) in deck mode and observes all tmux sessions + bare-shell sessions. compass is a pure *consumer* of tmux — it never creates or manages tmux sessions; the user keeps full control of their multiplexer. |
| 5 | Bell policy | **No bells.** Visual-only: amber sort + age in panel, OSC tab-title badge (`⌂ compass ▲2`), optional `compass status` for the user's own tmux status-right. Bells over SSH (e.g. Windows Terminal → SSH → Linux) are unreliable and annoying — permanently out. |
| 6 | Color usage | **Useful first.** Glyph shape and position carry all meaning; color reinforces. Monochrome/`NO_COLOR`/plain-ASCII fallbacks are first-class. |
| 7 | Historian placement | **Suspend-and-exec.** `a` suspends the compass TUI and runs the historian `claude` in compass's own terminal (like `git commit` → editor); exit returns to compass. No tmux involvement — asks are one-off. Leaves `Enter` as compass's only tmux write action. |
| 8 | Fleet density at 10+ sessions | **Two-line entries confirmed** against the M1 golden frames (status line + dim location line). Revisit only if a 15+-session fleet feels cramped in practice. |
| 9 | Live CLI in deck mode | **Yes — the middle panel is a live mirror** of the selected session's pane, streamed read-only via `capture-pane` polling. Glass, not a terminal: compass renders the pane's screen but never takes input for it; interaction goes through `Enter`, which hands over the real terminal (M6). Collapses on narrow terminals. (Possible future: an explicit interact mode forwarding keys via `send-keys` — parked, not designed.) |
| 10 | Fleet liveness (2026-08-31, first dogfood: 280 transcripts vs ~15 real sessions) | **The fleet means "something can need you."** Live = in a tmux pane, plus a small recency door (default 5m, `-live-within 0` shuts it) so a pane-matching miss can never hide a session that is writing its transcript right now. Everything else is the archive: never tailed, never amber, one `A` away, fully browsable (trail/reader/ask all work there). |
| 12 | Trail direction, revisited (2026-08-31, dogfood) | **Time flows downward** — oldest at top, newest at the bottom, **pinned** to the bottom so the latest needs no keys. Newest-on-top was chosen before anyone had used it and fought the thing it describes: a conversation reads downward. Ghost todos moved below HEAD with it (the future is further down, not further up). The trail also became a real viewport — it used to *drop* rows to fit, silently discarding the older half of a long journey. |
| 13 | The trail drives the conversation | From Lv2 the middle panel is the **reader anchored to the trail cursor**, re-anchored on every move: the trail is a minimap, the conversation is the code. Lv1 keeps the live mirror (watching), Lv3 hands the reader focus (reading). `Enter` therefore means one thing at every level — go to the live session. *Superseded by #15: Lv2 has no middle panel; the reader is Lv3.* |
| 14 | What a row says (2026-09-01, dogfood) | **Result, not process.** A fleet row's second line is the last thing the session *finished* — `1216✓ 2✗`, a commit — falling back to the call in flight only when nothing has. "Bash: pytest tests/auth -x" answers whether a session is busy, which the glyph already did; the counts answer whether it is going well, which nothing did. The state word goes with it on the two states the glyph tells you about anyway (`working`, `idle`), and stays on the two it must not let you miss (`needs you`, `stuck`) — the width freed goes to the session's name. And §2.2's own `18✓ 2✗` badge returns to Lv1 legs: the leg has always carried the parsed run, and Lv1 rendered the label and dropped it, putting the one number that says whether a leg went well two keypresses away. Target: a reader spends ~80% of their time at Lv1, so Lv1 has to answer without being left. |
| 16 | The board (2026-09-02, dogfood) | **Lv0: every trail that fits, side by side.** Dropping the mirror left a wide monitor showing one trail and a list of names, and the person looking at it worried about everything it was not showing — the mirror had been answering that worry, badly. The board answers it: the fleet at its 30-column floor, then as many trail columns as fit at 34 or wider, in the fleet's own order (needs-you, stuck, working, idle by recency), the selected session always among them. A column is bright while it has something to read — working, needs-you, or finished within the day and not opened since; `Tab` or `Enter` on it marks it read (persisted in ~/.cache/compass/seen.json, so a restart forgets nothing), and a bright column that has grown since the last look says by how much — "↳ 3 new legs · looked 2h ago" — and dim once it is history (the first dogfood: "porter finished two minutes ago and it's already dim"). The fleet list is not drawn on the board: its rows are the column headers, and the sessions without a column are named in a strip along the bottom. It is the default view on any terminal wide enough; `Tab` opens the selected column as the single trail, `Shift+Tab` from the trail returns. Board → reader is three `Tab`s, so §3's depth rule holds. The fleet keeps its two-line rows (§6's one-line idea is not needed: the board wants the fleet's width, not its height). Every column is polled each tick — the feeds already existed per session; only the selected one was being read. |
| 17 | Operator review, round one (2026-09-02) | **Four readers, four fleets, one afternoon — what they could not tell from the screen.** Four scripted operators (one busy session among many idle; a few in flight; sessions that spawn subagents; a day-long trail) each walked the deck at four terminal sizes and reported what a row failed to answer. What changed: a column's second row is the journey's own **verdict** ("✓ green 312✓", "✗ red 18✓ 2✗ · edited since", "✓ shipped 4m ago", "◈2 out · oldest 12m"), read off the trail, not the fleet feed; a needs-you row leads with **the question itself**, options and all, not the tool's name; HEAD says **how long** it has been open ("for 19m"), a subagent lane carries its clock and its finding at every level ("✓ 7m", "came back with no report"), a ship leg is never a tick and is named by its commit; a trail over two hours carries **hour rules** on its rail; the first ghost rail says "2 of 4 to go"; a column or trail cut at the fold says **what is above it** ("↑ 25 legs above · began 1d ago"); the fleet list says what it is not showing ("▾ 5 more below"); the header's warm chips carry their wait ("▲1 4m") and a calm fleet says "all calm"; numbers mean the same session at every level (the board's order); `g` says where it went; `Tab` into Lv2 on a scrolled trail lands where the eye is; `A` from the board opens the archive as a list. **Round two** (the same four, on the amended frames): a working session's second row is its **present**, not its past — "● build  wiring the filter    for 1h", with "◈3 out" when agents are — and the fleet row says the same words, so zooming in loses nothing; a needs-you row is **the question alone**, no class in front of it, and a stuck HEAD is named by **the call it is hung on**; "↳ N legs · never opened" was a constant wearing a row on every fresh launch and is gone (the delta stays); the header counts **agents out** and is only "all calm" when none are; a Lv1 trail longer than its panel draws **dense** — no air between legs, twice the journey on screen, forks and ticks and hour rules keeping their rows; prompts say "2h **ago**" so a column of figures is not one kind; `[` / `]` step between prompts as the **chapters** of a trail ("◉ 9/13 · "now the audit log" · 14:02"); a finding wraps to a second row instead of losing its second half; a touched row never restates its leg's label; a group header's clock is the wait of the state it echoes, and a header over one row carries no figures; the archive numbers its rows as drawn; the help on a terminal without a board does not describe one. **Round three**: a working column's second row is the **verdict** after all — HEAD is the column's last row anyway, so the header says what HEAD cannot ("✗ red 18✓ 2✗ · edited since", "◈3 back · 1 empty"); the fleet row keeps the present; the tmux session falls to the third row when the second has no room, so no column is attached to blind; a tall board with short trails wraps into a **second band** of columns instead of naming half the fleet in the strip over twenty blank rows (a day-long trail keeps the whole height); HEAD wears the **fleet's glyph and wait** when the session is hung or waiting ("◍ build  Bash: python backfill.py --all   silent 4m", "▲ design asks you   waiting 4m") and a question too long for the row is spelled out beneath it, options and all, at every level; a wide Lv1 panel spends its width **inside the row** — the failing test, the bug, the files beyond the label, beside the label — and dense drawing applies at Lv2 too; a Lv2 child never restates its parent (the run summary the badge carries, the commit a ship leg is named by); a test leg with no verdict is a row that says "?", never a tick; prompt rows carry their **chapter** ("◉ 9/13"), the first hour rule of a new day names the day ("│ Tue 00:12 ────"), a finding wraps to three rows; the fleet's group header never repeats the clock of the row beneath it and a cut list never ends on a header; keys that move nothing say why ("at the present · k goes back", "no board under 110 columns"). The walkthrough's reader now has a real conversation behind it — tool calls with arguments, results, errors — so Lv3 is judged on the product, not a placeholder. **Round four**: HEAD says when its session is **parked on its agents** — "● build  measuring   waiting on ◈2 · 30m" when nothing of its own has been written since the newest dispatch — which is the question "is the lead blocked?" answered from the parent's transcript alone; the working fleet row carries the **newest verdict** beside the present ("● fix  tokens.py   18✓ 2✗ · for 3m"), so a red suite is not a board-only fact below 110 columns; a stuck HEAD's "silent 4m" counts from the last thing written, the same clock as "no output for 4m"; one meaning per clock on the rail ("⋯ 20m out", "✓ 7m ago", "◈3 out 20m"); the fleet's group header reads as a place ("⌁ work"); the cursor keeps the row's glyph ("◉▸"); the row above the fold adds the day up ("↑ 138 legs above · began 22h ago · 7 ships · 2 red"); a plan says "2 to go · 2 done" and draws up to six ghosts; a long question gets four rows; a band may fold one column a little and never two; `Tab` from Lv1 **skips a Lv2 that would show nothing** beneath any leg and opens the reader on the present (Shift+Tab still reaches the cursor); the reader's title names the moment it is anchored to, a call nothing has answered is marked ("⎿ ⋯ still out", "⎿ ⋯ no result yet"), and every key that moves nothing says why (`ctrl+u` on a trail that fits, `G` when pinned, `j`/`k` at either end of the conversation); `[ ]` is in the footer. **Round five**: one sentence for a working or hung HEAD at every depth — "◈3 out 20m · quiet 15m" when the parent is parked on its agents (the wait, not the leg's age), "◈3 out 20m · for 2h" when it is not, "◍ build  Bash: python backfill.py --all   silent 4m" when it has hung — and the fleet row is that sentence verbatim, the verdict beside it dropped first when the row is narrow, the leg's own label when it is parked (the plan's task is the work it gave away); the fleet's age for a hung session is its silence; **"unread"** is the state word for a session that finished within the day and has not been opened, so monochrome and the board-less widths read it; the trail's title adds a long day up ("TRAIL · auth · 22h · 13 ships · 8 red") and keeps "↑ 128 legs" beside "↓ G" while scrolled; the fold row goes compact ("· 14⚑ 9✗") where the long form does not fit; Lv2 at a wide width **keeps the row as Lv1 drew it** and hangs beneath it only what the row could not carry (the cursor steps over rows that are not drawn); a long question flows from HEAD's row into the rows beneath; the board **packs its bands** to their tallest trail; a lane with a live session whose first prompt begins with its label is marked "→3" — a hedge, and `3` goes and looks; the archive's trail is titled by the row; the reader takes the fleet's width at Lv3 on a deck under 150 columns, names the row it is anchored to, spells an AskUserQuestion out on its call line ("⎿ ⋯ no answer yet"), steps its turns with `[ ]`, and its first `j` after a Tab moves; Shift+Tab skips a Lv2 that Tab skipped; the footer sheds its optional fragments before it clips. **Round six**: the parked sentence is the last thing a row drops — a narrow fleet row reads "● build  ◈3 out 20m · quiet 15m" with no label, never "◆ build  20✓", and a narrow HEAD keeps its whole figure over its label; the column's verdict says "quiet" too; an agent back with nothing wears ⌀, never the tick, and "→3" is in the legend; the header's stuck chip counts the row's silence, and a group header never repeats the row beneath it even when it echoes; a working row gives up its verdict before its subject is cut short; the reader's title carries the row as drawn ("auth: push feat/sessions · 15:28"), wraps a call line rather than clipping the question, says "the whole conversation is on screen" when it does, and has the whole width at Lv3 below 110 columns; a detail row never leads the trail's viewport (its parent takes the top row); the trail's title goes compact where the day does not fit; a band of eight rows is still a band; a cut fleet never ends on an entry's first line; the archive footer promises `tab` and `a`. (A note on the Lv2 skip: it fires only when no leg has any detail beneath it at any width; a trail whose detail rides on a wide row keeps Lv2, because the cursor is how the reader is aimed at an older leg.) **Round seven**: below 110 columns the reader's scroll arithmetic uses the width it is drawn at (it was clamping against a two-column document and drawing a one-column one, so `k` moved nothing at 100 columns); a working row gives up its verdict before its subject is cut at all, and the narrowest row gives up the class word before it clips the sentence; verdict clauses are shed whole, never cut mid-number ("✓ shipped 56m ago", not "✓ green 12…"); a header beside an echo carries no clock, and "⌁ work" is a header to the cut rule; the reader's title gives the anchored row the room the name leaves, and a result that is not the last call's says whose it is ("⎿ ↩ Agent(score the gates)"); a lane's "→3" survives a clipped label; the trail's title sheds the day whole when even the compact form does not fit; the narrow help keeps ⌀ and ◌; the archive footer promises `tab` and `a` at every width; the walkthrough's hung call follows the leg's own result, as a transcript has it. **Round eight (verification)**: the board's fold tally is the whole day's, the same numbers as the trail's title, whatever the height hides; a single reader step never lands the page on a line of air; the reader's footer keeps its keys beside a note (the keymap sheds fragments for the note, and the note is clipped before the keys go); without a board, selecting a session opens its trail and clears "unread". The four operators each reported their scenario done bar these. **First real dogfood of the branch** (the person, on their own fleet): a tick carries the leg's label ("│ build  go build ./…  1m", not "│ build"); a narrow HEAD keeps its label and the first clause of its figure, the reviewers' preference for the whole sentence over the name reversed by the person reading it; an idle session's open lanes are **lost, not out** ("⌀ lost 3d ago", "◈1 lost", and the header no longer counts them); the legend explains "◉ 10/11"; and **Lv2 opens the reader** beside the trail, following the cursor — one Tab shows the conversation at HEAD, j/k walk it back leg by leg, the second Tab hands the keys over. The Lv2 skip is gone with it. Still held: heartbeat on an open agent lane and Lv3 anchored to an agent's own conversation (both need the subagent transcripts, which compass did not read yet — it does since #49); folding repeated build⇄test cycles (withdrawn by its proposer: the long trails rotate through seven classes, and a fold would swallow the ships between them); nesting teammate columns under their parent (no reliable link in the transcript). |
| 18 | Three levels (2026-09-02, dogfood) | **Board → session → reader, on any terminal with a board.** The single trail with the fleet list beside it (Lv1) was the board's column drawn wider next to a list the board already is; the person reading it asked what they should expect there and could not answer. One `Tab` from the board now opens the **session view**: the column's own header card (number, glyph, name, state, verdict, tmux session) over the trail with its cursor on the present, and a **companion** panel beside it that follows the cursor — the conversation by default, the live tmux pane while `m` has it (the choice sticks, and the pane gives way to the reader when the keys move into it). `j`/`k` walk the legs, `h`/`l` slide to the neighbouring session at the same depth, numbers jump, `[ ]` step chapters, `Enter` attaches. The second `Tab` hands the keys to the conversation. No fleet list: the board is the fleet. Below 110 columns nothing changes — fleet and trail at Lv1, the cursor at Lv2, the reader alone at Lv3 — and the archive stays a list at every width, because a list is what it is. `h`/`l` and the arrows also run across the board itself. The Lv2 skip of round four is gone: the session view is what Lv2 shows. |
| 19 | The trail leads, the reader is a witness (2026-09-02, dogfood) | Three questions from the second real look at the session view. **Which side?** The trail on the **left**, the conversation to its right: the trail is the column the board handed over, the keys are on it, its card names the session, and a navigator stands to the left of what it navigates (the tree beside the file, the outline beside the page); the conversation was on the left because the reader wants width, and it still has the width — width is not a side. **Is the conversation readable?** It was a wall: absolute paths, every result "6 lines · space unfolds", prose wrapped to 150 columns, calls and prose in the same colour. Now a call's path is relative to the session's directory as the CLI shows it (`Edit(internal/ui/reader.go)`), and its argument is dim so a run of calls reads "Read, Read, Edit, Bash" and the prose is the only thing in the page's own colour (a question to you and an agent's assignment stay plain); a folded result leads with the first line that says something and how much follows ("⎿ 312 passed in 4.21s · 2 lines", never pytest's row of dots), a file's contents are counted, and "space unfolds" is the footer's line; prose wraps at 100 columns however wide the panel is; the model's markdown gets the little a terminal can honour — a heading bold, a fenced block dim and unwrapped, a bullet a bullet, the `**` dropped; and a `❯` turn carries its clock on the right, so `[ ]` lands on a moment with a name. **What is the product?** The **board and the trail are one feature** — the trail is the unit the board is made of, and the board is the thing nothing else gives you: every session's journey side by side, what changed while you were away. The reader is not a second product and never competes with the CLI at showing the CLI's own transcript; its job is narrower and worth doing: check the trail's claim — this leg, this moment, what exactly happened — without leaving compass, anchored to the row the cursor is on. So the reader is judged on being readable enough for that, not on being a chat UI (§5 stands: input happens in the real CLI). Depth: board → session → reader is unchanged. |
| 20 | Second look at the session view (2026-09-02, dogfood) | Four asks. **"When I Tab, the trail says Lv2 — expected?"** No: the tag now says where the keys are in the help's own words — `[session]`, `[reader]`; the board-less deck says `[trail]`, `[legs]`, `[reader]` — and no level number is drawn anywhere. **"`[ ]` in the reader works but highlights nothing."** It lands on a turn of yours and says so: the turn wears the inverse bar, the title names it, the footer counts it ("❯ 3/12 · "now the audit log" · 14:02"). **"Can the trail say more from the data?"** Two things it could and now does, both read off the transcript it already has. A failing test that has failed before says how many legs it has now failed in — "✗ test_refresh_expired_token · 3rd time" — which is the plainest sign of a loop a trail can show, counted in the segmenter where the failure is recorded. And a **compaction** — the harness folding the conversation into a summary because it ran out of context — is a rule on the rail with its clock ("│ ⟲ compacted 14:02 ────") and a count in the day's totals ("· 2 compactions", "2⟲"): a session compacted twice is working from a summary of a summary, and that is the trail to read closely. Held for now: the time the session spent waiting on *you* at each prompt (the gap between its last event and your next prompt, summed on the board) — real, but noisy when you were away on purpose; and hot files ("tokens.py, 4th leg"), which the repeat-failure mark mostly covers. **"Quick replies from the board?"** Yes, and it is compass's second write after attach. `r` at any level puts the replies on the footer for the selected session — "please continue", "report status", "you were stuck on the quota limit; it's back now — please resume where you left off", or the `reply = "…"` lines the config names, up to nine — and a digit types the line into the session's tmux pane and presses Enter (`send-keys -l`, then `Enter`, as a person would). Any other key puts them away and sends nothing; a session without a pane, and read-only, are told why. §5 stands: compass is not a chat UI and the input still happens in the real CLI — a quick reply is compass pressing the keys you were about to. (A session sitting on an AskUserQuestion menu will receive the line as keystrokes into that menu; the footer names the pane so you can see what it is doing first.) |
| 21 | Waiting on you, and the reply panel (2026-09-02, dogfood) | **"Go ahead with the waiting-on-you time on the board."** A session that has finished its turn is waiting for your next prompt, and that gap is time the session spent *on you* — the number that says which of a fleet your own turns are the bottleneck of. It is summed over the trail's prompts (the gap from the last thing that happened — a leg's end, a lane's return, the prompt before — to the prompt), a gap over three hours is not counted (that is you being away, not the session waiting), and a live session still idle or asking adds the open wait. The board's verdict row says "on you 1h20" as its last, first-shed clause; the day's totals say "waited on you 1h20" ("on you 1h20" compact); a prompt row wide enough says how long it sat before that prompt ("waited 40m · 2h ago"). Under five minutes is not a word. **The reply panel.** `r` on the footer was easy to miss and the person pressing it expected something to pop up: the quick replies are now a small boxed panel floating over the deck — "reply to api · ⌁ dev:1.0", the numbered lines, "a digit sends the line and presses enter · esc closes" — with the deck still visible around it and the footer saying "reply: 1–3 sends · esc closes". |
| 22 | Operator review, round nine (2026-09-02) | **The same four operators, on the second look's features.** All four said "not yet", and mostly for the same reasons. **The reply panel** names its target by the board's number as well as its name and pane ("reply to 1 · harness · ⌁ harness:1.0" — two sessions can share both), says what that session is doing before a digit is pressed ("▲ on a question · Open port 22…? [office CIDR / keep bastion] — the line is typed into that prompt", "● working for 2h — the line queues behind its turn", "○ idle 30m — waiting for a prompt"), wraps a long line instead of stretching across the deck, wears a column of air on each side, and stands beside the session it is about — under the selected column on the board, over the companion in a session, over the trail on a narrow deck — never over the row that names it; `r` is in every footer, shed first when a footer is tight. **Waiting on you** says "on you 1h20 today" and only when there is a day to add up: an idle session's open wait alone is the age two rows above, and saying it twice taught the eye to skip the clause; the prompt row says "waited 40m ·" wherever a readable prompt is left beside it, not only past 80 columns. **The session card's second row carries the day** — verdict, then "22h · 16 ships · 10 red · 2 compactions · waited on you 40m", the tmux name after — since the session view is the one place a long day is read closely and was the one place the day was not said; a stuck or waiting session keeps the hung call or the question there instead of the verdict; and at Lv3 the card drops its tag and `[reader]` moves onto the reader's title, where the bar is. **The repeat mark** fires from the second failure and names its unit — "✗ test_x · 2nd leg", "· 10th leg" — and the board's verdict row says "same test 3rd leg" beside a red run, so a test → fix → test loop with the same counts reads as a loop from the board. **A compaction** says what it is — "│ ⟲ context compacted 13:00 ───" — so it is not another hour rule, and the legend defines ⚑ beside ⟲. **The reader** names a result that landed late at its own depth, in words ("↩ result of Edit(tokens.py)"), so an agent's block holds only the agent's output; a still-out agent carries its clock ("⎿ ⋯ still out · 20m"); an Edit says "edited · +12 −3" and a Write "written · 12 lines" instead of restating the call; a page opened on a tail says so under its title ("↑ 212 lines above · 3 turns of yours"); and `[` from a fresh page lands on the turn governing where the reader is anchored, so a conversation that fits the panel no longer answers "no earlier turn" with a turn in plain sight. **The mirror** titled "· live" over transcript text was taken for the pane: with no capture yet it says so. **Keys**: a second `ctrl+u` at the top of a trail says so; `A` from the archive returns to the board it was pressed from, and with nothing archived changes nothing. The help's new legend lines fit their column. Held: a group header over several sessions carrying one row's clock; HEAD shedding "quiet 15m" before its label; the fold row choosing compact and long forms per column. |
| 23 | Operator review, round ten (2026-09-02) | **Two of four said good to go; the other two were one row away.** The reply panel now blanks the rest of every row it covers (a column's tail sliced at its border read as a leg with no label) and its working line carries the turn's own clock — "● working for 2h", or "◈3 out 20m · quiet 15m" — not the last write's, which had said "for 40s" over a one-hour turn. The session card's second row says the wait once (the day's total carries it; the verdict's clause is dropped when the day is drawn), always keeps the tmux session (the day's clauses go first), and for a working session with nothing to count says the present rather than the last finished leg. A compaction is drawn under a fork or a tick too — the porter trail's "1⟲" had no rule to point at. The reader's title never clips its own name: the anchored row is cut with a mark, the tag goes before the row, the name stays. A moment that lands on a result lands on its call. A call whose result landed after other calls says "⎿ ↩ result below" at the call site. A fold's remainder wears one shape — "5 more lines" — on clean and failed results alike ("+1 line" beside "edited · +1 −1" read as a diff stat). The repeat mark is "· 10th failure", not "· 10th leg" (beside "↑ 128 legs" the leg count read as the leg's number); the board says "same test 3rd failure". `r` is in the reader's footers; a note's own keys are the last a footer sheds; the two refusal notes are short enough to fit beside the keys; the footer says "space unfold" and "reply: 1–3 types and sends". The day's totals count from an hour, not two. The legend defines `?`. The walkthrough now presses `r` on an idle session, opens a two-turn conversation at Lv3, and the long scene's prompts sit a few minutes after the leg before them, so "waited 9m ·" renders. Held: the reader's title naming the level rather than the page; `[` on a turn already on screen moving the page; "same test" shed on a 35-column board column. |
| 24 | Operator review, round eleven (2026-09-02) | **All four said good to go.** What they listed as polish went in the same afternoon: the board's repeat clause is "· 2nd failure" (ten runes shorter, so a 35-column column keeps it); a row the reply panel's edge cuts ends in "…" ("shipped" alone had inverted "shipped on red"); a chapter note keeps the chapter keys over everything and any other note keeps the reader's own; the mirror's transcript fallback draws the same document as the reader, clocks and all; a stuck or waiting card's sentence has its column padding taken out ("sile…" was the padding, not the width); the reader's title on a question carries the whole question and clips it with a mark; the porter walkthrough's second prompt and compaction sit in time order. Held for another day: `⌁` and `?` in the narrow help, a card for a session with no pane saying "no pane", the tmux tag alternating between a card's second and third row across a wide board, the 220-column panel blanking more than it covers. |
| 25 | The quota error, missed (2026-09-02, dogfood) | A session dead on a 403 sat in the session view as "● test  pytest  for 56m" with the refusal in plain prose at the foot of its conversation. The parser read the API-error flag from inside the message only; the CLI writes it there, on the line's top level, as the message's model ("<synthetic>"), and sometimes as nothing but the text a person sees. All four shapes are read now — the text form only when the line opens with "API Error: NNN" (after an optional "Please run /login ·"), never from prose that mentions one — and the reader draws the refusal warm, "✗ API Error: 403 …", not as the model's words. |
| 26 | Replies that answer (2026-09-03, the panel's feedback) | The operators' first ask, three of four: the panel repeated a parsed question with its options and then would not let them answer it. Now the panel offers, in order, **the options of the question the session is sitting on** — sent as the CLI menu's own digit, then enter — the stock lines, and **stop**, which presses escape so the CLI interrupts its turn (the long-session operator: "when it has failed the same test ten times, please continue is the last thing I want to send"). `t` opens a line to type, since what needs saying is different every time ("rebase on main and re-run", "yes, narrow"). Every send leaves `↪ sent "…" · 2m ago` on the board's third row until the transcript shows a prompt at or after it — the trace the "queues behind its turn" case needed. The observe line (§5) is unchanged: input still happens in the real CLI; compass presses the keys a person was about to. Assumption to verify on a real menu: the CLI's AskUserQuestion prompt selects an option on its digit; if it only moves the cursor, the enter that follows confirms it. |
| 27 | Coming back, and what is owed (2026-09-03, the panel's feedback) | Three of the four operators said coming back was unassisted, and two that the board spent its screen on yesterday. **The read-line**: a rule on the rail where the person last opened the trail — "│ you were here · 4h ago ────" — so a return reads what is new from that line down. **The digest**: the column's third row adds up what came after the look — "↳ 38 new legs · 1 ship · 6 red, all test_x · looked 4h ago" — the sentence a person back from lunch assembles by hand. **Circling**: a session whose newest run is red and whose test has failed in three legs or more is a loop the state machine called healthy and busy; it is counted in the header ("↻1 circling", never "all calm"), in the terminal's title ("⌂ compass ▲1 ◍1 ↻1"), and sorts with the alarms. **Obligation**: the board is ordered by what a session owes you — a question, a hang, a loop, work in flight, an idle session that stopped red or with steps left, one not yet read, and the rest — and only what owes you gets a column; the shipped-and-read go to the strip however many columns are free, so "all calm" means nothing owes you. The board's trails are always packed: a rail row between every leg halved what fit. **The hook**: `hook = "…"` in the config names a command run on the crossings that matter while nobody is looking — into needs-you (api_error when it is one), into stuck, into circling, lanes returning — with COMPASS_EVENT, COMPASS_SESSION, COMPASS_TMUX and COMPASS_DETAIL in its environment; the first refresh is a baseline, not an event. No bells (#5) stands: silent and passive were never the same call. **Hiding** (asked for directly): `x` takes a session off the board — a test, a /resume — persisted beside the seen-times; the archive lists it, `x` there brings it back, and a hidden session that needs you or hangs comes back on its own. |
| 28 | Search and compare (2026-09-03, the panel's feedback) | **The fleet search**: `/` below the reader narrows the board, the list and the archive to the sessions whose name, opening prompt, branch, any prompt, a leg's label or a touched file matches; the header says "/query · 3 of 40" and `esc` clears it. Three hundred archived sessions were a corpus reachable only by `j`. **Overlaps**: the board's strip names two live sessions on one thing — "⚠ api and webapp both touched tokens.py in the last 20m", "⚠ auth and etl are both failing test_logout_nil_session" — since compass is the only thing in the room reading every transcript and was letting the person diff two columns by eye. Held: reading the subagents' own transcripts (the delegating operator's one change) waits on a sample of the files Claude Code writes for them, so the layout is read rather than guessed. |
| 29 | Operator review, round twelve (2026-09-03) | **The same four, on the feedback batch, against §3–§5.** All four said "not yet", and named the same seams. **The archive lists the hidden**: `A` opens for a hidden session with nothing archived, under a first group "hidden · x brings one back", with the row's real glyph and "hidden · " before its title; `x unhide` is in the archive footers; a hidden session that begins circling comes back on its own, and hidden sessions are out of every header count. **Circling has a row**: ↻ and the word "circling" on the session's own row, in the legend, and in the strip; the list floats a group holding an alarm above the rest (§2.4's tmux grouping stays within that); and the loop is keyed on the last three runs with a verdict — a red one among them whose test has failed in three legs — where a green run as big as the red one ends it and a narrower one (a subset, "pytest tests/auth 312✓" between two red full suites) does not. **The header says what owes**: "2 owe you" and "3 unread" chips, and "all calm" only when nothing owes. **The panel names its keys**: each group wears its mechanism — "answers · sent as the menu's own digit", "lines · typed into the prompt and entered", "stop · escape, which interrupts the turn" — the question's state line says "pick an answer below, or type a line", stop is offered only where a turn is running, and the walkthrough now sends one answer so the board's `↪ sent` is seen. **The search echoes**: the query narrows the fleet as it is typed, the footer shows it, esc restores the selection it was pressed on, and it clears only from the board or a list. **The read-line survives the look**: marking the look on the way in had erased the line the way in was for; the look before the current one is the line while the session is open. **Width and air**: the board's width is shared by the columns drawn (cap 64), the strip sits under the last band rather than the screen's floor and keeps the clauses that carry a key whole, and the digest counts the lanes dispatched since ("3 agents out, none back"). **The hook** fires once per session and event in ten minutes, once per set of lanes with the empties counted, and on "shipped on red"; §2.5 and §3 now name all three writes and say `readonly` does not cover the hook. §4 names the three places a line may wrap. Held: stable digits under hide and search (the numbers are the board's order, and reviewers were split); `→N` under a search; "circling" on the terminal title cannot be seen in frames. **Three new personas join**: the first five minutes (one session, an 80-column terminal), an alarm storm (three dead on quota, a question, a hang, a loop), and fleet hygiene (two live sessions with one name, a relayed prompt, no pane, the archive cliff, forty archived sessions with four names). |
| 30 | Operator review, round thirteen (2026-09-03) | **Seven operators — the four, plus a newcomer on one session, a triage operator in an alarm storm, and a fleet-hygiene operator with namesakes — on the amended batch, against §3–§5.** All seven said "not yet", and the consensus was about identity and honesty rather than polish. **Dead on the API is its own state** (the storm's one condition): `⊘` and the word "quota" or "api error" on the row, its own header chip with its wait, its own place in the obligation order — under the loop, since nothing typed clears it — `g` skipping it, HEAD ending on the refusal ("⊘ api … dead 18m"), and a reply panel that names the refusal and offers the remedy (`/login`) instead of "pick an answer below" over stock lines that would start a turn into a 403. **Digits are for life**: a live session takes its number on first sight and keeps it — rows re-sort as what they owe changes, the numbers do not — so `3` from muscle memory lands on the session it landed on this morning, in a fleet holding two sessions called `harness`; two live sessions in one tmux session are tagged by pane ("⌁ harness:1.0"), a paneless one says "no pane". **The header counts once**: a circling session under ↻ with the loop's age and nowhere else (the tally summed to one more than the board drew); "N owe you" and "N unread" beside the states, and "all calm" only when both are zero. **`x` is honest**: a session that is asking, hung, circling or dead stays on the board and the note says so ("etl hangs · it stays on the board") — the note had reported a hide that did not happen; a hide names the way back ("A, then x brings it back"), moves the selection to the neighbour, and the archive row keeps the session's name and pane under the hidden header. **The read-line draws before a tick** as before any row — a look that fell in front of a tick never drew on a long trail while the board billed for it, three rounds running; the compaction rule with it. **The third row is the tag's row** at every width: the digest or the sent-trace on its left, the tmux session on its right, neither evicting the other; the trace says which key it pressed ("↪ answered 1 · "office CIDR""), and below 110 columns rides under the fleet row with the digest, where the overlap line now also stands. **The reply panel blanks its own box** and nothing else, marks the cut on both edges, and stands beside a fleet list rather than on it. **A band is as tall as its tallest trail** and the strip follows it; a lone column takes the width. **A question breaks at its options**, never inside the brackets. **Typing a fleet search survives its own narrowing** — moving the selection to the match was ending the search, so the next key went to the deck and `enter` attached instead of keeping the query (the "search echo vanishes" three operators reported). A narrower green run does not end a loop; a loop's test counts as failing for the overlap line; a lead parked on its agents past ten minutes owes more than a session that is fine; inside a tmux group the calm tiers are one tier, so reading a row does not re-sort the list under the cursor; the view's own keys shed last from a footer. From the newcomer: a fleet of one opens on the session view; the bare HEAD row draws before its lane is built; silent keys speak; `x` refuses the only session; the quota line is offered only where a quota was hit; the help splits its columns to the keys' longest line, wraps definitions where the rows allow, and keeps `esc`, `?` and `q` when it must cut. Held: the relayed prompt marked as a relay (no reliable preamble in the transcript); a "recently live" group in the archive for a session whose pane closed; `[ ]` stopping on a compaction; attach leaving a note (outside tmux compass hands the terminal over, so there is no frame to say it on). |
| 31 | Operator review, round fourteen (2026-09-03) | **The same seven, on the amended batch.** All seven said "not yet", each for one thing, and five of the seven things were the same field: **the reply panel named its session by board position** while the rows wore digits for life, so "reply to 4 · api" stood over a list where 4 was billing — the panel now prints the row's digit. **The dead panel's remedy is the bytes typed**: `1  /login` under its own head, the trace quotes `"/login"`, and the quota line stands under "lines · start a turn — only once the quota is back" rather than as a reply; a dead session whose last leg had closed now ends its trail on the refusal ("⊘ api … dead 18m"). **A working session with no leg yet shows its present** — "● scout  thinking…  for 40s" — on the trail and the fleet row alike, which was the newcomer's whole first screen; a fleet of one opens on the session view (the walkthrough now takes the launch path, two polls), and its footer offers no key that moves between sessions. **The read-line stands among a leg's lanes**, before the first lane that left after the look, so a lane back since is below it where the digest counts it; closing the session view commits the look, so the digest stops billing for a session just read. **A loop's verdict leads with its red** ("✗ red 310✓ 2✗ · 10th failure · pytest tests/auth 312✓ since"), since the narrower green that does not end the loop was crowning the row green under a circling glyph; a loop in flight wears the loop's age, an idle loop its wait for a prompt. **The pane is the session's, not the board's**: namesakes are tagged by pane on the list's third line below 110 columns and on the session card, and a hidden namesake still counts. The group header echoes the worst row as the board ranks it (a ▲ over a group of ◍ ↻ ⊘ pointed at the wrong group); the fold counts sessions, not lines that open with a glyph; a search moving the selection is not a look; the answered trace keeps its clock and sheds the quote first; footers shed "(prefix d returns)" before a key or a note; the hide note names the digit and the pane, the list's last line keeps "1 hidden", the narrow archive footer carries `/` and `x`, the archive keeps a hidden session's digit and says "no pane"; the strip and the fold row shed names and clauses whole; the overlap line goes compact where the sentence does not fit ("⚠ webapp, api · tokens.py · 20m"); the trail's title keeps its compact day below 110 columns; a lone column under a full band widens to the cap; the panel stands under a short band; a question's HEAD row breaks at its options at every width; an unwrapped turn that just fits keeps its clock; the reader says "⎿ ⋯ no reply yet" under a turn nothing answered; a one-row trail says so; the mirror's key reads "m conversation" while it is on; the narrow help keeps `/ n N` and drops `m`, and legend definitions wrap into the rows that are free. Inside a tmux group the rows follow the board's order (#11's window.pane order yields to it). The alarm-storm, few-ongoing and fleet-hygiene walkthroughs gained scene-specific keys, so the dead panel, a real hide and the namesake's panel are witnessed. Held: the reader's read-line; `[ ]` on a compaction; the relay mark; the archive under `/`; "· 1 ship" under "✓ shipped". |
| 32 | Operator review, round fifteen (2026-09-03) | **The same seven, on the amended batch; the newcomer said good to go.** Three of the six "not yet"s were one gap in the walkthrough, not the product: the harness dropped the command a typed line's `enter` returns, so `t`, "go on", `enter` left no trace in any frame — the walkthrough now plays that send in like a digit's, and the typed line's trace is a named test. Three were one field: **the archive numbers its rows as drawn, hidden rows included** — a hidden row wearing its fleet digit over rows numbered by position gave one digit two sessions, the round-fourteen defect moved from the panel into the archive; the archive also opens on the hidden row when something is hidden, and `x unhide` is offered only with the cursor on one. **The cursor's row is never drawn over**: a child on the top row of a scrolled viewport keeps the row beneath its parent, since the parent-takes-the-top-row rule had scrolled the cursor off the screen at 80 columns; a pinned column that opens on a lane opens on the lane's leg. **The looks compass takes on its own are not looks**: a hide moving the selection below 110 columns no longer reads the session it lands on, and leaving a session there commits its look, so the digest stops billing for legs just read. The walkthrough seeds a look on every live trail worth it (the lead's read-line beside its compaction is finally in a frame), selects the board's first column after the trails are known, and presses `j` at Lv2. Also: the digest leads with the lanes; a loop's count comes before its caveat; an idle loop whose "· idle" must go takes the loop's age with it; the narrow quota head keeps its warning ("lines · only once the quota is back") and the dead panel closes at 80; the strip never sheds "+N more" (the overlap sentence goes compact first, fact first: "⚠ tokens.py · webapp, api · 20m"); the trace never cuts inside its quotes; a footer note sheds its clauses whole (the pane before the way back) and `? help` sheds last; the list never opens or closes inside an entry; the list's last line keeps its key ("· A") when narrow; a query the open session fails clears on the way out to the board, and the reader's title never echoes the fleet's draft; `[session]` is the word on a deck with a board, card or no card; the list's empty state under a query is two rows; the paneless `r` refusal and the archive's attach key say "no pane"; a card's question sheds its bracket clause whole; what stopped short floats above the read and unread in a tmux group; the help is one column below 150 unless a single column would cut the keys; `space` survives the narrow help. §4's empty state names the present; §2.4's ⊘ cell names the quota line as a turn. Held: the read-line in the reader; the Lv2 cursor on a detail row; "· 1 ship" under "✓ shipped"; a session's day on its card after the verdict; the reader's read-line. |
| 33 | Operator review, round sixteen (2026-09-03) | **Seven operators; the newcomer and the many-idle operator said good to go.** What held the rest: a fleet of one had lost its way out of a standing search (no board to zoom out to, so the query the session failed stood for the rest of the walkthrough) — the zoom-out refusal now clears it; the narrow trace row shed the bytes before the clock, so three dead rows read "↪ sent · 0s ago" alike — a typed line now keeps its bytes over the clock and an answer its digit and the clock, and the digest's clauses ride after the trace as many as fit rather than being evicted; the loop's fleet row without a board carried no count — a circling row's second line is now the verdict with its count; the list still opened inside an entry at 80 where backing up would have dropped the selection — it now opens on the next entry, the cut one counted above; the session card at ≥110 cuts a question's bracket clause whole; the session card carries the column's third row (trace or digest), since a fleet of one never draws the board; the reply panel stands above a row it would otherwise cover; `m` below the board's width no longer flips a mirror out of sight and refuses in the archive; a search's or an archive's landing is never a look, coming or going — only a session the person opened has its look committed on leaving; `enter · no pane` is said for any row without a pane; the hide note is the strip's own form ("3 notebooks hidden · A, then x"); the help's aside sheds whole and its multi-word terms never wrap inside; `G` on a one-row trail says so. Held: the trace displacing nothing now; the reader's read-line; a Lv3 page opening on air; the 80-column archive trail's title; `a` in the 80-column help. |
| 34 | Operator review, round seventeen (2026-09-03) | **Seven operators; the many-idle, the alarm-storm and the fleet-hygiene operators said good to go.** What held the rest: the archive drew the mirror after a board press of `m` had said only that the mirror shows beside a session — the archive is a list at every width and the flag stays with the session view; the reader's title cut a question inside its brackets where the card cut the clause whole — the title now follows the card's rule; the reply panel stood under a short band on the head rows of the band below (a band with a verdict and a trail and no name) — it stands under that band's head rows instead, its air shed first, and a dead session's panel states itself on one row so it fits on its own band's trail rows; the fold's "▴ 1 more above" hid a group's header and left "⌁ :0.0" under it as a pane of no tmux session — the fold row names the group it cut into; the footer's pane rule had never fired (a byte-count slip on the separator), so keys were shed before the note's pane clause — a bare pane tail now goes before any key, and a trace's destination ("to ⌁ ops:0.0", where the bytes went) outranks the optional keys and yields only to `? help` and the way out; the one-column help filled its spare rows with the loop and the trace before the tag and unread — the read-line, then the tag's line, come first; the two-column legend broke between a glyph and its term — a glyph binds to its words; a folded list short of its body left its slack under the archive's line — the slack sits between the fold and the strip; the archive's narrow title fell back to "TRAIL" alone — it names the project. Held: the board's digit and the archive's digit differing (the archive numbers as drawn); the relayed prompt's mark; a "recently live" group; the archive under `/`; `⊘ api` as a leg class absent from the seven; the digest riding after a trace only where its whole clause fits with the two-cell gap. |
| 35 | Operator review, round eighteen (2026-09-03) | **Seven operators; the many-idle, the few-ongoing, the alarm-storm and the fleet-hygiene operators said good to go.** What held the rest: below the board's width the legs and the reader named neither the help nor the way out — every session-view footer ends `· ? help · q quit`, and the narrow list offers `tab deeper`; a key shed for a clause the note then gave up stayed shed — the keys are shed against the note's final form, from the whole keymap, and `? help` goes last of all, only when the note's first clause alone cannot stand beside it; an answer's quote is its label and goes before its destination, a typed line's destination goes before its bytes; a note's quote is clipped to the footer's room rather than capped where it was made; a result landing after the person's next turn was drawn under `❯` as the prompt's reply — it is named at its call and where it lands, as a result landing after other calls is; the fold row carried the group's echo glyph and fell back to the count — it names the group alone, and a fold over a header and its air is no fold; the narrow help spends its rows on the lanes rather than the class names, its folded line leads with the lanes, and a row left after the fold takes the tag's line; the two-column help leaked its binding spaces; `m` in the session view says which side it turned to; `[` on a one-row trail says so; a refusal on a dead session reads `billing stays · dead on the API`, short enough to stand whole beside the help at 80. Held: the Lv2 cursor on a dead session stands on its last leg — the `⊘` row is a state, not a leg, and the row beneath says dead; the 120 help's fill keeps the compaction line over the trace; a query-narrowed list's strip follows the list; the trace row's digest only where its whole clause fits. |
| 36 | Operator review, round nineteen (2026-09-03) | **Seven operators; the many-idle and the alarm-storm operators said good to go; the rest held one row each.** What held them: below the board's width the list shed `tab deeper` first and kept `x hide` and `g grab`, which refuse on a fleet of one — the shed order is level-aware (on the list the way in outlasts them; deeper in, the chapter keys outlast it), and a fleet of one offers no `g`; in the session view at 80 a note's row kept `enter attach` and `esc back` and shed `? help`, the one row that deck finds the help from — under a note `enter` and `esc` go before the note's own keys and before the help, and three notes that stood a column or three too long are shorter; the reader's turn note was still capped at 40 where it was made — no note caps its quote, the footer clips it to its room, at a word boundary, and under a dozen characters sheds it whole; a chapter key's refusal is a chapter note and keeps the chapter keys; the 100 help's row of air under `keys` outranked the tag's line — the air buys the tag's line back when a legend line folded away; the 80 help defined the read-line and not the lanes — its trail row carries them when the lanes' own row does not survive; the reader's tail page opened on a bare `⎿` whose `↩ result of Bash(…)` was the last line above — the page never opens on a result row without its owner. A trace's precedence, said once: an answer keeps its digit and its destination over its quote; a typed line keeps its bytes over its destination; either destination outlasts the optional keys and yields to `? help` and the way out. Held: the Lv2 cursor on a dead session; the 152 card's question wrapping at the row's edge; the help spending a row on lanes a scene never draws (the help is not scene-aware); the 120 help's fill order; a query-narrowed list's strip. |
| 37 | Operator review, round twenty (2026-09-03) | **Seven operators; six said good to go.** What held the seventh, and what the six named next: the page rule of #36 stalled `j` in the reader on any call whose result was the next row (the top backed up to the call and nothing moved) and cut the tail page one row short — a step down steps past the block, and on the tail page the last row wins: the page opens on the result and the row above names its owner (`↑ 25 lines above · ↩ result of Bash(…)`); the reader's `]` refusal (`no later turn`) was not a chapter note and shed `[ ] turns` — it is one; the shed was greedy and never backfilled, so a list with 14 free cells offered none of `x hide` and `g grab` — shed keys come back, most recently shed first, where they fit, and `n/N` only with its `/ search`; the archive's hidden row lost `x unhide` to the way in — on the list it outlasts `tab deeper`; on a fleet of one `x hide` refused when pressed — it is not offered; the 80 help's read-line row carries `⌁ its tmux pane` when no row of its own survives; the board's `m` note reads as a state (`mirror on · beside a session (tab)`), not a refusal; the archive's empty state sheds its clause whole. On precedence, #34's "a note yields only to `? help` and the way out" is superseded by #36: under a note in the session view `enter` and `esc` go before the note's own keys and before the help. Held: the Lv2 cursor on a dead session; the 152 card's question wrapping; the 100 reader's question breaking inside its brackets on the call line; the 80 list's trace destination (it cannot fit beside `? help` and the way out at that width); the 120 help's fill order. |
| 38 | Operator review, round twenty-one (2026-09-03) | **The one report the round produced said good to go; the rest were lost to a rate limit and re-run as round twenty-two.** Three seams it found in the round-twenty batch: a step past a call-and-result block landed the page on the air after it, against the rule that no single step opens a page on a blank row — the step now lands on a line; the mirror's refusal grew back four cells and cost the narrow list its way in — it reads `mirror needs 110 columns`, keeping the verb; the key backfill restored the attach hint over a hide note's pane clause, so the row's lowest-ranked fragment (#31 ranks the parenthetical beneath a key or a note) stood where the hidden session's pane belonged — the pane clause outranks the hint. The reader's above-row keeps its turn count when it also names a result's owner; the count says how many of your prompts are above the fold, and the owner is what a bare `⎿` lacks. |
| 39 | Operator review, round twenty-two (2026-09-03) | **Seven operators; five said good to go, and the two that did not named the same seam from opposite ends.** The footer's shed order was one list for every level, so a level gave up its own keys while keeping keys it does not use: the reader offered `a ask` on a row that could not fit `[ ] turns`, and a list named the chapters of a trail it had not opened. Now the order is built per level — what every level shares goes first (the attach hint, the between-sessions keys, and `a ask` outside the archive, where it is the reason to be there); under a note in the session view `enter` and `esc` follow, since everyone knows them; and each level keeps its own keys longest, the chapter keys belonging to the trail and the reader, `space unfold` to the reader alone, and the way in outlasting the keys that act on a row. The backfill stops at the first key that will not fit: a key comes back only if every key ranked above it came back too. A trace's destination outlasts every optional key at every width — it fits at 80 once the keys it outranks are shed, which #37 had recorded as impossible. The help no longer names a key the deck would refuse — on a fleet of one, `g` and `x` go, and the rows they held go to `[ ]` and `a`, which the deck's own footers offer — and on a short body the keys the footers offer outrank the rest. A page never opens on a transcript blank, landing or stepping. A card's trace clips its bytes rather than dropping them for a cell. Held: the archive's narrow title naming the project; the 152 card's question wrapping; the reader landing on a wrapped sentence's tail; the 80 fleet row's red verdict; `→1` undefined at 80. |
| 40 | Operator review, round twenty-three (2026-09-03) | **Seven operators; one said good to go and six named one row each — every one of them a place where the deck says a thing it does not mean.** The footer's per-level order now runs on rows with no note as well as under one, so a level's own keys are what it keeps at every width; the way in and the way out are not shared keys but the level's own, ranked under what the level is for, and `a ask` stands with the session view's keys where `a` grounds a claude in that session. Every level asks what `enter` does rather than promising an attach: a paneless session said `enter · no pane` on its list and `enter attach` one keypress deeper. The help keeps the rows for the keys the deck's own footer is offering — an 80-column help named `g`, which no footer there offers, while `x`, on nine of them, was cut for the room — and its `g` row now says what `g` does, which is to take the oldest session that needs you and skip a dead one. The reader's page is computed at the width it renders: at 220 it was scrolled by a document measured for another column, so it opened on a blank row; a tail page steps over a blank forwards and keeps its last row. A circling row's age is the state's own everywhere: hoisting the loop's age where `· idle` would not fit put `circling 18h` on a card whose every other view said `circling · idle 3h`. A board column's trace keeps a stub of its bytes rather than none. The archive's way home is `A fleet`, which leaves room for the way in at 80. Held: the archive's narrow title; the 152 card's question wrapping; the 80 fleet row's red verdict; the board's `m` leaving no standing sign; `→1` and `G` undefined at 80. |
| 41 | Operator review, round twenty-four (2026-09-03) | **Seven operators; one said good to go and five of the six others named the same row.** The help was reading the keymap the footer *would* draw with room to spare, not the one it draws: at 80 it kept `g`, which that footer had already shed, while `a`, `space` and the search — named on nine footers between them — had no row. It reads the shed keymap now, and a key row the footer is not offering goes before any row it is, in the order of how guessable the key is without its row. `a` is named on every level's keymap, since `a` grounds a claude in the selected session at any depth. In the session view the way deeper outlasts the key that acts on a row, and `m` ranks the same whichever side its toggle is on — its label changes with the mirror's state, and a rank that changed with it flipped the row's order under one keypress. A circling row says whether a turn is in flight wherever eight cells of name survive it, so a card no longer wears the shape its neighbour uses for a loop's age. The reader's above-row cannot shed the clause that names a bare result's owner — that is what the row is for — and where a page sits at the true top it says so rather than drawing air. The archive's narrow title carries the day it added up. Held: the archive's title naming the project at 80; the unread chip differing between a board and a list; `k` then `j` not returning the page to where it was; the class names glossed only at the two widest. |
| 42 | Operator review, round twenty-five (2026-09-03) | **Seven operators; none said good to go, and six named the same screen — the help at eighty columns, where eighteen bound keys did not fit fourteen rows.** They fit now: below the board's width the rows pair, `tab/⇧tab` in one, `j / k` carrying `ctrl+d/u` and `G`, `x / A` carrying the archive, `? / q` the way out — every key the deck binds has a row, and the folded legend leads with the tag and unread, which every row wears, before the lanes, which belong to a lead that delegates. A key the deck offers anywhere it speaks — the fleet's last row, a hide note — counts as offered. A circling row says which way the loop is turning, `· idle` or `· working`, wherever the name's own floor survives it; the floor is the layout's, not a guess. The way out is the last key to go before the help: a reader row kept the key it had just refused and shed `esc back`, leaving the one width where the reader is the whole screen with no exit but `q`. The reader's above-row says where the page is even when the conversation fits whole. Held: `k` then `j` not returning the page; the 80 fleet row's bare `circling` (the name's floor leaves no room for the word); the reply panel over the strip; the archive's title at 100 naming two things in one list; the read-line cleared from the board by a reading. |
| 43 | Operator review, round twenty-six (2026-09-03) | **Seven operators; the alarm-storm operator said good to go.** The board seats what owes you first and the rest follow, where a whole band of them fits in the rows that are left: a 220x48 board named two sessions in its strip over twenty-eight blank rows, and the wider the terminal the more it hid. The strip still names what genuinely does not fit. A key the deck would refuse keeps its help row where there is room — it refuses in words, and those words ("nothing archived yet", "the only session stays", "mirror needs 110 columns") name a thing the help is the only place to look up — and it is the first row cut when there is not; `m` has a row below the board's width for the same reason. A row that says "no pane" offers neither write: `r` types into a pane as `enter` attaches to one. The way out is the last key to go before the help, except under a chapter note, whose own keys go after it (#24): the row refusing `]` must carry `[ ] chapters`. The narrow help teaches `ctrl+d/u` the distance §3 gives it. Held: the unread word on a row already counted under "owe you"; the open wait added to `on you`; the 120 card spending its width on `[session]`; the archive's two title schemes; the 80 legend's second gloss of the tag. |
| 44 | Operator review, round twenty-seven (2026-09-03) | The round-twenty-six carve-out that let a chapter note keep its own keys took the way out with it: one footer at eighty columns kept `j/k rows` and shed `esc back`, leaving `q quit` as the only named exit. The row's movement key gives way before both — the arrows move too, and the help says so. The open wait counts up to the point you were plainly away rather than being dropped whole past it: a session waiting four hours reported less than one waiting two, on the row whose whole job is to say which session your own turns are the bottleneck of. Held, with the reason: the lead's own verb is not on the status row because that row is the verdict (#17, round three) and HEAD, the column's last row, carries the present — the operator who asks for it twice is reading cards whose HEAD is cut, which is a case for the card, not for the rule; a row wearing `unread` while counted under "owe you"; the 120 card spending its width on `[session]`; the marks a folded legend cannot fit at eighty columns; the board seating a session with nothing owed ahead of one that owes, when the bands pack. |
| 45 | Dogfood: a wait is not a hang (2026-09-06) | **"It says stuck but I don't see that it's stuck."** The session was inside `Bash: sleep 420` — a deliberate wait for a background run — and the machine called ninety seconds of silence a hang, as it does for every call. But a shell command has a budget the harness gives it and enforces: two minutes unless the call names one, ten at most, and the command is killed when it runs out. So inside that budget the silence is the session doing what it said, and the row says so — "● fix  Bash: sleep 420  for 2m of 10m" — and only past it does `◍ stuck · silent 4m` mean anything. The budget is read off the call's own input (`timeout`, in milliseconds), never guessed from the command. Every other tool answers at once or not at all, so the threshold stands for them; a lead parked on its agents keeps the parked sentence, which is its wait. **And the page keys.** "At Lv2 on a `fix` I need to scroll a lot" — `ctrl+d`/`ctrl+u` were bound at every level and named nowhere but the help, so a person on a long trail pressed `j` sixty times. The footer now names them wherever it has the room, shed first when it has not, and `PgDn`/`PgUp` do the same thing: nobody should reach for a page key and get nothing. |
| 46 | The name at every level (2026-09-06, asked for; designed with the panel) | **"If we can add the session name in all the levels."** The frames showed why it was missing: the name's cell moved on every zoom — column 2 on the card at Lv2, column 58 on the reader's title at Lv3, the other way at 80 — and at 80 the reader's title for the two sessions called harness was byte-identical, so a `4` then a `2` replaced the whole screen under a row that did not change. The fix is one row, not a new one: the selected session's digit, name and — only when a live namesake shares the name — its `⌁` tag, after `⌂ compass` on the header, the reply panel's own form (#31) so the two read as one fact. No second header row (it would cost one of the trail's eighteen body rows at 80×24), no breadcrumb (#20: no level is drawn anywhere), no third copy of the tag, no state glyph beside chips that use the same shapes, and never ahead of the alarm: the chips never shed, the identity does. |
| 47 | A fleet of one keeps its past (2026-09-06, asked for; designed with the panel) | **"When I have only one session it enters the session directly and hides all the others — nicer if some of the past were still shown instead of pressing A to search."** #43 already said it: blank rows below the fleet belong to sessions, not to air, and a fleet of one is the loudest case — a 220×48 deck spending forty rows on nothing while a dozen finished sessions sat behind one key. The **recent band**: under the live rows, in the column the fleet already owns, the archived sessions that ended last, newest first, numbered on from the live fleet's last digit (a fleet of one draws up to eight), each row the digit, `○`, the name and its opening prompt, the verdict where there is room — `✗ red 18✓ 2✗`, `✓ shipped`, the board's own clause, which is what answers "do I want this one back" — and when it ended. On the narrow list it is a group under the live ones and takes the archive's `A browses` line as its header, so the key is said once; in the session view it is a rule where the trail ends, drawn into the rows the trail leaves and never over a leg. A digit opens its row in the archive, where the trail is already known how to draw, and `A` comes back to the live fleet where you were. Flat, newest first, no project groups, no cursor into it, no paging: the archive is still the archive, and this is its first rows. Never on the board (its blank rows are more columns', #43, and its strip is the door), never under a search (a search names what it found), never tailed, never amber, never counted. |
| 48 | "Not sure what circling means" (2026-09-06, dogfood) | The row said `↻ tinker  circling 52m` and the person reading it did not know the word. It is the loop of #27 — the newest run red and the same test failed three legs running — and "circling" was the trail's metaphor, not the reader's. The row now says **looping**, the word a developer has for an agent going round the same failure, with the same clause after it (`looping · working`) and the same age; the legend says `↻ looping`; the verdict row beside it already says which test and how many times (`✗ red 230✓ 4✗ · 3rd failure`). The hook's event name stays `circling`: it is a config-facing string, and a rename there breaks a hook for a word. |
| 49 | What the agents are doing (2026-09-06, asked for; designed with the panel) | **"Sometimes it would be nice to see how it spins subagents and what the subagents are doing."** The board said `◈3 out 20m · quiet 15m`, and the simulation showed what that sentence tells you about the agents: nothing — the lead's last write is the newest dispatch, so "quiet 15m" is the newest lane's age said again. Three agents burning twenty minutes and every clock on the screen counted the lead's silence. Claude Code writes each agent's transcript beside the lead's — `<session-id>/subagents/agent-<id>.jsonl`, with a `meta.json` naming the Agent call it answers — and compass now reads them: **paired by the call's id** from the meta file, never by the description (the sample's "Round 22: few ongoing" is exactly the string that repeats); the machine that judges a session judges the agent, so its call in flight and its silence use the same words and the same threshold (and the same shell budget, #45). What changed, each only where a file was read: a lane whose own file has gone quiet wears **`◍`** at every level — the whole answer at 80 columns, no cells spent; from Lv2 down an open lane carries its **head** beneath it where a returned lane carries its finding — `└ ● Bash: python dla.py …  wrote 40s ago`, `└ ◍ Bash: pytest -x tests/plugins  silent 12m`, `└ ⋯ nothing written yet`; the parked clause's second half is the agents' own — `◈3 out 20m · 1 silent 12m` (the hung one is the only actionable fact and displaces the rest whole) else `· newest 40s ago`; the header's chip says `◈3 out · 1 silent 12m` in place of "oldest", which was the wrong alarm. **The reader on a lane**: `Tab` with the Lv2 cursor on a lane opens the reader on the agent's own conversation in place of the lead's — aiming, not depth, so board → legs → reader is still three — titled by the lane as drawn and clocked by it (`◍ Red-team the plugin architecture · 15m out`), its above-row saying whose conversation it is (the person has no turns in it; the first turn is the lead's assignment), opening on its newest line; `Esc` or `Shift+Tab` brings the lead's back with the cursor still on the lane. What it is not (§5): never a fourth level (a depth-2 agent is a call line inside the agent's conversation), never a session (no column, no digit, no `g`, no `x`, no count in the tab title), never steered (`r` and `Enter` still go to the lead's pane — the agent has none), never an alarm (a silent lane is not amber and does not sort with needs-you: nobody can unblock an agent, and treating it as needs-you would teach the eye to discount `▲`), never analytics, and never inferred: a missing directory, an unpaired id, or an unreadable file leaves the lane exactly as it was. |
| 50 | OpenCode in the fleet, and which tool is which (2026-09-06, asked for) | **"If it works with opencode that would be awesome too. Then we need to show which one is claude (what model) and opencode (what model)."** OpenCode keeps everything in one SQLite file — `~/.local/share/opencode/opencode.db`: a `session` row per conversation (title, directory, the model it was opened with, a parent for the subagents its task tool spawns), a `message` row per turn with the role, provider, model and clocks in its JSON, a `part` row per block — text, a tool call with its state (pending → running → completed / error), step markers. compass reads it **as a transcript**: an `opencode://<id>` key that the tailer opens through a registered scheme, each turn reported once in the transcript's own event shapes (the prompt, the call with the classifier's field names, its result, the words), so the state machine, the segmenter, the reader and the board see an opencode session exactly as they see a claude one — same states, same legs, same rows. Read-only, incremental, one query when nothing changed; a store that will not open leaves the fleet as it was. The pane pairing knows both CLIs, and a claude session never lands in an opencode's pane. **The tool and the model**: every session knows the model that last answered (`message.model` on a Claude Code assistant line, `modelID` on an opencode message), and the tag row — the column's third row, the card's right — says `claude · opus-4-1` or `opencode · sonnet-4-5` beside the pane; the tool word is drawn only where it tells rows apart (another tool's row always, every row when the fleet runs two tools), and the model's vendor prefix and release date are dropped. The digest keeps its floor over the tool tag. `a` on an opencode session briefs the historian to run `opencode export <id>` instead of reading a file. Not done yet: opencode's subagents as lanes (they are child sessions with a parent id — the shape is there), and a dumb-glyph test for the ambiguous-width `½` the footer no longer draws. |
| 51 | Operator review, round twenty-eight (2026-09-06) | **Four operators on #45–#49; none said good to go, and each named one thing.** The budget of #45 was read off the *oldest* pending call, and Claude Code batches calls: a Read issued beside `sleep 420` put the session back on `◍ stuck` at ninety seconds — the budget is now the latest of every call in flight, capped at the harness's ten minutes, and carried on the row from the first second so "of 10m" does not blink in at ninety. `· board` sat ahead of the identity on the header, so the name slid eight cells on every Tab out of the board — the word now follows the identity, and the archive's header and a zero-match search keep the digit. A fleet of one coming back from the archive at 120 columns landed on a one-column board with no band and twenty blank rows — it lands on the session view it opened on. An agent that had written nothing for eighteen minutes wore the same `⋯ out` as one that wrote forty seconds ago — a lane with an empty file is judged from its dispatch, so it wears `◍` and says `nothing written · silent 18m`, the parked clause and the chip count it, the digest says `· 2 silent 18m`; a lane whose file was read drops the `→N` hedge; the lane's reader title clips its label and keeps its clock; `◍` beside a lane is in the legend. The band's verdict sheds its counts before it goes, so a red row keeps `✗ red` where a green one keeps its tick; the help's digit row names the band; the page key sheds after the attach aside and says `half page` — `½` is East-Asian ambiguous, two cells in a CJK terminal. Held, with reasons: a narrow HEAD keeps its label over the parked clause's second half (the person's own call, #17 dogfood); the archive numbers its rows as drawn, so a band digit lands under a different number (#32); the reply panel and the mirror's title name the pane a write goes to, a copy the header's tag does not replace; `j` on a fleet of one over the band says "the only session" — the band is not a cursor's; the stuck clock's origin flips from the call's age to the silence at the alarm (#17 r4 pins "silent"). |
| 52 | Operator review, round twenty-nine (2026-09-06) | **Five operators; the fleet-hygiene one said good to go.** The header's identity sits at the same cell in every one of the corpus's frames now, so #46 holds. What the rest named: the page key was still shed before the attach aside — the fold in #51 had been written but not applied; the budget's clock came from the oldest pending call while the budget came from another (`for 45m of 10m`), so the row now counts from the call the budget belongs to, a `run_in_background` call gets no budget (the harness enforces none), and the batched-call, cap and carry cases are tests. A fleet of one coming back from the archive at 120 columns still landed on the board — that fold too had been written in the wrong place; it is in the `A` key now. The digest under a parked lead said `↳ 3 agents out · 2 silent 1…`: the count restated the row above and the one new fact was cut mid-number, so the digest says `↳ 2 silent 18m` alone; the lead's reader stubs carry the lanes' own verdicts (`⎿ ⋯ still out · 15m · ◍ silent 12m`, `· wrote 40s ago`); the lane reader's footer no longer offers `r` and `a`, which are the lead's. **Two tools at eighty columns**: no frame at 80 or 100 named a tool or a model, because the list's third line made the pane tag and the tool tag alternatives — they are a ladder now, at every width: the tool, its model and the pane; the tool and the pane; the pane; the tool and its model; the tool — the longest form that leaves the digest twelve cells, the card's verdict twenty-four, the list's digest sixteen, the model going before the tool word and the pane, since the word tells two rows apart and the pane attaches; the header carries the tool and model where the fleet runs two, shed after the board word; a hidden but running opencode session keeps its tool in the archive. A session that fails the search in the session view says so on its card; the archive's `enter · no pane` sheds before `tab deeper`; the narrow help names PgDn/PgUp where it has the room. §4's "lifelong digit" was wrong on the one screen where the number changes — the archive's — and now says what #32 decided. Held: the archive's rows carry no verdict (their trails are not parsed — the band's eight are the only ones read, on purpose); the header restating the card at 220 (the invariant is the point); `j` on a fleet of one over the band ("the only session" — the band is not a cursor's). |
| 53 | Operator review, round thirty (2026-09-06) | **Four operators; none said good to go, and two of the four one-things were the same lesson.** Two folds had taken two rounds to land — the page key's rank and the budget's clock — and both were the two with no test: they are pinned now (the shed order, the footer at 152, and a stale Read beside a fresh sleep asserting `Since`). A fleet of one at 120 columns was still taught a board it refuses — the help's zoom rows, its `board:` legend, the archive footer's `⇧tab board` — because those words were keyed on the terminal's width, not on whether a board is drawn; they are keyed on the board now. `A` remembers the level it was pressed at and returns to it, so the legs keep their cursor and the reader its page; `r` is not offered beside `enter · no pane` in the archive either. An agent whose file was read and holds no turn had no reader at all: it is a designed empty state now (`◍ the agent has written nothing since it was sent · silent 18m`, then the assignment), and its stub under the lead's call says `◍ nothing written` once, not its silence twice. Two tools: a hidden running opencode session says its tool on the archive's row, and the reply panel — the one panel that types into another CLI — says which. The alarm storm's fine session is four minutes into a ten-minute run, so #45's row is on a frame at last; and a clip never cuts a number in half (`API Error: 4…`). Held with reasons: the 152 reader footer standing twenty cells short under the restore rule (the price of the page key at Lv2, recorded); the 152 help's `board:` gloss clipped by height; the archive's rows without verdicts; `j` over the band. |
| 54 | Operator review, round thirty-one (2026-09-06) | **Four operators; none said good to go, and three of the four one-things were folds that had landed on the wrong path.** `A` remembering its level (#53) was wired into the `A` key, and the band's digit enters the archive on its own path: it now remembers the level too, opens the archive as a list with no cursor, and the way back restores the legs with their cursor or the reader with its page at every width. The walkthrough tail meant to open the empty-file lane's reader landed on another session; it selects the lead first. The budget's row said `● build  the flag  for 4m of 10m` — the wait named and not the call: a budgeted HEAD is named by its call, as a hung one is, and the legend glosses `of 10m`. The reply panel's head, one cell over at eighty columns, clipped the pane it exists to name (`⌁ dev:2…` is a legal target that is not this one): it sheds the tool before the pane, §4's order, and the header two rows up still says the tool. Also: `⇧tab` on a fleet of one at eighty columns said "no board under 110 columns" — a screen this fleet never gets at any width — and says "the only session" now; the wide help's `board:` legend goes with the narrow one; a returned lane's file is read too, so Tab on it opens the agent's own conversation titled `✓ Score encoder gates … · 2h ago`, and the lead's reader says `⌀ came back with no report` in the trail's own words instead of a call-shaped `no output`; the empty-file reader keeps one clock (the title's) and gives the assignment the lane's glyph, not `❯`; the archive's hidden row reads tool first like every other tag row; a clip never leaves a bare `·` before its mark, and the reply panel's own clipper keeps numbers whole; `↑ 1 leg`. Held, with reasons: the archive's Lv2 footer at eighty shedding `tab deeper` with two cells to spare, and the 152 footer after `m` — both the restore rule's price, recorded in #53; the digest's `↳ 2 silent 18m` under a row that counts `◈3 out` — the count is the row above's and the silence is the digest's, one fact each; the page key shedding off the row that answers `ctrl+u` — a note takes the room a key gave. |
| 55 | Operator review, round thirty-two (2026-09-06) | **Four operators; none said good to go, and the alarm-storm one said why in one line: round thirty-one answered four operators and pinned none of it.** Eight of its folds could be reverted with the suite green. They are pinned now, one assertion each — the budgeted call on the column, the clip's number and separator, the overlay's cutter, the wide help of a fleet of one, the reply head at eighty columns, the archive's tool-first row, the empty lane's words, the plural, the way back restoring the reader's title, the tag yielding to a whole digest, the assignment's glyph. What the frames showed besides: the tag row took back the model on a column a hide had widened and clipped the trace a narrower column drew whole — a rung is taken only after the longest one that leaves the digest whole; `A` out of the archive restored the level, the page and the cursor and blanked the reader's title, because the conversation lands a poll after the cursor is placed — the reader anchors when its events land; the archive's footers below the list name `A`; the board legend's wrapped tail outlived its head; the lane that came back had no reader on any frame and the empty one's page stopped the gutter — the tail reaches both, and the empty page runs the panel's height; the lead's assignment in a lane's reader wore `❯` — it wears the lane's glyph, since nobody typed it; the mirror's transcript fallback carries the lanes' verdicts; a clip trims the separator and the space it stood on together; the attach aside comes back whenever it fits, whatever stayed shed above it — it is not a key, it finishes one. Held, with reasons: the 152 help shedding `↳ what came after` (clauses shed whole, and that one is last); the panel's peek leaving a bare word; the digest's `↳ 2 silent 18m` under a row that counts `◈3 out`, rejected a third time by its reviewer and kept a third time — the count is the row above's, the silence is the digest's. |
| 56 | Operator review, round thirty-three (2026-09-06) | **Four operators; none said good to go, and each named a fold of the round before that had landed a cell short.** The archive's trail title shed its own name with the day's clauses and fell back to the project ("TRAIL · api" over a row that read "fix the 401 on token refresh"): the day's clauses go, the name never. The returned lane's reader showed another lane's turns — the scene's fixture, not the pairing: every agent's conversation in the walkthrough is its own now, with the returned one's finding at its end. The budgeted card built its sentence at the card's full width and then fitted it beside the tag, so the second cut ate the clock — `● build Bash: go test ./... -coun… for…` — the sentence is built at the width it is drawn at, and the row's own shedder cuts the call and keeps `for 4m of 10m`. The tag ladder dropped the tool word at its third rung and took it back at the fifth, so on a two-tool fleet a wider column said which tool less often than a narrower one: where the word is earned, the bare pane is the last rung. Also: `A fleet` below the archive's list is the way home and goes with the way out, after the level's own keys; the archive's door stays under a search on a fleet of one; the lane reader's chapter note wears the lane's glyph, and an empty page offers only the way out; the overlay cutter is pinned at the length where both its guards fire, and the budget's gloss at all. Held, with reasons: the panel's peek leaving a bare word (blanking to the row's end would lose the `2✗ 3m` and `for 33m` remainders that read well); the 80-column fleet row shedding `build` to keep the parked sentence (#17 r6: the sentence is the last thing a row drops); a returned lane's finding wrapped under the fold at eighty columns (the fold cuts rows, and a wrapped finding is rows); the archive's rows past the ninth carrying no digit (#32: nine digits); first-session's help naming the band with nothing archived (the help teaches keys the deck binds, and `A` is bound). |
| 57 | Operator review, round thirty-four (2026-09-06) | **Four operators; three reported, none said good to go, and the one-things were the band, the fold and a refusal.** The band at a hundred columns bought its verdict's counts out of the prompt, row by row, so five of eight rows showed less prompt than at eighty and two kept `212✓` while two beside them dropped it: the band decides its verdict form once — the counts go for every row when any row would have to buy them below the prompt's floor. The trail's fold at eighty took a returned lane's own row and left its finding under `◆ scout`: a finding's parent is the lane that brought it, and the fold draws that lane over the finding, not the leg. `no session 2` answered a digit whose session was hidden seven keys earlier, beside a strip that said `1 hidden · A, then x`: the refusal names the session and the way to it. Also: the band's digit no longer writes a note the footer's `A fleet` already says (it cost `tab deeper` at eighty); the help below the board's width cuts the refused `m` row before `g`, a key that works there; a wrapped turn's chapter note marks its cut and strips the lane's glyph; the board's legend gloss fits a legend column whole. Held, with reasons: `↑ 1 leg` counting neither the prompt nor the lane above the fold (it counts legs, as its word says); the 120 help shedding the `↪` gloss (clauses shed whole, last first); the digest under a parked lead keeping `2 silent 18m` over `1 new leg` (its reviewer's fourth rejection, the same answer: the silence is the actionable fact); the 152 help's legend column at its own width. |
| 58 | Round thirty-four, the alarm storm's half (2026-09-06) | **Three of #56's four folds reverted with the suite green, and #56 recorded them as done — "the pin is not a chore after the fold; it is the fold."** They are pinned: the card's sentence keeps `for 4m of 10m` across the widths that cut it, the ladder at fourteen cells answers the tool word and ends on the bare pane, the archive's title at thirty-six cells keeps `fix the 401`; and round thirty-four's own folds with them — one verdict form for the band, a finding's lane at the fold, the hidden session named on a digit, the refused `m` row cut before `g`. A lane row at the top of a pinned viewport stays: drawing its leg over it orphaned the finding beneath. A clip after a token that ends in a dot (`go test ./...…`) reads as more dots — the token goes whole. The legend's wrapped rows never end on the separator, and the board's gloss fits its column with two cells over. Held: the 80-column fleet row's `Bash…` (the trail beside it says the call); `↑ 1 leg` on the call site (pinned on the helper). |
| 59 | Operator review, round thirty-five (2026-09-06) | **Four operators; none said good to go, and one caught a regression that landed during the round.** A clip fix for "40…" (a whole number before the mark reading as a cut one) dropped the half of #53's guard that asks whether the cut is *inside* the number, and walked `403` off three `⊘` rows at the widths people run: the guard is back as #53 wrote it — a number backs out only when the cut falls inside it — and "take 40…" is held as the price of `403`. The dotted-token rule (#58) fired on the rune before the cut and missed at the widths that produce `./…` and `./...…`: it runs after the trim now, on the token the row ends on, until the row ends on neither a dot nor a slash, pinned at those widths; `isLaneRow` was dead code and the fold's rule is pinned instead. The archive named a session by its project on the header and the reader's title while the trail beside them named it by its row: all three say the row now, and a hidden live session keeps its name before its prompt. The lane reader's chapter note quoted the whole assignment and bought it with the level's keys: it is a chapter note like the lead's — the quote goes before the keys. The column's tag took a rung by the digest's floor where a longer digest would have kept its clock: it takes the longest rung that costs the digest no clause, else the last. The help's digit row names the band only when there is an archive. Held: a whole number before the mark ("take 40…"); the 120 help's `↪` gloss (its reviewer's merge of `?`/`q` is the narrow help's form, and the wide help keeps them apart on purpose); the short-height two-column help clipping inside an NBSP-bound phrase (off every frame; a last resort); the board at 120 naming a session in its strip where a band would fit (#47: never on the board). |
| 60 | Operator review, round thirty-six (2026-09-06) | **Four operators; three reported so far, none good to go, and two of the three one-things were the round before's folds landing a cell short.** The column's tag rule "else the last rung" threw the tool word away on both opening boards for a digest the longer rung drew the same: the rule is the longest rung that costs the digest no clause, else the longest that draws the digest the shortest would, else the last — and the narrow list's third line uses the same rule instead of its old sixteen-cell floor. The lane reader's chapter note was a chapter note for the shed order but not for the note's own minimal form, so its quote never clipped and the way out was shed at eighty: the quote goes first, as the lead's does. The reply panel in the session view was sized for a fleet list that was not there and stood five cells into the trail at 120: it stands inside the companion, three cells off the rail. An empty lane said its silence twice, on its row and under it: once. Held: the finding's second row under a pinned lane (the fold cuts rows); the 120 board's strip over blank rows (#47: never on the board; the band is the archive's, a live session's place is a column or the strip). |
| 61 | Round thirty-six, the alarm storm's half (2026-09-06) | The dotted-token rule (#58, #59) shed the whole token back to the space before it, so a column eleven cells wider said strictly less and lost the name of the hung run (`Bash: python…` at 134 columns, `Bash: python backfill…` at 133). The rule strips the trailing run of dots and slashes instead — `backfill.` is `backfill…`, `github.com/example/` is `github.com/example…`, `go test ./...` is `go test…` — never the token. Two pins sat one width from their case and are at it now: the trailing dot at eight and nine, and the lane at the fold asserted on the substituted row when the viewport opens on a finding's second row. Recorded: the held "whole number before the mark" and the fixed "cut inside a number" are adjacent widths of one string (`took 4…` beside `took 4.03s…`); the held one stays held, since backing a whole number out is what walked `403` off the storm's rows. |
| 62 | Round thirty-seven (2026-09-06) | **The alarm-storm operator said good to go** (with fleet hygiene, #29). What held the rest, folded: the header's ladder skipped the rung between the full tag and none — the model goes before the tool word (#52), so the archive header at 80 keeps `1 api · opencode · ⌁ dev:2.0` with a cell to spare; the frame `m` is pressed on names `m` — its note is short (`the live pane`) and the key is not optional there, whichever side the toggle is on and whichever level it was pressed at, so the way back is a key in the row rather than a clause the width can shed; the live card's clock at Lv3 stopped a cell short of the column — the marker's cell is the clock's when the marker is gone; the overlay's cut mark stands at the cut, not where the row's text ran out; the refusal one press after a hide names the pane the hide note named; below the board's width the live reader names the archive door in its footer (`A archive`), ranked with the way out as `A fleet` is (#56), since no band or fleet row is on screen to name it; a tick row reserved four cells for a `?` no tick row can carry (a test leg is never a tick), and the label takes its whole field; the scene's hung call is narrated by its own command; the `↪` gloss rides the fleet's tag row (clauses shed whole, last first), since `↪` is a fleet row's mark and the 120x34 help drew it on its board while cutting the trail row that defined it. Held: the four-cell room the card's sentence keeps (r36); `g` for `plural` (#58); the whole-number adjacency (#61); the band's cap of eight (#47). Recorded, no change: `clip` on a prefix of separators alone, and a mark after a hyphen — off every frame. |
| 63 | Round thirty-eight (2026-09-06) | **Three operators still out; what each named, folded.** The round-37 clock fold had widened the live card's Lv3 row by a cell instead of taking the marker's: the cell of air belongs to the marker, and with no marker there is none, so the deck's rail is straight again and no row reaches the terminal's last cell (pinned on the row's width and on the frame). The board's last-band rule (#62's `avail = rem + 2`) is pinned on the 120 subagents board. HEAD's label outranks its sentence's second half whenever the label is being cut, not only when six cells were left of it (#51's order reaching the frame): the second half is on the card and the digest already, the name nowhere else. A spaced dash is a separator in `clip`'s cutset (the hyphen inside a token stays). The refusal after a hide keeps the pane the hide note kept whenever the keys leave room, hint or no hint. The reader's `[reader]` stands on the reader in the archive as in the live view, wherever the trail's title gave the word up; and at Lv1 the word stands with the mark — `[fleet]` on the fleet's title, none on the trail — since #20's reason is "where the keys are". The empty lane's reader offers no `h/l session` wherever the key stands. The `↪` gloss is a fleet row of its own in the help, and comes back before the compaction's row when rows are short. The overlay paints nothing past its panel. Held with a reason: the empty lane's reader saying the assignment under a title that clips it — the body row is where the assignment is whole, and a document that is only its title is nothing to read. |
| 64 | Round thirty-nine, the second-day operator's half (2026-09-06) | The archive reader's title said its own name twice: the anchored row's clause is the ship leg's label cut before its bracket, and what survived the cut was the name already on the row — the clause goes when its cut form is a prefix of the name (#23 protects the name; §4 says a line answers a question once). A panel titled TRAIL wears `[legs]` at every width, the help's word for the level; `[session]` is the card's word alone, where the row is the session (#20). The overlay draws no right-hand mark when its peek rule blanked the whole peek — the left mark already says the row was cut. The subagents half: the fold that paints a lane over its finding's first line marks the block's first surviving row at its head (`└ …two are the same root cause`), since the count was the finding; the 80 help glosses `✓ back` on the trail's own row when the fold row shed it for the marks #17 pins; a comma or a semicolon joins the clip's cutset; and the strip's `A, then x` stands down on the one frame whose note teaches the same key, coming back when the note goes. Held: the anchored row painted full width at 80/100 (the cursor's own inversion, drawn where the reader has the width); the two numberings (#32). |
| 65 | Round forty, the second-day operator's half (2026-09-06) | **The two-tools operator said good to go in round thirty-nine** (with fleet hygiene and the alarm storm). A failed result never leads with a line that reads as a pass: `go test` opens with the package that passed, and the archive reader's one row about yesterday's red suite read `✗ ok … 4.21s · 1 more line` with `12 passed · 1 failed` behind the fold — the verdict leads and the `ok` line goes behind the fold with the rest, the shape every pytest scene already drew (#19's own example). And #64's reader-title pin did not bite: the model it built never reached the branch (no events polled for the api session, so no anchored row) — it is now the walkthrough's own path, polled after every key, and fails on the revert. The subagents half: the plan strip counts an item in progress as neither to go nor done (`1 to go · 1 doing · 1 done`; `2 done` had counted a measurement whose lane was twenty minutes out); the lead's card says how many lanes are back beside the ones out (`◈3 out 20m · 2 silent 18m · 1 back`), where the back clause was drawn only when nothing was out; and the digest substitutes the silent clause for `none back` only where the row above does not already say the silence — since #49 it does, so the digest keeps the answer the row above has no room for. Recorded, not folded: the card's sentence collapsing the class column the trail pads (prose, not a table); the live reader's title repeating the anchored row two lines under it (shedding the clause while the row is on screen touches every reader frame; a round of its own if the panel wants it). |
| 66 | Round forty-one (2026-09-06) | Two operators still out. The second-day half: below the board's width the reader owns the screen, and a fresh session's reader said `⎿ ⋯ no reply yet` over twenty blank rows — an abandoned session's screen too — so its tail carries the present the trail draws (`● scout  thinking…  for 40s`, §4's own row) when no trail panel and no fleet row is on the frame to say it; the reader's title also sheds a clause that is the name and a bracket (`fix the 401 on token refresh (commit)`), since the leg's bracket is not worth a second copy of the name on the row (#64 had held the whole form as earning its cells; at the two widths it fit, 27 of its 36 cells were the name again). The subagents half: the digest calls a lane that came back with nothing `back, empty` (the help's own glossary: `back` alone means a finding beneath), and counts the lanes dispatched after the look as `sent since` rather than `agents out`, since the card's `1 back` a row above counts every lane and one word for two scopes on touching rows read as a contradiction; at 120 the card borrows the header's form of the out clause (`◈3 out · 2 silent 18m · 1 back`, the dispatch age the lane rows carry anyway) where the lane-back clause was a single cell short; and the agent's own reader says the silence on the call it is hung on (`⎿ ⋯ no result yet · silent 12m`). Recorded, not folded: an archived session's two unlabelled ages a row apart (the title's start age beside the row's last-event age — every bounded fix touches 13 to 30 files; a round of its own). Checked and cleared: a green `go test` fold keeps its `ok` line (skipping it says less); the reader wraps a long call, as §4 permits there. |
| 67 | Round forty-two, the second-day operator's half (2026-09-06) | At eighty columns the recent band answered "when" on all eight rows and "how it went" on none: `✗ red` left seventeen cells where the prompt's floor asks eighteen, so #51's ladder stopped a rung early. The ladder has a last rung — the verdict's own mark (`✗ 2h`, `✓ 6h`), glyph-guarded to `✓✗⚑` so a verdict that opens with a word (`build 10h`) keeps no bare word. One file of forty-five changes, 240 rows gain their mark. The subagents half: a lane's `→N` link survives its own file in hand where the session it matches wrote more recently — the board called an agent silent twelve minutes while compass's own match had it working thirty seconds ago two columns right, and `→N` was glossed on thirty-six frames and drawn on none (#49's "not a guess" stands; "better" did not); lanes back are counted by their return and lanes out by their dispatch, so a lane sent before the look that came back after it is the digest's news; and the digest says `back since` where the longer word costs it no clause (#60's rung), keeping `back, empty` at 80 and 100 where it would clip. Held: the 80 legend's `⌀ empty` for the digest's `back, empty` — a prefix of the meaning, on a row at its floor. |
| 68 | Round forty-three (2026-09-06) | **The second-day operator said good to go** (with fleet hygiene, the alarm storm and two tools). The subagents half: #67 drew `→N` at eighty columns where no help glossed it — the read-line's row takes `· →3 its session` wherever the tag's own gloss already stands and the cells are there (one row per 80-column scene, nothing above); and a linked lane's sub-row says the fresher reading's clock beside its own (`silent 12m · →1 wrote 30s ago`) where the whole row still fits, since the link exists because the session is fresher and the sub-row was the one place with room to say so. Checked and cleared, recorded so it is not re-raised: the card and the header still count a linked lane as silent — a hedge may add a route, never drop a lane from an alarm (#49). Held: the digest's `none back` under a card's `1 back` (the clause binds its own scope across the comma; every longer form costs the `looked` clause). |
| 69 | Round forty-four, subagents (2026-09-06) | The key `→N` tells you to press emptied the reader: a session change at Lv3 kept the lane that belonged to the session that left, and the new session's reader opened on an agent it does not have (`reading the transcript…` under `the agent's own conversation`, for good) — the lane is cleared with the events when the selection moves, so a digit or `h/l` from a lane reader lands on the session's own reader. Below the deck's width the silent agent's own page says the fresher reading beside its silence (`⎿ ⋯ no result yet · silent 12m · →1 wrote 30s ago`), since at 80 and 100 no trail panel carries the link (#66's rule, #68's clause). Held with a reason: the link is one-directional — a hedge routes to a session and never names one, so session 1's own rows do not say a lead is counting it (#49: a prefix match on a first prompt may add a route, not a fact). |
| 70 | Round forty-five, subagents (2026-09-06) | A digit inside the trail finishes its arrival on the present, as `tab` and `h/l` do: `pointAs` now ends with the cursor move `sessionMove` ends with, so `→N`'s key lands with a row under the cursor and the companion reader with its clause, where it landed with neither and the first `j` was spent placing the cursor. Held with a reason: below the board's width the lead's fleet row never says a lane came back — the row is at its floor (33 cells at 100, 27 at 80; every form carrying `1 back` is a cell or more over), and the class word and the dispatch age it keeps are the lead's own (pinned since round one); the lane back is on the trail's lane row one `tab` away and in the digest when it came back since the look. Recorded: at 80 the lead's row also sheds its class word (deliberate, fleetlist.go's own rule), competing for the same two cells. |
| 71 | Round forty-six, subagents (2026-09-06) | #69's rule was a width proxy for a fact: at 120 the trail panel is drawn and its sub-row sheds the fresher clock for want of cells, so `→1` stood on the frame with no row stating why. The lane reader's stub now carries `· →1 wrote 30s ago` wherever the trail's sub-row does not — the stub asks the sub-row's own fit arithmetic (#68) against the width the layout gives it — and the pin asserts the fact (exactly one of the two rows says it) at every width rather than the proxy. Held with a reason: the read-line after a reply takes the digest's clauses whole, in rank, and stops at the first that does not fit — skipping the lanes clause to draw `1 new leg` behind it would make the lesser clause read as the whole news (recorded: 23 blank cells at 220 after `↪ sent "please continue" · 0s ago`); the 80 help's folded row files three fleet marks under `trail:` — deliberate, at its floor (#17). Recorded: `journey.Task.Owner` is parsed and drawn nowhere; no frame is wrong by it. |
| 72 | Round forty-seven, subagents (2026-09-06) | The board pointed at a lane with `→1` and never said that lane's own silence: below Lv2 the lanes' heads were withheld by #49's "no cells spent at eighty", a width argument standing in for a room test, while the column ended in fifteen blank rows at 220. The board now spends its spare rows on the lanes' heads: the trail builds the column with them and falls back to the glyph alone only where they would cost it a row, and the board packs twice — with the heads and without — keeping them only where the same columns are drawn either way, so no session loses its column to them and the 80 trail keeps its first prompt. A clip never ends on a flag's leading dash (`--model moe_by_andy -…`), the hyphen inside a token staying (#63). Recorded, not folded: the teammate session's shared list is on no frame — its one task is in progress and the plan strip draws only what is pending; the live row above says what it is doing. |
| 73 | Round forty-eight, subagents (2026-09-06) | The reader filed a returned agent's finding under another call's result: the guard that draws a late result under `↩ result of` asked only whether another call had been made since, and the Compare agent's report — answered after another agent's report and a file read — stood as the second output line of `Read(SKILL.md)` while its own call wore no stub. A result is adjacent to its call only while nothing has been drawn between them: any result landing clears the adjacency, and a call whose answer comes after another call's answer wears `⎿ ↩ result below`. No shipped frame changes; the walkthrough never opened that reader (a third of the scene was uncovered), and the pin walks the route. The overlay's peek blanks a lone word with no digit (`ago`, `still`, `report`) — #55's held reason defends the `2✗ 3m` and `for 33m` remainders that read, and those still peek. |
| 74 | Round forty-nine, subagents (2026-09-06) | An agent's own page denied the write its head reported: the empty page chose its sentence from the hung bit alone, so a lane that wrote forty seconds ago and whose file holds no turn to draw said `the agent has written nothing yet` under a trail row saying `wrote 40s ago`. The page splits as the head does — a written file says `nothing to read yet` with the head's own clock (`· wrote 40s ago`, or `· silent 12m` past the threshold), and only a file with nothing in it says `written nothing`. No shipped frame changes; the pin walks the route. #73's peek pin could not fail — its cut landed inside a token, which the older mid-token rule blanks on any build — so it now lands in whitespace and fails on the revert. |
| 75 | Round fifty, subagents (2026-09-06) | Below the deck's width the lane's own page never said the call it is inside: the row `tab` was pressed on said `● Bash: python dla.py…  wrote 40s ago`, and the page it opened said `⋯ nothing to read yet · wrote 40s ago` over fifteen blank rows and a footer claiming all of it was on screen, because #66's tail was gated on the session reader alone. Where the page owns the screen (no fleet and no middle panel) and the lane's file has been written, the page draws the lane head's glyph and call beneath the empty row — the clock stays above, said once (#60) — and where a trail panel is drawn the row stays the trail's. A lane that has written nothing adds no row. No shipped frame changes; the pin walks the route at every width. Checked and cleared: the live lane's stub wears no state glyph (the clock answers, `◍` is the quiet mark by design); two `harness` sessions on one board are told apart by digit, glyph, pane and digest; a bare `…` on a peek that covered only a rail is #62/#64's mark at the cut. |
| 76 | Round fifty-one, subagents (2026-09-06) | At eighty the agent's page shed the way out for a note: a lane's page with no turns offers only the attach key and `esc back`, so the attach key led the row, its separator-led fragment matched nothing in the shed order, and `esc back` — ranked last of all (#24) — was the only key left to shed under `no turns of yours in this conversation`, leaving `q quit` the only named exit. The attach key's leading forms join the shed order at the attach key's own rank, as the movement keys' leading forms already did, so a row it leads sheds it before the way out. No shipped frame changes; the pin walks the route at every width. Checked and cleared: under a standing search the lane's `→1` goes and the digit refuses honestly; session 3 reads whole at 80 on the route the walkthrough does not walk; the lead's stubs answer all three questions at once. |
| 77 | Round fifty-two (2026-09-06) | **The subagents operator said good to go — every operator on the panel has.** Fleet hygiene (#29), the alarm storm (#62), two tools (#65), the second day (#68) and the subagents (this round). Recorded, not folded: the lane page's assignment row repeats the title's name at the four widths where the title carries it whole; at 120 the title clips and under a search the title's right side is the query, so the body row is the only naming there, and the page cannot yet know whether its title clipped — a redundant row is not a false one. Checked and cleared: the three questions the scene asks (which are out, which came back with what, which have gone silent) are answered at every width; every counted claim checks out against the fixture; hiding the linked session drops the link honestly; leaving a lane page sideways lands on a session; the keys a lane page does not offer refuse in words or are invisible to the harness. |
| 78 | First live look after the panel (2026-09-07) | The owner's own fleet, thirteen live and 285 archived. Folded: the harness's `<tool_use_error>` wrapper is taken off a result line before it becomes a leg's detail or a reader row (`Untag`); a Bash command that begins by entering the session's own directory is shown from what it does (`cd <cwd>; git diff …` → `git diff …`), on the activity string and in the reader, a bare `cd` staying; `g grab` is offered only while a live session is waiting on you and not dead on the API — the help still teaches it; OpenCode's default title (`New session - <timestamp>`) is no title, so the row is headlined by its first prompt; and the recent band ranks a session with legs before one without, newest first within each, since four `Test message` sessions had taken the slots from yesterday's work. Recorded: the header carries the selected session's digit, name, tool and model at every level (#46), the digit only where the board has numbered the session. `docs/PROCESS.md` now says how the loop is run. |
| 79 | The owner's second look (2026-09-07) | **The band says which tool ran a session** where the fleet — live and archived alike — runs two, and always on an OpenCode row: `billing · opencode · "reconcile the…"`, the row rule (#50) on the band. The word is the row's own, like the pane tag: drawn where the row keeps its prompt's first eight cells beside it and shed where it would not, so a long name on one row does not strip the others (a band-wide form, #57's rule for the verdict, took the word off every row for one long name). **A session wears the name its person gave it**: Claude Code writes `/rename` as a `custom-title` line (and an `agent-name` twin) in the transcript; the parser carries it, the scan takes it from the head or the tail, the live tail takes it as it happens, and the deck names the session by it over the directory's — in the fleet row, the header, the band, and the archive, where a renamed session keeps its name before its prompt (`checkout-flake-hunt · "the checkout suite…"`). The scenes carry both: two of yesterday's second-day sessions ran under OpenCode and one was renamed. **Space names the call it unfolded**: the reader has no cursor, so Space takes the first folded result on screen, and the note says which (`unfolded Bash(go test ./...)`) — a person pressing it did not know which row it had acted on. The session view's band takes the band's own forms, as the list's does. |
| 80 | Round fifty-three, the two-tools operator's half (2026-09-07) | At a hundred columns the band's only tool word was `claude`, on the one row that did not need it, while both OpenCode rows stayed silent: a row of the default tool now sheds its word before a row of the other tool, and where an OpenCode row cannot keep `opencode`, no row draws a word. An archived row says its tool where the archive holds two (`opencode · spike/billing`), and so does its header: `toolTag` had withheld the word for anything not live, a reason #79 refuted. #78's default-title fold is pinned where it reaches the fleet, not only on the helper. The second-day half: an opening quote with nothing after it joins the clip's cutset while it is unclosed (`checkout-flake-hunt · "…` spent four cells on no prompt, on the band and the trail title alike; a closed quote is never stripped); Space's note names the call without the reader's glyph. Recorded: the archive's group heading as the place for the tool word (said once per group) — the row's own tag line says it instead, as every other row's does. |
| 81 | Round fifty-three, the fleet-hygiene operator's half (2026-09-07) | The band at a hundred columns stopped two rows short of the column it owns: it folds the archive's line into its own header, but was sized against a body that still reserved that line's two rows, so two ranked sessions fell off for air (#47 forbids exactly that). The band takes those rows. Three pins that could not fail were re-aimed: the fleet-of-claudes rule now reads a frame that draws the band; `g grab`'s API-error half has a fleet whose only question is dead; the rename's scan halves (head and tail) are read by `Discover` alone, with no live tail to satisfy them. Recorded: the alarm storm is where a fleet whose only `▲` is a `⊘` would be seen on a frame. |
| 82 | Round fifty-four, the two-tools operator's half (2026-09-07) | The one live session stood wordless over a band that named two tools: a live row was judged by the live fleet's tools and the band and archive by everything compass holds. One rule now — a row, live or archived, wears the word where everything held runs two (`1 hello · claude` in the header, `claude · ⌁ main` on the card); a fleet of one claude with no archive still needs no word. Held with a reason: the clip mark glued to a complete name where the cutset took the whole prompt clause (`checkout-flake-hunt…`) — #54's pin says a shed clause still marks that something followed (`Please run /login…`), and a name with no mark would read as a session with no prompt; the six blank cells are the price. |
| 83 | Round fifty-four, the fleet-hygiene operator's half (2026-09-07) | **The second-day operator said good to go.** A list's footer offered `ctrl+d/u half page` on every frame that carried it — 27 of 27 inert: at Lv1 the keys page the trail beside the list (§3), and the archive's trail is five rows in thirty-five, so the key did nothing while `j/k move` beside it moved the list and the row read as though the page key paged what `j` walks. At Lv1 the fragment is shed where the trail fits, the same shed the lane page makes (#56). The compact help names what the page keys page (`ctrl+d/u pages the trail`), as the wide help's own row does. |
| 84 | Round fifty-four, the second-day operator's half (2026-09-07) | #82 left an empty row between the card and the trail on the frame the scene opens on: the card's third row was gated on the fleet column's third line, which the tool word had just made non-empty, and drew the delta, which was empty. The row is the delta's, so the delta decides it. Recorded: the band's digits and the archive's numbering differ by design (#32, §4); a stale comment beside `toolTag`. |
| 85 | Round fifty-five, the two-tools operator's half (2026-09-07) | At 120 the two same-directory `api` rows made opposite trades: one drew `opencode · ⌁ dev:2.0`, the other shed `⌁ dev:1.0` — the only place the frame could say where it lives — to keep `looked 25m ago`, which its own column's divider draws again five rows down. The tag beside a digest now takes a higher rung carrying the word and the pane where it costs the digest only its trailing look clause. The wide help glosses the tool word (`claude · opencode — which CLI runs the session, where the fleet runs two`), as it glosses the pane, the states and the trace. |
| 86 | Round fifty-five, the fleet-hygiene operator's half (2026-09-07) | Walking the archive, one `j` took the cursor off the window and drew it nowhere: the window's end was trimmed after the settle loop, so the selected entry fell off the bottom and no row wore `▸`. The window settles until the selection is inside it. At a hundred columns a hide left no trace on the screen: the band folds the archive's line into its header, and that line carried the hidden count — the header carries it now (`recent · 41 archived · 1 hidden · A`). The archive's board strip headlines its sessions by what was asked, as its cards do: four project names cannot tell forty sessions apart. |
| 87 | Round fifty-five, the second-day operator's half (2026-09-07) | At 152 the help clipped a definition inside a parenthesis it never closed (`(3h+…`), and the clause behind it — the budgeted row's own definition (#45) — was gone from the frame: when the legend has no rows left, an aside the clip would leave open goes with its clause, at that call site only (a general cutset ate a call's own bracket). The compact help names the reader as the page keys' object once the keys are in it: #83's `pages the trail` was true at Lv1 and false at Lv3, and a fleet of one never draws the wide help's row that names both. |
| 88 | Round fifty-six, the fleet-hygiene operator's half (2026-09-07) | #86's strip pin was vacuous: it pressed only `A`, landing at Lv1 where no strip is drawn, so it passed against the reverted code. It presses `esc` to the archive's board now and fails on the revert. Held with a reason: the lone board card at 120 drawn 64 cells wide under a band ruled at 39/79 (#30, #31, and a golden) — refutable only with a full band above it. |
| 89 | Round fifty-six, the two-tools operator's half (2026-09-07) | The help glosses the tool word where a row wears it and nowhere else: a one-tool fleet's help defined `opencode` on fifteen files where no row said it, and at a hundred columns the two-tool help defined `⟲`, a mark on no frame of the scene, while the word seventy-four rows wore was defined nowhere — the gloss is gated on the fleet running two CLIs (the row's own test) and comes back before the compaction's row. A trace's clock goes whole or not at all: `↳ answered 1 · 0s a…` cut a time inside its word where the bare `0s` fits and matches every other clock in the column. Held with a reason: a ship row's label cut one letter inside an unclosed `(commit)` at 80 — a general open-parenthesis cutset in `clip` eats a call's own bracket in the reader (#87 kept the rule at the legend's call site). |
| 90 | Round fifty-six, the second-day operator's half (2026-09-07) | The board dropped the selected session and marked nothing: `boardKeys`'s guard never fired (called with the whole order) and `boardPack` trimmed afterwards, so `esc` out of the archive at 120 drew six columns, a header saying `7`, and no `▸` — against #16's "the selected session always among them". The selection takes the last drawn slot when the pack trimmed it. A ship leg's label never ends inside a bracket it opened (`(c…`): #87's shed at this call site too, the reader untouched (#89's hold was about `clip` itself). A fleet of one: the card's bare tool word yields to the trace's clock, since the header names the tool on every frame and the send's age is on no other row; the pane still stays (#85). |
| 91 | Round fifty-seven, the two-tools operator's half (2026-09-07) | #90's leg-bracket pin pressed the wrong session and drew no ship row; it walks the archive's own route now and fails on the revert. In the archive a hidden live session's row draws its model where the frame's header has shed it (#62) — at 80 `sonnet-4-5` was on no frame and behind no key — and keeps its pane where the header says the model. Recorded: the card half of #90's yield is inert on every frame (the list row's half carries the decision and is pinned). |
| 92 | Round fifty-seven, the fleet-hygiene operator's half (2026-09-07) | Two rows the fleet had and did not spend. **The help's legend is budgeted without the tool gloss.** A one-CLI fleet's legend was wrapped against a raw text that still held the gloss and its wrapped tail, and the gloss was dropped after: at 152 the legend shed `(3h+ = away) · for 4m of 10m — inside its shell command's budget` off the away-threshold row while the last two rows of its own column were blank, and #87's "no rows left" was false on that frame. `helpLegendFor(tools)` builds the text the wrap budgets — the gloss is in it only where a row wears the word (#89) — and `dropToolGloss` stays as the belt. Three rows in seven files, no new clip. **The band's gate measures the rows the band is budgeted with.** At eighty the gate asked for two free rows of a body that had already given the archive's line its two, while the band was budgeted with those rows back (#81): after a hide the column ended on `41 archived · 1 hidden · A` over two blank rows with forty-one sessions behind `A`. The gate is `free+len(tail) >= 2` now, and the band's header sheds `recent · ` before its counts where the column cannot hold it — the shed the archive's line makes for itself, so at eighty the header is that line unchanged and the rows are only added; and the eighty-column Lv0 header that read `recent · 41 archived · 1 hidden…`, its key cut, is whole. Pinned by the fleet-hygiene help at 152 and the eighty-column band after `x`; both fail on the revert. |
| 93 | Round fifty-seven, the second-day operator's half (2026-09-07) | **A fleet of one never spends a row on the bare tool word.** At 100 and 80 the frame the scene opens on drew `claude` alone on the live entry's third row — the only tool word on a frame whose two OpenCode band rows drew none, #80's shape on the live entry — because #90's gate shed the word only where it cost the trace its clock, and fifty seconds in there is no trace to cost. The gate sheds the word too when the trace is empty: the entry is two rows, the band moves up one. Two files, two frames each, one row each. Pinned at both widths; fails on the revert. (The pin the operator found vacuous is #91's, already re-aimed.) |
| 94 | Round fifty-eight, the fleet-hygiene operator's half (2026-09-07) | #92's band gate was unpinned: the one-`x` pin walks a route the header shed alone covers, and with the gate reverted it passed while the walkthrough lost its two rows. The pin that bites walks the hide's own route — `r`, `1`, `j`, `x` — where the reply's `↪ sent` row takes the free rows to one; it fails with the gate reverted and passes at HEAD. No product change. |
| 95 | Round fifty-eight, the second-day operator's half (2026-09-07) | #92's rule, for the board's line. A fleet of one has no board, and `dropBoardLegend` took the `board:` line out after the wrap had budgeted it: at 152 the help shed `⚠ two sessions, one thing` off the read-line row over a blank row of its own column while 100 and 120 drew it whole. `helpLegendFor(tools, board)` builds the text the wrap budgets without the gloss and without the board's line where the fleet wears neither; the two drops stay as the belt. Two files, one row each. Pinned by the second-day help at 152; fails on the revert. |
| 96 | Round fifty-eight, the two-tools operator's half (2026-09-07) | **A fleet of one never wears the bare tool word the header says.** #93's sentence was false on twelve frames: `↪ sent "go on" · 0s ago      claude` under a header that said `1 hello · claude`, over two OpenCode band rows that drew no word, because the gate shed the word only where it cost the trace a clause or there was no trace. The reason #90 and #93 gave was never the cost: it was that the header names the tool. The gate is that reason now — a fleet of one sheds the bare word where the frame's header carries ` · <tool>` — so the frames the header sheds it on keep the row's word. Twelve rows in one file; the pane tag stays (#85); the multi-tool scenes do not move a cell. Pinned on the typed-reply route at 100 (the stock-line route draws no trailing word); fails on the revert. |
| 97 | Round fifty-nine, the fleet-hygiene operator's half (2026-09-07) | **A relayed message is an ask of a kind, worn as `relayed "…"`.** The porter column of every fleet-hygiene frame drew `"Another Claude session sent a message: …"` as the ask — 271 rows of a state the product could not produce, since `promptTitle` returned nothing for the relay and the segmenter opened no chapter on it. #30/#31/#34 held the relay unmarked for want of a reliable preamble; the preamble is `relayPrefixes`, in the tree the day before the hold, so the reason was false when written. Now `Event.Relayed()` names the one relay that is an ask — another session's message — and `Machinery()` no longer covers it: the title is the message under its own rank (above a bare slash command, below a person's words), `SessionInfo.Relayed` and `Prompt.Relayed` carry the fact, and every row that quotes the ask draws the word before the quote — `◉ relayed "the encoder is in, run the gates"` — never inside it. A background agent's echo stays machinery. The fixture models the product path. Pinned in transcript, fleet, journey and the frame; each fails on its revert. |
| 98 | Round fifty-nine, the second-day operator's half (2026-09-07) | **A search narrows the band; it does not blank it.** `/billing` said `no session matches` over nineteen blank rows while the band had drawn billing a keystroke earlier and the archive found `1 of 12`. #47's "a search names what it found" was the reason for the blanking and is what the blanking broke. The band holds the rows that match the query, the miss note under an empty live list says `no live session matches /q` and the band draws beneath it, and in the session view the archive's door stands only where the band drew nothing. Two files moved. The #47 pin asserts the new rule; pinned at 100 on `/billing`. |
| 99 | Round fifty-nine, the two-tools operator's half (2026-09-07) | **`?` and `q` share a row at every width.** At 120 the two-tools help spent a row each on `?` and `q` — a merge every narrower help makes, gated on `!board` for no reason the frame showed — and the row it cost was the tool gloss's: eight glyphs on no frame of the fleet were defined while `claude`/`opencode`, worn by 110 rows, were not, #89's sentence false at that width. The merge is unconditional; 21 files gain a definition and lose none. Pinned at 120 on the two-tools help. |
| 100 | Round fifty-nine, the second-day operator's second finding (2026-09-07) | **The card leaves the present to the trail.** In the session view the card's fallback sentence — the board column's present (#56) — was drawn three rows above the trail's own HEAD row in the same column. Where the card's second row begins with a sentence a trail row below it draws, the card keeps only its tag, right-aligned as the board card's bare tag row is. Nine files, one row per session-view frame; the #41 count of "thinking…" goes from two to one, its reason (the trail already carries the present) intact. Pinned at 152. |
| 101 | Round fifty-nine, the fleet-hygiene operator's second finding (2026-09-07) | **The hide note is the refusal's sentence.** `2 harness hidden · A, then x` under a strip that said `1 hidden`, in the fleet with two sessions called harness, read as a count that was wrong; the digit refusal already wrote `%d %s is hidden`. One word; 25 files, one row each. Pinned. |
| 102 | Round sixty, the second-day operator's half (2026-09-07) | **The session view's miss note says whose miss it is.** #98 landed on the list's note and not the card's: at 120, 152 and 220 the fleet of one said `no session matches /billing · esc clears it` three rows above the band row it found, while 80 and 100 said `no live session matches`. The card says `no live session matches /q` wherever the band beneath holds a match. The walkthrough's `/pytest` matches nothing, which is why the frame was never drawn; pinned on `/billing` at 152. |
| 103 | Round sixty, the second-day operator's second finding (2026-09-07) | **The archive says a row's verdict.** Eleven of twelve archived rows said nothing about whether the day went green or red, with 27 free cells beside `claude · fix/api-timeouts`; the clause #47 names as the one that answers "do I want this one back" was on the band and nowhere in the archive. The archive row's second line ends on the band's own clause — `verdictClause`, shared with the band — where the column has room, and sheds it first where it has not. Pinned at 152. |
| 104 | Round sixty, the two-tools operator's half (2026-09-07) | **#100 on every shape of the repeat.** #100 compared the card's whole second row with each trail row below it, so the cursor's `▸` on HEAD and a leg that draws the sentence behind its class and before its clock both defeated the rule — 69 frames of two-tools still said the present twice. The compare is on the card's sentence, what stands left of its tag, against each row with the cursor's mark taken out; the card keeps its tag either way. One assertion of round one moves with it, the hung call said once. Pinned on both shapes at 120. |
| 105 | Round sixty, the two-tools operator's second finding (2026-09-07) | **The archive's title names the session alone where the ◉ row draws the ask.** #59 gave the title the ask so a row was named and not its group; on the frames where the ◉ row two rows below draws that ask whole, with its age, the title's copy was a clipped one with its quote left open, at 100 cut mid-word. The title keeps the clause only where the viewport has scrolled off the ◉ row, the one case where it is the only copy — #59 narrowed, not refuted. Pinned at 100 on the hide's route. |
| 106 | Round sixty, the fleet-hygiene operator's half (2026-09-07) | **#97 reaches the reader.** The reader drew a user turn's raw text, so the product would have drawn the relay's envelope over two rows while the fixture's reader events carried the body and hid it — 119 rows across the five fleet-hygiene files the product could not produce. The reader draws the message inside the envelope, as the card and the trail say it; the trail's `◉ relayed` marks the relay, the reader's turn is what was said. The fixture's reader line is the line the transcript holds. The corpus does not move a cell. Pinned on `readerDoc`; fails at HEAD. |
| 107 | Round sixty, the fleet-hygiene operator's second finding (2026-09-07) | **On the board a column says its HEAD once.** A working column shows its HEAD row anyway, so a card that fell back to the present said what HEAD says five rows down — and at 120 and 152 the card's copy was the clipped one over the trail's whole one. #100's rule on the board: where a trail row says the card's sentence — whole, or clipped where the trail's is whole (`saysSame`, shared with the session-view card) — the card's second row is blank; the tag keeps its own third row. Eight files; two goldens move by one row each. Pinned at 152. |
| 108 | Round sixty-one, the fleet-hygiene and two-tools operators' one thing (2026-09-07) | **#107 compares against the rows the frame draws.** The card's compare ran over the rows the column composed, and the reply box is composited over the body afterwards: under it 39 frames said a session's present nowhere, a stuck column's hung call among them, and the docs column kept three rows with the middle one blank. `View` settles the box's place before the body (`replyBox`), `boardLines` runs the compare and skips the rows the box covers — vertically, and horizontally where the box begins inside the trail's prefix, since a row the box starts past still draws its sentence to its left. Exactly those rows return; nothing else moves. Pinned on the alarm-storm reply frame at 120. |
| 109 | Round sixty-one, the fleet-hygiene operator's second finding (2026-09-07) | **Where the reader has the screen to itself, a relayed turn wears the word on its row.** #106's reason held only beside a trail: at 80 and 100 the reader owns the width and eighteen frames drew a lead's message as the operator's own. The turn row is `❯ relayed · the message`; the mark is the row's, not the turn's — the title and the `[ ]` note go on quoting the message, since the word inside the quote clipped after a comma. Pinned at 100. |
| 110 | Round sixty-one, the second-day operator's one thing (2026-09-07) | **The reader's title keeps its clock alone where the page draws the anchored turn.** `READER · hello  add a --version flag · 17:59` stood two rows over `❯ add a --version flag  17:59` on 291 of 307 title frames — #65 deferred it for scope, and #100, #104, #105 and #107 have since folded the same shape. Where the page holds exactly one turn row and `saysSame` finds the anchor on it, the title is drawn without the clause; with more turns on the page the clause is the only thing that says which is the anchor, and stays. 628 title rows move, nothing else. Pinned at 80. |
| 111 | Round sixty-one, the second-day operator's second finding (2026-09-07) | **A fleet of one at eighty and a hundred says the present once across the rule.** The list row's second line and the trail's HEAD row stood on one physical row, byte-identical, on a hundred frames. `presentBeside` — a fleet of one, no board, the selected live session working or stuck, and no reply box over the trail — drops the row's present line, so the trace beneath moves up. The corpus was checked frame by frame: no sentence leaves the screen. Pinned at 100 on the opening and the typed-reply frames. |
| 112 | Round sixty-one, the two-tools operator's second finding (2026-09-07) | **The row the card gave up takes the rungs the tag row shed.** #107's blank row stood over a tag that had shed the model for want of room, on the board whose whole question is which model; on nineteen columns the model was on no row. `hoistTag` draws the tool word and its model right-aligned on the freed row and leaves the pane alone on the tag row, so where a column lives never moves. The #85 pin asks for the pane by itself. Pinned at 120. |
| 113 | Round sixty-one, the two-tools operator's third finding (2026-09-07) | **The alarm leg's wrap budget is the span the row draws.** `headLabelMax` reserved `waiting 10m` for a row drawing `waiting 4m`, and at 220 a question one cell over wrapped inside itself and jammed its tail against the options. `headLabelRoom` budgets against the row's own span, the constant standing where the span is unknown. One golden moves: the 152 board's question fits its row whole. Pinned at 220. |
| 114 | Round sixty-two, the fleet-hygiene operator's one thing (2026-09-07) | **#111 for any fleet, against the rows the trail draws.** The rule was gated on a fleet of one, and in a fleet of four at eighty and a hundred the selected row still repeated the trail's HEAD, byte for byte or as a worse clip, on 109 frames. The deck settles the trail column before the fleet column and the row's present line goes where `saysSame` finds it on the trail's drawn rows — #108's lesson, on the deck. The assumption form (`journeyLine` at a notional width) was measured and rejected: it disagreed with the row's own line and lost 134 sentences. Checked frame by frame with a clip-aware compare: no sentence leaves the screen. Pinned at 100 on the fleet-hygiene opening frame. |
| 115 | Round sixty-two, the fleet-hygiene operator's second finding (2026-09-07) | **Where #110 bares the reader's title, the clock goes with the clause.** The kept clock was the third copy of one time on a 24-row frame — the turn's row and the `[ ]` note carry it — and the reason for keeping it, a reader scrolled to an hour, is the case the fold excludes. The bare title is the panel and the name, as the trail's is over its subject. The #110 pin asks for no clock. |
| 116 | Round sixty-two, the two-tools operator's one thing (2026-09-07) | **The card's compare sees the question the trail wrapped.** #107 compared row by row, and a head label wrapped over `▲ design Open port 22 to` and `│  ├ the office CIDR?` hid the repeat; at 152 the card's copy was the cut one over the whole. `wrappedLabel` joins a leg row's continuation rows onto its label — the span on the right and the options row left out — and `saysSame` looks for a clipped copy anywhere in the row, behind the glyph and the class. The freed row takes the model (#112) and the pane returns to the tag row. 136 rows on 115 frames in three scenes. Pinned at 120. |
| 117 | Round sixty-two, the two-tools operator's second finding (2026-09-07) | **The `↳ N new legs` digest yields to the model where the divider draws the count.** The tag row spent its cells on a count the column's own `you were here` divider draws five rows down, and shed the model to do it — `opus-4-1` on no row of 26 frames, on the board whose question is which model. #85's reason for the look clause, applied to the count: `yieldNewLegs` gives the row to the highest rung that fits, not where #112 already hoisted. The #85 pin asks for the pane and the word by themselves. Pinned at 120. |
| 118 | Round sixty-two, the two-tools operator's third finding (2026-09-07) | **The digest's floor is its shortest clause.** Two floors of twelve refused the pane rung to a row where `claude · ⌁ :1.0` fit in exactly the row's cells, so the pane — what attaches and is nowhere else (#85) — was on no row for one of two rows called api. `digestFloor` is eleven, the width of `↳ 1 new leg`. 33 rows in one file. Pinned at 80. |
| 119 | Round sixty-two, the second-day operator's one thing (2026-09-07) | **The trail's present is the session's, not the view's.** A live session selected while the archive list was on screen — a search that emptied the archive's rows, so the cursor never left it — drew `scouting will appear here` over its own prompt row, fifty seconds old, under a header that said `●1`. `trailOpts` gated HEAD on `!m.archiveView` as well as `s.Live`; the view gate is gone. The corpus does not move a cell. Pinned at 80 on the search-then-archive route. |
| 120 | Round sixty-two, the second-day operator's second finding (2026-09-07) | **In the archive at eighty and a hundred the reader's title names the session alone where the turn row draws the ask.** #105's rule one column over: the reader has the screen at those widths, so no trail title stands to be repeated by a bare name, and the title's copy of the ask stood two rows over the turn row's whole one. `readerTitleBare` names the session and the day it added up. Wider, the trail's title beside it carries the ask and the reader's stays. Pinned at 80. |
| 121 | Round sixty-two, the second-day operator's third finding (2026-09-07) | **The header leaves two cells before its chips.** The identity ended one cell from `●1  archive 12`, whose own separator is two, so `· claude ●1` read as one clause. The reserve is two cells; where the identity is already clipped it costs a letter, not a clause. Pinned at 80 on the archive's twelfth row. |
| 122 | Round sixty-three, the second-day operator's one thing (2026-09-07) | **The archive reader's title keeps the ask, not the cursor's clock, where the turn row draws the ask.** At 120 and up the title read `fix the 401 on token refresh  15:31` over a turn row that timed those same words `15:00`: the clock was the trail cursor's moment, not the ask's. Where the page draws the turn row the clock goes with the clause it does not time (#115's rule); the ask stays, since the trail's title beside it carries the day and the reader's is the one that names the ask. Ten rows move. Pinned at 152. |
| 123 | Round sixty-three, the second-day operator's second finding (2026-09-07) | **The bare archive title runs the trail's own ladder.** `readerTitleBare` said the day in the compact form unconditionally — `cli · 5d · 1⚑`, 24 cells of 79, a glyph the 80-column legend never names — where the trail's title had drawn `1 ship` in words. The long form where it fits, compact where it does not. Pinned at 80. |
| 124 | Round sixty-three, the two-tools operator's one thing (2026-09-07) | **The needs-you row leaves its question to the trail beside it.** #111 and #114 gated the rule on working and stuck, so the one state whose sentence is a question — byte-identical two rows apart at a hundred, the clipped copy over the whole at eighty, on 87 frames — never reached the compare, and the compare was row by row where the trail had wrapped it. The gate takes `wantsAttention` and the API error, the attention branch's line runs the compare too, and the compare sees `wrappedLabel` (#116's helper). The freed rows are spent: at eighty a folded fleet gains a whole session row. Pinned at 100 and 80. |
| 125 | Round sixty-three, the two-tools operator's second finding (2026-09-07) | **The compare sees the row's sentence without the verdict it spliced in.** At a hundred the row had room for the newest verdict — `● fix tokens.py 18✓ 2✗ · for 22m` — and the compare missed the trail's HEAD row, which says the rest; the verdict is on the trail's test row. `verdictRe` strips the clause for a second compare. 25 rows in two files. Pinned at 100 on the search route. |
| 126 | Round sixty-three, the two-tools operator's third finding (2026-09-07) | **A column the reply box begins inside is composed at the width the box leaves.** The band was composed at its width and cut where the box began, and the cut took only the right-aligned age, leaving a mark over eleven blanks and the row without its clock while both fit. With the box settled before the body (#108), `boardLines` composes that column at `left-x-1`, one right edge for its rows. Twelve rows in two files. Pinned at 120 on `j`,`r`. |
| 127 | Round sixty-three, the fleet-hygiene operator's one thing (2026-09-07) | **#114's compare is not fooled by the trail's cursor.** At Lv2 the cursor lands on HEAD and draws its mark between the glyph and the class — `●▸test` — and the compare took the mark for part of the sentence, so on every Lv2 frame at eighty and a hundred the row repeated HEAD, 48 frames in twelve files. The trail row is compared with its first `▸` replaced by a space, as the card's compare does (#104). The freed rows carry sessions: a folded fleet's counter shrinks. Pinned at 100 on `tab`. |
| 128 | Round sixty-three, the fleet-hygiene operator's second finding (2026-09-07) | **The `[ ]` note counts and quotes the turn and leaves the clock to the row.** The note stands only while the turn it landed on is drawn, and that row carries its clock at every width — 65 of 65 note frames, 870 of 870 turn rows — so the note's clock was never the only copy, and at 220 it was the clipped one over the whole. The cells the clock gave up go back to the quote. #20's pins hold. Pinned at 80. |
| 129 | Round sixty-three, the fleet-hygiene operator's third finding (2026-09-07) | **The `↳ N new legs` digest yields to the pane alone where no rung stands above it.** #117 returned as soon as the tag row's rung was the top of the ladder, so in a one-tool fleet the count stayed beside the pane while the divider drew it. The blunt form — every digest yields — was measured and rejected, since it dropped `· 1 red` and `· 1 ship`, clauses the divider does not draw; the narrow form keeps the gate on a digest that is only the count. Pinned at 120 on many-idle. |
| 130 | Round sixty-four, the second-day operator's one thing (2026-09-07) | **The archive row's verdict is the band's ladder.** #103 appended the whole clause or nothing, so 82 of 281 archive rows said nothing about green or red, the red rows first since `✗ red 12✓ 1✗` is the longest form. The row runs `recentKeep`'s forms — the counts, the clause's two words, last the mark alone — and bare rows fall from 82 to 19. 67 rows move, none loses anything. Also: #122's pin ended on `[`, which parked the cursor where the clocks agree, and passed on the revert; it walks the corpus's own route now and fails on it. Pinned at 100 on `A`. |
| 131 | Round sixty-four, the second-day operator's second finding (2026-09-07) | **The trace note leaves the quote to the row that draws it.** The footer's `↪ sent "please continue" · to ⌁ main:0.0` was forty cells, its first twenty-four the row's own sixteen rows above, and it shed `enter attach`, `tab deeper`, `r reply` and `/ search` to pay for them; on 75 of 78 note frames the row draws the bytes. `View` settles the body before the footer (#108's shape) and `noteLeavesTheQuoteToTheRow` trims a sent or answered note to its destination where a drawn row carries the bytes; the three frames where no row does keep the note whole. 73 footers move; the keys come back. Pinned at 80 on `r`,`1`. |
| 132 | Round sixty-four, the two-tools operator's one thing (2026-09-07) | **The column #112 hoisted yields the count too.** `yieldNewLegs` was skipped where the card row was blanked and returned early where that row was blank, so of the two columns called api only one drew `↳ 1 new leg` though both drew the divider with one leg under it. It runs on every column; where the rungs already stand on the row above, the tag row keeps the pane. 52 rows in five files. Pinned at 120, 152 and 220. |
| 133 | Round sixty-four, the two-tools operator's second finding (2026-09-07) | **On the reply frame the needs-you row leaves its question to the box.** The box covers the trail's row (#108) but says the question itself under its own head, one row up; the row's copy was byte-identical at a hundred and the cut one at eighty. `View` keeps the box's rows (`replyRows`), and where the box is up the compare runs against what it says. Six rows. Pinned at 100 and 80 on `r`. |
| 134 | Round sixty-four, the two-tools operator's third finding (2026-09-07) | **A chapter note whose quote would be cut is the count alone.** #128 gave the clock's cells to the quote, and where the room did not hold it the footer drew `❯ 1/1 · "tighten the…"` under a turn row drawn whole four rows up, while the same route at 120 and 152 drew the bare count. The footer's shed skips the cut form for a turn note. Twelve rows. Pinned at 80 on alarm-storm. |
| 135 | Round sixty-four, the fleet-hygiene operator's one thing (2026-09-07) | **#129's gate is a strict match, not a suffix test.** The lead's digest orders its clauses tallies first and sheds the trailing look clause for width, so at 152 and 120 `↳ 3 sent since, none back · 1 new leg` ended in `new leg`, passed the gate, and was dropped whole — a narrower terminal said strictly less about the lanes that never came back, which is what #129's own recorded reason forbids. `newLegsOnly` matches the count and nothing else. 22 digests come back, nothing else moves. Pinned at 152 on subagents. |
| 136 | Round sixty-four, the fleet-hygiene operator's second finding (2026-09-07) | **The look clause is the divider's own words.** `↳ 3 new legs · looked 1h ago` stood over a divider reading `you were here · 1h ago` with three legs under it: both clauses the divider's, on 271 board rows. Where the divider carries that same age, the trailing look clause is taken off before the strict match, so the digest yields as #129 has it; card rows beside another session's trail keep theirs. Pinned at 220 on many-idle. |
| 137 | Round sixty-four, the fleet-hygiene operator's third finding (2026-09-07) | **A folded list's slack goes to the band.** The band's gate ran on the whole list, so a list that overflowed its body never reached it, and the rows an entry-whole window could not use were blanked over `41 archived · A browses` — 74 frames with forty-one sessions behind the key. When the window leaves slack, the band is offered it with the archive's line folded into its header, as when the list fits. One file, 46 rows, no sentence lost; one round-one assertion accepts the band's header as the column's last word. Pinned at 80 on `r`. |
| 138 | Round sixty-five, the second-day operator's one thing (2026-09-07) | **The trail's title leaves the span to its own first row.** `TRAIL · api · 3h · 1⚑ 1✗` stood over `◉ "fix the 401 on token refres… 3h ago` two rows under it on 174 frames, and the five-cell repeat pushed the day into glyphs where `· 1 ship · 1 red` fit. `trailDayHere` drops the span where the trail's first row draws that same age — not under the reply box, not when scrolled past its first row — and the totals stay. 171 rows; no frame loses the age. Pinned at 120 on `2`. |
| 139 | Round sixty-five, the second-day operator's second finding (2026-09-07) | **Past the reply box no lone clock stands with nothing to time.** The overlay kept a covered row's tail unless it was a bare word without a digit, so a hidden turn row left `┐ …    17:59` past the box on 81 frames, every one a lone clock. The peek is kept only where it holds a space; #56's remainders (`2✗ 3m`, `for 33m`) are two words and still stand. Pinned at 152 on `r`. |
| 140 | Round sixty-five, the fleet-hygiene operator's one thing (2026-09-07) | **The selected row's compare decides, not the state.** `presentBeside` refused an idle row, whose second line falls back to its last leg's label — the trail's own row beside it — on 24 frames at eighty and a hundred. The state test is gone; 22 of the 24 freed rows carry a session; no verdict sentence matches a trail row, so none is dropped. Pinned at 80 on `j`. |
| 141 | Round sixty-five, the two-tools and fleet-hygiene operators' shared finding (2026-09-07) | **The selected row's count yields to the trail beside it.** The list is the board's column below the board's width, and `↳ 1 new leg` on the selected row was what the trail's `you were here` divider and the leg under it said, one panel over — on two frames the model it cost was on no row of the frame. `countBeside`: the selected live row, the box down, the trail's rows carrying the divider, the digest only the count with the look clause the divider carries taken off first; the ladder takes the cells. 43 rows in nine files. Pinned at 100 and 80 on the search route. |
| 142 | Round sixty-five, the fleet-hygiene operator's second finding (2026-09-07) | **The look clause goes whether or not the count is alone.** #136 trimmed the clause and then, where the count was not alone, redrew the row from the untrimmed string, so `↳ 1 new leg · 1 ship · looked 3d ago` stood over a divider saying `3d ago` while two columns beside it had yielded. The row is redrawn without the clause and with its other clauses; 114 rows in eleven files, every diff exactly the clause; one round-41 literal trimmed to the scope word it tests. Pinned at 220 on many-idle. |
| 143 | Round sixty-five, the two-tools operator's second finding (2026-09-07) | **The count yields before the rungs are hoisted.** #132 ran `hoistTag` first, against a tag row that still carried `↳ 1 new leg`, then yielded the count, leaving `opencode · sonnet-4-5` stranded on the row above `⌁ dev:2.0` while the whole 33-cell tag fit the 35-cell column. The order is blank, yield, then hoist only what the tag row still cannot afford; both api columns name tool, model and pane on one row. 44 rows in two files. Pinned at 120, 152 and 220. |
| 144 | Round sixty-five, the two-tools operator's third finding (2026-09-07) | **The session card's count yields to its own trail.** The card's third row was `↳ 1 new leg · looked 25m ago` over its own divider six rows down in the same column — the case #136's carve-out for another session's trail does not cover. Where the row is only the count and the look the read-line below draws, it goes and the trail takes it. Six rows. Pinned at 120, 152 and 220 on `3`,`tab`. |
| 145 | Round sixty-six, the second-day operator's one thing (2026-09-07) | **Every archive row says green or red.** #130's ladder ran behind the whole branch, so where not even the mark fit the row gave the branch every cell and dropped the mark — 19 rows, and at eighty the band said `✗ 4h` of a session whose archive row said `claude · fix/webapp-timeouts`: the deeper view said strictly less. The branch yields the cells for ` · <mark>`; a clipped branch still names where the day happened, a missing mark names nothing. Exactly the 19 rows move. Pinned at 80 on `A`. |
| 146 | Round sixty-six, the second-day operator's second finding (2026-09-07) | **The hide refusal counts only the live.** `the only session stays` stood over a band that counted twelve more and named eight by digit; #10 made live and archive two words, and the same function's other refusal already draws the distinction. `the only live one stays`, at 79 of 80 cells, costs no key. Ten footers; round fourteen's literal moves with it. Pinned at 80 on `x`. |
| 147 | Round sixty-six, the fleet-hygiene operator's one thing (2026-09-07) | **The board's stranded rows take the band.** #47 refused the band on the board because "the board's blank rows are more columns'"; where every live session already has a column no further column can ever fill them, and at 152 the board stood on twenty-six blank rows under `41 archived · A browses` while the list one width narrower named five finished sessions. Where the strip is the archive clause alone and two rows are free, it gives way to the same band the list draws, the header folding the strip's line. 243 rows added on 111 board frames, none lost; a packed board with `+N more` or a fleet that has spent its digits does not fire. Two board goldens re-baselined, the band their only diff. Pinned at 120, 152 and 220. |
| 148 | Round sixty-six, the fleet-hygiene operator's second finding (2026-09-07) | **A scrolled fleet column never opens on air.** The guard that forbids it ran before the loop that escapes a cut entry's middle, and that loop walked forward onto the separator that ended the entry — twelve scrolled frames opened on a blank row that cost a session. The guard is applied again after the escape. Pinned at 80 on the walkthrough's scrolled list. |
| 149 | Round sixty-six, the two-tools operator's one thing (2026-09-07) | **The note's compare takes off whatever the row draws right-aligned, and every trailing clause while the row still holds its quote.** #131's matcher stripped a pane and one trailing clause; a two-tool fleet's row wears the tool word where a pane would stand (#50) and at 220 carries the clock and the count, so the note kept `"go on"` on five frames and at eighty paid for it with `tab deeper`. The quote guard keeps #131's other half: a row that draws no bytes is never trimmed into one that seems to. Pinned at 80 and 220 on the typed-reply route. |
| 150 | Round sixty-six, the two-tools operator's second finding (2026-09-07) | **The card's third row keeps its trace and leaves the count and the look to its own read-line.** #144 dropped the row only where it was nothing but those clauses; beside a trace they stayed, and the card said twice what its own column said once, on 190 rows in thirteen files. `countLessBeside` returns the row less the look the divider carries and less the count clause, the `↳` moving on to the next clause of the group so the lanes (#135) and the ship stay; where nothing is left the row goes as #144 has it. Pinned at 120, 152 and 220. |
| 151 | Round sixty-six, the two-tools operator's third finding (2026-09-07) | **On the board and the selected list row the count clause goes with its other clauses.** `yieldNewLegs` took the look off and then redrew a row whose count was not alone with the count still on it, and the list's strict gate did the same — 281 board rows and thirteen list rows said the count their divider drew beside `· 1 red`. #129's reason holds whether or not the count is alone, as #142 ruled for the look: the count goes, the `↳` moves on to the clause that stays. Two pin literals move to what they test (#136's and round forty-one's). Pinned at 220 on many-idle. |
| 152 | Round sixty-seven, the second-day operator's one thing (2026-09-07) | **Every refusal that names the fleet of one counts only the live.** #146 scoped the hide refusal; the move and zoom-out refusals still said `the only session` two rows under a band numbering nine, on 26 rows. `the only live one` everywhere but the archive view, where the unscoped word is the true one; each footer had exactly one free cell, so no key sheds. Pinned at 80 on `shift+tab`, `j` and the archive. |
| 153 | Round sixty-seven, the second-day operator's second finding (2026-09-07) | **The one-row trail's refusal names the leg.** `the trail is one row` stood over a trail panel drawing two glyph rows — the prompt and HEAD — because the count was prompts plus legs and this trail has no leg yet; the same-shaped row is a cursor stop elsewhere, so the shape did not say which it was. `no leg to move to` says what is true of it. Round sixteen's literal moves with it. Pinned at 80 on `ctrl+u`. |
| 154 | Round sixty-seven, the fleet-hygiene operator's one thing (2026-09-07) | **The board's band is a column too.** #147 composed the band at the board's whole width and the reply box cut it: fifteen rows were a verdict and a clock past the box with no session, forty-five were a row split by 68 cells of box, its left half without verdict or clock. Where the box begins inside the band it is composed at the width the box leaves, floored at the list's own column width — #126's rule three lines down. Sixty rows in twelve frames, none lost. Pinned at 120, 152 and 220 on `j`,`r`. |
| 155 | Round sixty-seven, the two-tools operator's one thing (2026-09-07) | **The session card's tag row goes where the header has said every clause of it.** #100 left the card its tag; two rows under a header that had just drawn `opencode · sonnet-4-5 · ⌁ dev:2.0` the row said strictly less than the header, on 63 frames in nine files. The card's second row is settled before the trail's height is fixed, and where every ` · ` clause of what is left stands in the header the row goes and the trail and the reader each take a cell. At 80 and 100 the header sheds the tag and the card is its only place, so those frames do not move. Pinned at 120, 152 and 220 on `j`,`tab`. |
| 156 | Round sixty-seven, the two-tools operator's second finding (2026-09-07) | **The hide refusal is `the live one stays`.** #146's recorded reason — the scoped word costs no key — was false on two frames: the 23-cell note pushed the 79-cell footer over and shed ` · tab deeper`, the frame's only naming of the way in. The 18-cell form leaves four cells spare and is still scoped to the live; the other refusals keep `the only live one`, having a cell to spare. Two literals move. Pinned at 80 on `x` in second-day and first-session. |
| 157 | Round sixty-seven, the fleet-hygiene operator's second finding (2026-09-07) | **A hidden session does not cost the board its band.** #147's gate was a strict string match on the strip, and a hidden count — a clause of the band's own header (#86) — broke it: after `x` the board stood on twenty blank rows again while the list at a hundred kept its five. The gate is that the strip names no session and ends in the archive clause, with the overlap guard the list's gate carries. 126 rows in 21 frames, ninety of them air that now names a session; one round-one literal accepts the band's header as the way back. Pinned at 120, 152 and 220 on `x`. |
| 158 | Round sixty-seven, the fleet-hygiene operator's third finding (2026-09-07) | **A covered column's sliver is not a lone clock.** #139 tested the whole peek right of the reply box for a space, so where the peek ran on into the next column whole, the covered column's one-token sliver — `…    6m` for a leg whose class and label were under the box — rode along on 57 cells in twenty frames. The peek's first segment, up to the first rail, is tested on its own. Sixty-three rows, none lost. Pinned at 120 and 220 on many-idle. Also this round: #151's list half and #149's trailing-clause loop each reverted with the suite green; their pins now read the list at a hundred and very-long at 220. |
| 159 | Round sixty-eight, the two-tools operator's one thing (2026-09-07) | **The zoom-out refusal is `nothing to zoom out to`.** The 42-cell `the only live one · nothing to zoom out to` took the eighty-column footer down to `j/k move · ? help · q quit` with nine cells idle, naming neither the way in nor the way deeper, on eight frames — beside the footer where #156 had just bought `tab deeper` back. The 22-cell note leaves both keys; the fleet-of-one clause was the hide refusal's, not this one's. A general rule — the note yields before the footer yields the way in — was measured and rejected, since #14, #17 and #18 rank a note's clauses above optional keys. #152's literal moves. Pinned at 80 on `shift+tab` in second-day and first-session. |
| 160 | Round sixty-eight, the two-tools operator's second finding (2026-09-07) | **The trail stands left of the reader it navigates.** #19 put the navigator to the left of what it navigates; the three-column deck's fleet, middle, trail order was #9's, written for the mirror — a rendering of something else, which nothing navigates. Where the middle is the reader, the deck is fleet, trail, reader, and the tab to Lv3 keeps the trail on the reader's left instead of carrying it eighty cells across the screen (#46). With the mirror in the middle nothing moves. Ten frames in second-day. Pinned at 120 on `2`,`tab`,`tab`. |
| 161 | Round sixty-eight, the second-day operator's one thing (2026-09-07) | **An archived row says whether it shipped.** #145's ladder took the first form that fit behind the whole branch, so `opencode · spike/billing · ✓` stood where the band had said `✓ shipped` one keypress earlier — and inside the archive the band is off the screen, which makes #145's recorded reason false there. Where the two-word form is a word the mark cannot say — `shipped`, not `red` or `green` — the branch yields its tail for it, the tool word and four cells of branch standing. Exactly 23 rows, all archive second lines. Pinned at 80 and 100 on `A`. |
| 162 | Round sixty-eight, the second-day operator's second finding (2026-09-07) | **The chapter keys' refusal names the prompt.** #153's override sent `no leg to move to` for `[` and `]`, which move by prompt and are glossed `[ ] chapters` three cells to the left; the true answers were composed and thrown away. `no earlier prompt` and `no later prompt`, the `G is the present` clause going on a one-row trail where `G` refuses too. Eight rows, no cell cost. Pinned at 80 on `tab`,`[` and `tab`,`]`. |
| 163 | Round sixty-eight, the fleet-hygiene operator's one thing (2026-09-07) | **A search the live fleet does not answer keeps the board's band.** `no session matches /reconcile` stood over 29, 35 and 43 blank rows while eight archived sessions answered the query and the list twenty columns narrower named five under `no live session matches` — #98's complaint on the board, #147's shape one keystroke later. The board's miss draws the band the list draws, its note saying the miss is the live board's; the canonical corpus is byte-identical, since the fold fires only where the live fleet answers nothing. Pinned at 120, 152 and 220 on `/`,`reconcile`. |
| 164 | Round sixty-eight, the fleet-hygiene operator's second finding (2026-09-07) | **The band narrowed by a search counts what the search left.** Under `/pytest` the band held the one row that matched (#98) under a header saying `41 archived`, which read as #47's newest-first contract when it was not; the deck's own header says `2 of 4` for the live half. `recent · 1 of 41 archived · A browses`, the shed ladder unchanged. Ten rows. Pinned at all five widths. |
| 165 | Round sixty-eight, the fleet-hygiene operator's third finding (2026-09-07) | **The no-pane refusal names the key and keeps the way in.** `no pane · nothing to type into` cost the eighty-column footer `tab deeper` and the hundred-column footer `/ search`, and said `no pane` twenty cells from the footer's own `enter · no pane` on the same row — #156's and #159's harm on the refusal neither reached. Shortening alone bought no key back; naming the key does, in the form `mirror needs 110 columns` already uses: `reply needs a pane`, `attach needs a pane`. Four rows; two literals move. Pinned at 80 on the paneless session's `r` and `enter`. |
| 166 | Round sixty-nine, the two-tools operator's one thing (2026-09-07) | **The later-chapter refusal is `no later prompt` everywhere.** `no later prompt · G is the present` cost the eighty- and hundred-column footers the key the `[` refusal beside it kept, and named a key the help already names at every width; #161 had already shed the clause where G refuses too. One row. Pinned at 80, 100, 120 and 152 on the two-tools scene's `]` past the last prompt, against the `[` refusal's footer. |
| 167 | Round sixty-nine, the two-tools operator's second finding (2026-09-07) | **The card's tag goes even beside another clause.** #155 shed the card's second row only when it was nothing but the tag; a row that carried a trace before the tag kept the tag, so `claude · tmux harness` stood twice on the trail at every width. `tagTheHeaderSays` splits the row at its last double space, and where the header says every clause of the tag the row keeps the trace and sheds the tag. Pinned on the two-tools scene's third session at every width. |
| 168 | Round sixty-nine, the second-day operator's one thing (2026-09-07) | **The archive door counts what the search left.** Under a search the fleet's last line said `12 archived · A browses` while the band's header (#164) said `0 of 12 archived`, and the door named a key that lands on nothing. The door counts the archived sessions the search matches in the band's form, `0 of 12 archived`. Pinned at 80 on the second-day scene's `/pytest`. #159's pin widened to the wide deck (120, 152, 220) at the second-day operator's ask: the `nothing to zoom out to` refusal keeps `enter attach` and `a ask`. |
| 169 | Round sixty-nine, the fleet-hygiene operator's one thing (2026-09-07) | **The archive door counts what the search left wherever it is drawn.** #168 gave the count to the list's last line and left the board's strip and a fleet of one's own door counting the whole archive: at eighty `0 of 300 archived · A browses`, twenty columns wider `300 archived · A browses`, over the same fleet and the same search. One helper, `archiveDoorCount`, feeds all three sites. Eighteen rows in nine files, none lost. Pinned under `/pytest` on `many-idle` at 120, 152 and 220, `second-day` at 120 and `few-ongoing` at 220. |
| 170 | Round sixty-nine, the fleet-hygiene operator's second finding (2026-09-07) | **A row that clipped the quote still takes it from the note.** `noteLeavesTheQuoteToTheRow` said the row keeps the quote whole or clipped, then compared the row to the whole note, whose destination clause no row carries; so a clipped `"please con…` on the row left the forty-cell note standing, and the eighty-column footer lost `enter attach` and `tab deeper`. The row is compared to the note's sentence as well as to the whole note. Three rows in three files. Pinned at 80 on `many-idle` and `very-long` and at 152 on `many-idle`, on `r`, `1`. |
| 171 | Round sixty-nine, the fleet-hygiene operator's third finding (2026-09-07) | **The band's shipped row keeps its word beside worded neighbours.** On the band composed beside the reply box at 152 the one session that shipped wore the bare `✓` its `✓ green` neighbours wear with a word, because `✓ shipped` is two cells wider and fell a rung on the same floor. The band decides once whether any row of it keeps a word (#57's device), and where one does the row whose word the mark cannot say — `shipped` (#161) — keeps it at the mark's own floor. Scoped to a mixed band: the unscoped form took the eighty-column band's prompt and failed the pin that keeps a narrow band's mark. Three rows in one file. Pinned at 152 on `j`, `r`. |
| 172 | Round seventy, the second-day operator's one thing (2026-09-07) | **The archive row that shipped keeps its word on the 26-cell column.** In the archive's three-column deck at 120 the fleet column is 26 cells, and #161's floor — the tool word and four cells of branch — was two cells out of reach for the row whose tool word is `opencode`, so `opencode · spike/bill… · ✓` stood two rows under `claude · chor… · ✓ shipped` wearing the tick its green neighbours wear, while 80, 100, 152 and 220 all said `✓ shipped` of that session. The floor keeps three cells of branch. One character; two rows move in the corpus, both at 120. Pinned on the second-day scene's `A`, `tab` at 120. |
| 173 | Round seventy, the two-tools operator's one thing (2026-09-07) | **The way back is taught on the row with room for it.** #64 stood the strip's `A, then x` down on the frame whose note said it, and the note — 27 cells at eighty — cost the footer `tab deeper` and `x hide` beside a strip row with 24 cells free; at 100 `x hide`, at 120 `/ search`. #64 never said which of the two rows yields, and the measurement says the footer is the wrong one. The strip always carries `N hidden · A, then x`; where a drawn body row carries that clause the note says only what happened — #128's and #170's relocation, the hide note's form. Where no drawn row carries the count (a band that folds it into its header, #86) the note keeps the way back. `A, then x` still stands once per frame, so #64's pin holds. 42 rows in 16 files. Pinned on the two-tools scene's `j`, `x` at 80, 100 and 120. |
| 174 | Round seventy, the two-tools operator's second finding (2026-09-07) | **The trail's top refusal is `at the start`.** `at the start of the trail` is 25 cells against the twelve-cell note floor and shed a key at four of five widths — `[ ] chapters` at 80, the keys pressed next; `r reply` at 100; `a ask` at 120; `m live pane` at 152 — on 24 of the 30 frames that draw it, while `of the trail` is said by the panel's own title two rows up. The mirror of the same trail's `at the present`. Two rows; round nineteen's fixture literal moves. 34 rows in 30 files. Pinned on the two-tools scene's `tab`, `ctrl+u`, `ctrl+u` at 80, 100, 120 and 152. |
| 175 | Round seventy, the fleet-hygiene operator's one thing (2026-09-07) | **The way back yields to a key naming a level.** #173 moved `A, then x` to the strip row that had room — and on the three fleets whose strip draws the hidden count beside the archive door (`41 archived · 1 hidden · A`, 34 cells with the clause in a 32-cell column) the strip has no room, so `3 notebooks is hidden · A, then x` still cost the eighty-column footer `tab deeper`, the frame's only naming of the way deeper. The way back is the one clause of a note that yields to `enter attach`, `tab deeper`, `tab session` or `tab reader`, measured against the keys the note floor alone leaves, and only then; `A` stands on the strip and the help says what `x` does there. Three rows in three files; 100 and up byte-identical. Two synthetic pins from rounds fourteen and seventeen re-pinned to the news and the level key. Pinned at 80 on `x` over the fleet-hygiene, many-idle and few-ongoing scenes. |
| 176 | Round seventy, the fleet-hygiene operator's second finding (2026-09-07) | **The hidden count counts the search too.** Under `/pytest` the strip said `1 of 41 archived · 1 hidden · A browses` while `A` listed one archived row and no hidden group: the hidden session was one the query did not match. `hiddenDoorCount`, beside `archiveDoorCount`, feeds the strip's clause, the list's combined line and the band's header, which now also takes `archiveDoorCount` instead of its own copy; a rung under the key sheds the search clause to `0 hidden` where the column cannot hold it, so eighty falls back to `1 of 41 archived · 0 hidden · A` rather than clipping the key. Canonical corpus byte-identical. Pinned on the fleet-hygiene scene's `x`, `/pytest` at 100, 120, 152 and 220. |
| 177 | Round seventy-one, the two-tools operator's one thing (2026-09-08) | **The board's `m` note is `mirror on`.** `mirror on · beside a session (tab)` was 34 cells against the twelve-cell floor and every cell over it a second copy: `beside a session` is the help's own `m` row, `(tab)` the same footer's `tab session` sixty cells to its left. It cost the 120 footer `a ask`, `/ search` and `g grab` and the 152 footer `enter attach (prefix d returns)` — #166's and #165's harm, on the key the note is about. #37 recorded the note's form as a state, not its length; `mirror on` is still a state, the shape `the live pane` and `the conversation` use one level down. One literal; 21 rows in 21 files, the note-free footer everywhere. The board test that asked the note for `tab` now asks the footer. Pinned on the two-tools scene's `m` at 120 and 152. |
| 178 | Round seventy-one, the fleet-hygiene operator's one thing (2026-09-08) | **The archive's own header counts what its list holds.** Inside the archive under a search the chip said `archive 41 · 1 hidden` over one drawn row, no hidden group and no `x unhide` — the fourth site #176 did not reach, and its comment "the list holds both; the chip says so" was false on that frame. The identity's clause counted the drawn rows, hidden ones included, against the archived total: `1 of 41` over a list with no archived row, `1 of 0` on a fleet with a hidden session and an empty archive. The chip takes `archiveDoorCount` and `hiddenDoorCount` — `archive 1 of 41 · 0 of 1 hidden` — and in the archive the identity's clause names the search alone. Seven lines; canonical corpus byte-identical, 105 off-canonical rows moved and none lost. Pinned on the fleet-hygiene scene's `x`, `/pytest`, `A` and `x`, `/eda`, `A` at five widths. |
| 179 | Round seventy-one, the second-day operator's one thing (2026-09-08) | **The archive reader's title leaves the ask to its turn row at every width.** At 120 and up the title drew `READER · fix the 401 on token refresh` two rows over a turn row that draws those words whole, with the trail's `◉` copy a third time on that row — the repeat #105, #110, #120 and #138 each folded, standing here because #122 held that a bare title would repeat the trail's title beside it. The day is the half that repeats, not the name: the live reader's `READER · hello` stands beside its own card at every width (#115). Where the turn row is drawn the title is bare, and where a trail title stands beside it that title carries the day. Twelve rows, all the second-day scene's; #122's own pin, which guards the clock, still passes. Pinned on `2`, `tab`, `tab` at 120, 152 and 220. |
| 180 | Round seventy-two, the two-tools operator's one thing (2026-09-08) | **A note shorter than the reserve costs no key.** The footer held twelve cells for any note, the room a chapter note grows back into (#134); `mirror on` is nine cells with no longer form, so at 120 pressing `m` on the board shed `a ask` — eight cells — to stand twelve blank ones beside a nine-cell note, and changed nothing else on the screen. #177's "the note-free footer everywhere" was false at 120. The reserve is twelve or the note's own width, whichever is less, at the fit and at the no-keymap guard. Three rows in three files, the 120-column boards; the 62 chapter-note frames untouched. Pinned on the two-tools scene's `m` at 120 against the note-free board's keys. |
| 181 | Round seventy-two, the second-day operator's one thing (2026-09-08) | **The archive reader's clock goes with its clause.** At eighty the title read `READER · fix the 401 on token refresh   15:31`: the clock is the anchored leg's, the ask is timed `15:00` on its own turn row, and the page had scrolled past that row so nothing corrected it. #64 dropped the anchored row's clause where what survived its cut was the name already on the row, and left the clock it timed standing alone beside a name it does not time; #115 says the clock goes with the clause. Where the clause goes, the clock goes with it. One row in the corpus; two of #64's and #66's pins traded a `15:31` proxy for an anchor check. Pinned on `2`, `tab`, `tab` at five widths: no reader title wears a clock with no clause to time. |
| 182 | Round seventy-two, the fleet-hygiene operator's one thing (2026-09-08) | **The board's miss keeps the doors beside it.** A search nothing on the board answered returned before the strip was drawn, so `no session matches /eda · esc clears it` stood over 27 blank rows with no hidden count, no archive door and no `A` — while a hidden live session did answer the search, the very row `A` lists, and the same fleet twenty columns narrower drew `0 of 41 archived · 1 of 1 hidden · A` from the list's own tail (#169's shape). The miss draws the strip under it, 69 of 78 Lv0 miss frames. Canonical corpus byte-identical, 57 off-canonical rows moved, none lost. Pinned on the fleet-hygiene scene's `x`, `/eda` at 120, 152 and 220. |
| 183 | Round seventy-two, the fleet-hygiene operator's second finding (2026-09-08) | **The fleet's search counts what the board can draw.** `/p · 3 of 4` stood over three columns that all answered the search and a strip saying `1 of 1 hidden`: four of four answered, and the fourth is a row the numerator can never reach, its count being the door's beside it (#176). The denominator is the sessions on the board, `3 of 3`. Canonical corpus byte-identical, 275 off-canonical rows moved. Pinned on the fleet-hygiene scene's `x`, `/p` at five widths. |
| 184 | Round seventy-three, the second-day operator's one thing (2026-09-08) | **A sent trace says when.** At eighty the fleet entry's trace row spent 24 of its 28 cells on `↪ sent "please continue"` and dropped ` · 0s ago`, while every other clock in the column stood and the same row drew `↪ sent "go on" · 0s ago` once the line was shorter. #33's reason — a typed line keeps its bytes over the clock — names the bare `↪ sent · 0s ago`, not a clipped quote; the rung below already accepted a three-cell stub. A rung above it: the quote yields its tail for the clock while an eight-cell stub of it survives, `↪ sent "please co…" · 0s ago`; below that the bytes stay whole as #33 has it. 185 rows in eight files, none lost; round thirteen's literal loosened to the trace's head. Pinned at 80 on `r`, `1` over the second-day and first-session scenes. |
| 185 | Round seventy-three, the two-tools operator's one thing (2026-09-08) | **The mirror's undo is named while the mirror stands.** #62 held `m` back from the shed only while its own note stood; one keypress later the note was gone and at 120 the key went with it, so the session view drew the pane's own panel under a footer naming no way back to the conversation — ten plain Lv2 frames in three files, the whole exposure, since the mirror cannot open below 110. One shed order for the note-free and the noted footer, with `m` held back wherever the mirror owns the panel; `h/l session` yields, `enter attach` and `a ask` stay (#168). Under another note that note's keys come first, so `at the start` at 120 is left as it is. Ten rows in three files. Pinned on the two-tools scene's `m`, `2`, `tab` at 120, 152 and 220. |
| 186 | Round seventy-three, the two-tools operator's second finding (2026-09-08) | **The answer's digit is the row's.** #128 gave a trace note's bytes to the row that draws them, and the digit is the answer's bytes — which line went — yet `↪ answered 1 · to ⌁ ops:0.0` stood eight cells wider than it needed over a row three lines up saying `↪ answered 1 · 0s ago`, costing the hundred-column footer `x hide` and the 152 footer the attach aside. Where a drawn row begins with the note's head the note keeps the verb and the destination, `↪ answered to ⌁ ops:0.0`, the shape `↪ sent to ⌁ dev:2.0` already has. Pinned on the two-tools scene's `r`, `1` at 80, 100, 120 and 152. |
| 187 | Round seventy-three, the fleet-hygiene operator's one thing (2026-09-08) | **A hide refused keeps the way deeper.** `billing stays · dead on the API` (31 cells) cost the eighty-column footer `tab deeper`, the frame's only naming of the way deeper, while `etl stays · it hangs` (20) on the same scene at the same width kept it; the comment on the literal said "short enough to stand whole beside the help at 80", true of the help and false of the way deeper. The clause after `stays` is the selected row's own state, drawn on that row, in the header chip and in the trail beside, so it yields to a key naming a level exactly as the way back does (#175), and only where the key comes back. Two canonical rows, nine off-canonical; one round-eighteen assertion re-pinned to the name and the level key. Pinned at 80 on `x` over the alarm-storm and very-long scenes' alarmed rows. |
| 188 | Round seventy-three, the fleet-hygiene operator's second finding (2026-09-08) | **A hidden live column names its pane once.** On the archive board the hidden session's row drew `⌁ work:2.0 · main` (#53's "where it lives") two rows over the column's tag row drawing `⌁ work:2.0` again. The tag row is the invariant, so the tag is decided before the row that stands over it and that row keeps the branch, which the tag cannot say: `main` over `⌁ work:2.0`. The fleet list and the archive list, which draw no tag row, are untouched. Canonical corpus byte-identical; 102 off-canonical columns in 27 files. Pinned on the many-idle scene's `x`, `A`, `esc` at 120, 152 and 220. |
| 189 | Round seventy-four, the second-day operator's one thing (2026-09-08) | **The archive reader's title says what the header does not.** #105 kept the ask on the title where the page has scrolled past the turn row, "the one case where it is the only copy" — and at eighty it is not the only copy: the identity header two rows above titles the archived row by its ask, whole, so `READER · fix the 401 on token refresh` stood under it and the 24-row frame named neither the session, nor when it ran, nor how it ended. Where the header says the clause the title sheds it (#167's device) and spends its cells as the same route does twenty columns wider: `READER · api · 3h · 1 ship · 1 red`. One row in the corpus. Pinned on `2`, `tab`, `tab` at five widths: no reader title repeats a clause of the header. Held, at the operator's own ask: the archive's ship row at eighty as a clipped third copy of the ask (`◆ ship   fix the 401 on token refresh…`), a content-row rule with 40 rows across two scenes that wants measuring against `very-long`, where subjects and asks differ. |
| 190 | Round seventy-four, the fleet-hygiene operator's one thing (2026-09-08) | **A width refused keeps the way deeper.** The eighty-column Lv1 footer leaves 22 cells beside `tab deeper`; `no board under 110 columns` is 26 and `mirror needs 110 columns` 24, so both cost that footer the frame's only naming of the way deeper — the harm #175 and #187 folded, on the very form #165 held up as the one that "names the key and keeps the way in"; it never had. The unit is the one word the person's own terminal supplies: it yields to a key naming a level under `levelKeyLost`, and only where the key comes back; the number and the key stay. Eighteen canonical rows in nine files, 100 and up byte-identical, no test touched. Pinned at 80 on `m` and `shift+tab` over the fleet-hygiene and many-idle scenes. |
| 191 | Round seventy-four, the two-tools operator's one thing (2026-09-08) | **The reserve is what the note draws.** #180 stopped the footer holding twelve cells for a note with no longer form; a note whose only longer form is far wider than twelve is the same case. The reader's chapter note has two forms — `❯ 1/1` and the count beside the whole 41-cell prompt — and nothing between, so at eighty the row stood `❯ 1/1` beside 22 blank cells with `space unfold`, the reader's own key, shed for them; at a hundred `a ask`, at 120 `n/N`. After the shed and the no-keymap guard, a key comes back while the note is drawn at the same width beside it: `noteDrawnIn` measures the form the display loop would choose, `keysHeld` is the backfill run against that measure, keeping #39's discipline. Reducing the reserve itself was tried and rejected: it bought `tab deeper` with the note's own clock and quote. 27 rows in 15 files, every one a key returning, no note changing a byte. Pinned on the two-tools scene's route into the reader's previous turn at 80, 100 and 120. |
| 192 | Round seventy-five, the second-day operator's one thing (2026-09-08) | **The row that shipped says what the frame does not.** At eighty the archive's ship row drew `◆ ship   fix the 401 on token refresh…`, a clipped third copy of the ask the identity header and the `◉` row both draw whole, and the cut threw away `(commit)`, the only word on the row that says how the day ended — #189's finding held for measuring. Measured: `very-long`'s ship subjects carry no bracket, so a rule keyed on `nameAndBracket` leaves it byte-identical. Where a ship label is the trail's own ask with only a bracket after it and will not fit, the row draws the bracket's word, `◆ ship   commit`; a label in the session's own words is untouched. `TrailOpts.Ask` carries the leg's prompt. 97 rows in six files, every one with the ask whole elsewhere on its frame. Pinned on the second-day scene's `A` and `2`, `tab` at 80, 120 and 152. |
| 193 | Round seventy-five, the second-day operator's second finding (2026-09-08) | **The archive board's footer names the chapter keys it answers to.** `[` and `]` act on the archive's Lv1 list as on the live one and refuse there with `no earlier prompt`, but both archive Lv1 keymaps were written out without `[ ] chapters`: 133 idle cells at 220, while one `tab` deeper the footer named it with four cells to spare. The key is named; the existing shed order takes it at 80 and 100. One golden re-baselined on that footer row. Pinned on the second-day scene's `A` at 120, 152 and 220. |
| 194 | Round seventy-five, the fleet-hygiene operator's one thing (2026-09-08) | **A trace keeps the way deeper.** `↪ sent to ⌁ harness:1.0` is 23 cells against the 22 the eighty-column Lv1 footer leaves beside `tab deeper`, so a reply to the fleet's own namesake cost the frame its only naming of the way deeper while the same keys on `⌁ tinker:0.0` (22) kept it — the harm #175, #187 and #190 each folded. The destination proves where the line landed (#39) and stays; the arrow is its preposition, and `to` answers no question the glyph does not, since the header, the strip and the tag row all draw the pane bare. It yields to a key naming a level, and only where the key comes back: `↪ sent ⌁ harness:1.0`, `↪ answered ⌁ ops:0.0`. Four canonical rows in four files; one assertion of #186's pin re-pinned to the destination. Pinned at 80 on the fleet-hygiene, alarm-storm and subagents scenes' replies. |
| 195 | Round seventy-five, the two-tools operator's one thing (2026-09-08) | **The reader's title says what the page does not.** #110 bared the title where the page draws the anchored turn, testing turn rows only; the anchored row is not always a turn, and a leg's is drawn as its tool call — `⏺ AskUserQuestion(Open port 22 to the office CIDR? …)`, wrapped — two rows under a title saying the same words, with the leg's own highlighted row between. At eighty the title spent 40 of 78 cells on it; one keypress later the byte-identical page carried a bare title. Where no turn row says the anchor and the page, read as one sentence, does, the clause goes with its clock (#115). 79 rows in 23 files, every one a title losing a copy. Pinned on the two-tools scene's route into the reader at five widths. The operator's second finding, `↪ answered to ⌁ ops:0.0` costing `tab deeper` at eighty by one cell, is answered by #194. |
| 196 | Round seventy-five, the two-tools operator's third finding (2026-09-08) | **The archive's hidden row says what the header does not.** Under `⌂ compass · 1 api · opencode · sonnet-4-5 · ⌁ dev:2.0` the hidden session's row drew `opencode · ⌁ dev:2.0 · main`, two of its three clauses the header's, drawn identically. #188 folded the same against a tag row and left the list alone as drawing none; the identity header is the antecedent here, and #167 and #189 are the device. On the selected row the tool word and the address go where the header says them and the branch stays: `main`; at eighty, where the header sheds the model, `sonnet-4-5 · main` (#91). Twelve rows in three scenes; two literal pins from rounds thirty-two and fifty-two re-pinned to the split between header and row. Pinned on the two-tools scene's `j`, `x`, `A` at five widths. |
| 197 | Round seventy-six, the second-day operator's one thing (2026-09-08) | **The archive's selected row says what the header does not.** #196 split the header from the row for the archive's hidden live row and left the archived row three lines below untouched, so the selected archived row still drew the tool word its own identity header draws whole and paid for it in the one clause it alone carries: `claude · chor… · ✓ shipped` under a header ending `· claude`, four letters of `chore/deps`; at eighty `claude · fix/api-timeou… · ✗` where the whole branch and the verdict's word both fit without it. On the selected row the word goes where the header says it; an unselected row keeps its word, which is what tells it from its neighbour where the archive holds two tools (#80). 39 rows in five files; #103's literal re-pinned to the verdict it asserts. Pinned on the second-day scene's `A`, `A`,`8` and `A`,`8`,`tab` at 80, 120 and 152: the selected row neither repeats the header's tool word nor clips. |
| 198 | Round seventy-six, the two-tools operator's one thing (2026-09-08) | **The archive's list keeps the way deeper where the row can be attached.** #52 ranked `a ask` with the archive's own keys because there it is "the reason to be there — a claude on a session you can no longer attach to", and at eighty that rank cost the archive list's footer `tab deeper`, the frame's only naming of the way deeper, on rows whose own `enter` says `enter attach`: a hidden live session, still there to attach to. Where the row can be attached `a ask` takes the live list's rank, a key that acts on a row, which the way in outlasts (#39); where `enter · no pane` stands nothing moves. Four rows in four files. Pinned at 80 on the two-tools, subagents and many-idle scenes' `x`, `A`. **Held**, the operator's second finding: two sessions in one directory wear no `⚠ two sessions, one thing`. The mark means a collision — a file both touched within twenty minutes, a test both are failing — and a shared checkout with neither is the two-tools scene's intended setup, not a warning; a directory clause would fire on every deliberate pair. If a frame shows the two colliding and the mark silent, that is a defect. |
| 199 | Round seventy-six, the fleet-hygiene operator's one thing (2026-09-08) | **The reader's footer names the archive where no row does.** #62 handed the door to the footer below the board's width, on the reason that there the reader takes the whole screen and no row names the archive; above it the door is the band's, and the band takes only the digits the live fleet has not used, so twelve live sessions over three hundred archived left the reader's frame at 120, 152 and 220 naming neither the count nor the key while the same keypresses at a hundred columns named both. The guard asks what the frame drew, not how wide it is: `rowNamesTheArchive` reads the body rows for `archived · A` in every form the door sheds to. 36 canonical rows in three files, all many-idle; one round-thirty-seven pin draws the frame before reading the footer. Pinned on `tab`, `tab` over the many-idle and fleet-hygiene scenes at 100, 120, 152 and 220: the door is named once, on a row or on the footer. |
| 200 | Round seventy-seven, the second-day operator's one thing (2026-09-08) | **A page that is all on screen offers no key that scrolls it.** #83 dropped `ctrl+d/u half page` from the Lv1 footer on a trail that fits and #56 dropped the whole set from a lane's page with no turns; the reader's own page was left offering `j/k scroll` and `ctrl+d/u half page` on a page it draws whole, and the app itself said `all of it is on screen` beside the key the moment it was pressed. At eighty the idle key cost the `[` refusal the archive door, which no row of the frame named either, while the `]` refusal two cells shorter kept it. Where the reader's document fits its height the two scroll keys go; `[ ]`, `space`, `/` and `n/N` still act and stay, and `space unfold` leading the row gets its own head form in the shed order. 491 rows in 37 files, all footers, no frame losing a key; #191's pin re-pinned to the bare key. Pinned on the second-day scene's `tab`, `tab` at five widths, and the `[` refusal's door at eighty. |
| 201 | Round seventy-seven, the two-tools operator's one thing (2026-09-08) | **The trail's footer keeps the way deeper under a note of its own.** At eighty the Lv2 keymap is 65 cells against the 64 the twelve-cell note floor leaves, and `[ ] chapters` — fifteen cells, the widest optional key on the row — outranked `tab deeper`, so every note the trail draws that is not a chapter key's own (`at the start`, `no leg to move to`, `mirror needs 110`) cost the frame its only naming of the way deeper. The chapter key yields to a key naming a level, measured against the footer the frame draws with no news on it, and only where the key comes back; under a chapter key's own note the key the note is about stays (#24, #57). Thirty rows in nine files, every one gaining `tab deeper`, every width above eighty byte-identical; #174's pin re-pinned to the key the cells buy at eighty. Pinned at 80 on three scenes' `ctrl+u`, `m` and `[` routes. |
| 202 | Round seventy-seven, the two-tools operator's second finding (2026-09-08) | **Above the board's width the session view names the hidden session.** The hidden count and its door are the fleet's last line and the board's strip; at Lv2 and Lv3 above 110 columns no fleet body stands beside the trail, so a fleet of four counted three on the chips and no row, chip or key said where the fourth went, while the same keypresses at eighty and a hundred drew `1 hidden · A, then x`. Where the layout draws no fleet column the chip carries `1 hidden · A, then x`, the strip's own form; where a fleet body stands the chip does not (#64). Pinned on the two-tools scene's `x`, `tab` at five widths: the hidden session is named once. **Held**, the operator's third finding: the archive's selected row spending `api · ` and clipping the ask by one cell at 152 and 220 — the name is the list's own identity column (#52, #56) under a group heading that names no project, and the ask stands whole on the `◉` row of the same frame; nothing is lost, only cut. |
| 203 | Round seventy-seven, the fleet-hygiene operator's one thing (2026-09-08) | **The session view's footer names the archive where no row does.** #199 replaced #62's width test with what the frame drew but kept its level test, so one press shallower — the Lv2 session view, where the trail and the companion take the whole screen and the band is not drawn — eleven frames per width at 120, 152 and 220 on many-idle named neither the archive count nor the key, while the same keypress at a hundred columns named both. The guard reads from the waypoints level up; the door goes before whichever word the level's way out wears, `esc back` or `esc board`, and joins the waypoints shed order at the rank it has in the reader's. 33 canonical rows in three files; the door named exactly once on every frame. Pinned on many-idle's session view at 100, 120, 152 and 220. Recorded, not folded: a board card at 120 spending `· 0s ago` to draw its pane a third time by one cell (`board.go`'s own comment names the case). |
| 204 | Round seventy-eight, the two-tools operator's one thing (2026-09-08) | **A wrapped label keeps the words before the options.** At 152 the board card asked its question twice and named no model: `wrappedLabel` broke on the whole continuation row once the options began on it, and at a 48-cell column the wrap is one row carrying `CIDR? [office CIDR / keep bastion]`, so the question's tail went with the options, #107's compare failed, the card drew a copy of the very sentence its trail row says and the tag was never hoisted — `claude` bare where 120 and 220 draw `claude · sonnet-4-5`. The words before the `[` on a continuation row are the label's. Ten rows in one file. Pinned on the two-tools scene's board at 152 and on `wrappedLabel` directly. |
| 205 | Round seventy-eight, the two-tools operator's second finding (2026-09-08) | **The answer's digit is the row's at every width.** #186's shed was guarded on the note costing a key, which its prose never said; at 220 nothing was short, so `↪ answered 1 · to ⌁ ops:0.0` stood under a card row carrying `↪ answered 1` and the frame taught the digit twice (#64). The condition is the one #186 wrote: a drawn row begins with the note's head. Three rows in three files; #186's pin widened to 220. |
| 206 | Round seventy-eight, the second-day operator's one thing (2026-09-08) | **The attach refusal yields to the key that acts.** In the archive one `tab` deeper than the list, the reader's footer at eighty drew 63 of 79 cells and shed `a ask` — eight cells, the one key the archive is for — because `enter · no pane`, eighteen cells, outranked it and could not fit either, and the restore rule brings keys back lowest rank first. #52 already ranked the refusal below `a ask` on the archive's own list and #198 kept it; the reader and waypoints shed orders had the opposite rank. An attach that cannot work is a refusal, and a refusal goes before a key that acts. Seven footer rows in four files, every one gaining `a ask`; one golden re-baselined on a frame that already said `no pane` twice. Pinned at eighty on the second-day scene's archive reader. |
| 207 | Round seventy-eight, the fleet-hygiene operator's one thing (2026-09-08) | **The reply box does not stand on the strip.** The box's left began at cell zero for the board's leftmost column, so a reply from that column covered the strip whole — the archive's count and the key that browses it — and a board over forty-one archived sessions named neither at 120, 152 and 220, with up to 24 blank rows beside the box; the panel's footer cannot answer it since `A` does not act while the panel is up (#62, #64), and the same panel one column over named both. Where the drawn frame names the archive nowhere and the box is what covered it, the box steps one board column right and the deck is drawn again; a step that buys nothing is not taken. 39 rows in three files, all fleet-hygiene. Pinned on the fleet-hygiene scene's `r` at 120, 152 and 220. |
| 208 | Round seventy-eight, the fleet-hygiene operator's second finding (2026-09-08) | **The card yields the pane the header draws to the trace's clock.** #85's reason for the tag ladder's last rung, "the pane is nowhere else on the board", is false where the identity header draws this very pane; #59's fallback took it anyway, and at 120 the selected column spent `· 0s ago` — the send's clock, on no other row of the column — to draw `⌁ harness:1.0` a second time, one cell short. Where the rung is the bare pane, the header draws it and taking it costs the trace its clock, the tag goes (#90's device, from the tool word to the pane). Six rows in three files; two-tools and subagents byte-identical. Pinned on the fleet-hygiene and subagents scenes' `r`, `1` at 120 and 152. |
| 209 | Round seventy-nine, the fleet-hygiene operator's one thing (2026-09-08) | **The row leaves the present to the trail the box leaves standing.** With the reply box up, `presentBeside` compared the row only against the box's rows, on the reason that the box covers the trail's row (#108); a box thirteen rows tall over a twenty-five-row body leaves the HEAD row on the frame, and on many-idle at a hundred the list row repeated, cut, the present the trail drew whole two rows under the box's bottom edge — #111's exact harm. The compare is against the rows the frame draws (#108, #207): trail rows the box covers are skipped, the rest still count. Nineteen rows in one file; the freed row gives the list a twelfth session. Pinned on many-idle's `r` at a hundred, both sides. Recorded for the panel, not raised: a row of capped board columns at 220 leaves twenty idle cells per board row while a prompt clips (`boardColMax`, pinned in round thirteen as "a row of them is capped") — the two-tools operator's `good to go` this round is the first. |
| 210 | Round seventy-nine, the second-day operator's one thing (2026-09-08) | **The chapter keys yield where they cannot move.** At eighty the session view's footer spent fifteen cells on `[ ] chapters` — on a trail of one prompt with the cursor standing on it, where `[` answers `no earlier prompt` and `]` answers `no later prompt` at every width — and shed `enter attach`, the one write action a live session has. #83, #56 and #200 each dropped a key that cannot move; this is the one they did not reach. `chapterKeysMove` asks whether either key moves from where the trail stands, and where neither does the chapter key is first to go — only where a key that acts comes back for it (a refusal, the attach aside and the page key do not count), so a wide footer still names what `[` and `]` are (#193); under a chapter key's own note the key stays (#24, #57). 68 footer rows in 22 files, every one gaining a key that acts. Pinned on the second-day and first-session scenes' `tab` at eighty. |
| 211 | Round eighty, the second-day and two-tools operators' one thing, found by both (2026-09-08) | **The turn keys yield where they cannot move.** #210's rule one level in: at eighty the live session's reader drew 65 of 78 cells, spent twelve on `[ ] turns` while anchored on the only turn — where `[` answers `no earlier turn` and `]` answers `no later turn` — and named no key that acts on the session it reads. `turnStand` and `turnKeysMove` mirror `readerChapter`'s arithmetic; `chapterKeyStuck` picks `[ ] chapters` on the trail and `[ ] turns` in the reader; `keysActGained` reads both sides without the attach aside, so a row that gains only the parenthetical has gained nothing. Proved at all 296 frames the rule calls stuck: replaying each prefix and pressing both keys moved nothing. 115 footer rows in 22 files, every one losing only `[ ] turns` and gaining a key that acts; nothing at 220 moves. The two operators' patches rendered byte-identically. Pinned on the second-day and two-tools readers at eighty, with 152 and 220 keeping the key and the many-idle and very-long readers landing `❯ 1/1` and `❯ 12/12` under #24's own-note case. |
| 212 | Round eighty, the fleet-hygiene operator's one thing (2026-09-08) | **The box's mark says what this column lost.** #64's rule — no right-hand mark where the peek rule blanked the whole peek — was measured on everything right of the panel, so a board column the reply box covered while it was blank wore `…` because an untouched column further right had words in it: at 220 six rows of a reply frame said something was hidden over a column empty across its width, one of them beside the box's own closing border. `markCut` reads the cells the overlay overwrites, after their last rail: `…` where the box covered something of this column, a bare cell where it covered nothing; a rail alone still counts (#75). 89 rows in eleven files, nothing changing but that one cell; the round-ten literal re-pinned to the same split. Pinned on many-idle's `r` at 220, 152 and 120 against the unboxed frame. Recorded, not folded, at the operator's own measurement: dropping `boardColMax` moves two thousand rows and breaks two pins, so a content-aware cap is the subagents and very-long scenes' design question. |
| 213 | Round eighty-one, the second-day operator's one thing (2026-09-08) | **The movement key yields where it cannot move.** At eighty the second day opened on `j/k move · enter attach · tab deeper · r reply · / search · ? help · q quit`, eleven cells on a key that answers `the only live one` at every width, while `a ask` was off the row and one press later `r reply` too. #210's and #211's rule at the key the row leads with: `moveKeysMove` asks whether `j` or `k` lands anywhere — a second row on the trail, a second session in view on the list and the board, the reader always — and where neither does the movement key joins the stuck keys, taken only where a key that acts comes back for it; under the movement key's own note the key stays (#24, #57), and the help still teaches `j / k` (#43). Proved at all 218 stuck frames of 1041. Fifty rows in twelve files, every one losing only the movement key. Pinned on the second-day and first-session scenes at eighty. |
| 214 | Round eighty-one, the fleet-hygiene operator's one thing (2026-09-08) | **The turn keys yield on a page drawn whole.** #211's `turnKeysMove` called every stand off a turn a mover, since `[` lands on it; but the reader draws no cursor on the turn it stands on, so on a conversation the reader draws whole that landing writes its note and changes no drawn cell — at eighty the reader spent twelve cells on `[ ] turns` over a one-turn page in plain sight and named neither `enter attach` nor `a ask`, while the same page two presses later already shed the key. Off the only turn the landing counts as a move only where the page moves under it. 22 canonical rows in thirteen files, every one a footer losing only `[ ] turns`; many-idle, whose readers scroll, untouched. Pinned on the fleet-hygiene scene's reader at eighty with three other sides held. |
| 215 | Round eighty-one, the two-tools operator's one thing (2026-09-08) | **#214 reverted: a turn key that moves the mark stays.** #214's reason — that on a page drawn whole `[` writes its note and changes no drawn cell — was measured under `forceASCII`, which strips the reader's inverse bar; with colour on, `[` moves that bar seven rows onto the turn it lands on, on two-tools and on fleet-hygiene, the scene #214 was pinned on. The landing is the highlight the reader draws for its anchored row, the very thing a person asked for, so the key acts. `turnKeysMove` is #211's again and the 22 rows #214 moved are restored; #214's pin is dropped and the two-tools operator's, which reads the frame with colour on, stands in its place. |
| 216 | Round eighty-one, the two-tools operator's second finding (2026-09-08) | **A stuck key is not the gain that buys the trade.** #213's yield traded eleven cells of `j/k move`, stuck on the archive's one row, for fifteen of `[ ] chapters` on a trail of one prompt, where both chapter keys refuse; the chapter key and the movement key are one rule (#210, #211, #213), so one coming back for the other buys nothing. `chapterYield` refuses a gain that is itself stuck. Four rows in four files. Pinned on the two-tools scene's archive list at a hundred. |
| 217 | Round eighty-one, the two-tools operator's third finding (2026-09-08) | **The live row says what the header does not.** The live list's selected row drew the tag the identity header of the same frame draws whole — `claude` — and the trace lost its `ago` to it. Where the header draws every clause of the selected row's tag the row sheds it and spends the cells on the trace's clock (#90, #167; #196 and #197 on the archive's halves); an unselected row keeps its word, which tells it from its neighbour where the fleet runs two (#80). 78 rows in two files; round twenty-nine's literal re-pointed from the list to the frame. Pinned on the two-tools scene's live list at 80 and 100. |
| 218 | Round eighty-two, the fleet-hygiene operator's one thing (2026-09-08) | **#208's yield reads the trace through its style.** `columnTag` tested the trace with a prefix and a suffix while `boardDelta` returns it rendered dim; under `forceASCII` the tests held and the fold was measured to take the duplicate pane off, but with colour on the escapes made all three miss, so the frame a person sees drew `⌁ harness:1.0` a second time and spent the send's clock on it (#215's lesson: the frame is the coloured one). The trace is stripped before it is read. Zero rows move under `forceASCII`; six canonical rows in three files with colour on, every one a board trace losing a duplicate pane and gaining its clock; colour-against-ASCII divergence across 24 sequences goes fifteen to none. Two pins with colour on. Applying #215's lens to every surviving stuck-key rule: 950 stuck frames, no press moves a drawn cell with colour on, so #210, #211 and #213 hold. |
| 219 | Round eighty-two, the second-day operator's one thing (2026-09-08) | **The page key sheds at its own rank wherever it leads the row.** One `tab` into the running session at eighty, the trail's footer spent eleven cells on `j/k rows`, which answers `no leg to move to` on both keys, while `a ask` was off the row: #213's yield was never taken at Lv2 because when the movement key at the row's head yields, `ctrl+d/u half page` inherits the head, the separator-led fragment `shedOrder` names matches nothing, and the cells bought back the 22-cell page key instead of a key that acts. The head form #56 gave the attach key and #200 gave `space unfold`, given to the page key, and `keysActGained` counts neither form as a gain. 22 rows in four files, all footers, each losing only the movement key; proved on 176 stuck frames colour on and off. Pinned on the second-day and first-session trails at eighty. |
| 220 | Round eighty-two, the second-day operator's second finding (2026-09-08) | **The page key sheds from a trail the panel draws whole.** #83 shed `ctrl+d/u half page` at Lv1 on a trail that fits and #200 in the reader on a page all on screen; Lv2, the level the keys actually page, was the gap, and at 152 the session view spent 21 of 142 cells naming it over a two-row trail. Where the trail's document fits its box the key goes, in both forms; where it does not, it stays. Round twenty-nine's pin re-pinned to the trail's own length. Pinned on the second-day session view at 120, 152 and 220, and the very-long trail keeping the key. |
| 221 | Round eighty-two, the two-tools operator's one thing (2026-09-08) | **#220 corrected: the page key that walks the cursor stays.** #220 shed `ctrl+d/u half page` wherever the trail's document fits its box, but at Lv2 the keys move the cursor half a screenful of rows, not the viewport (§3), and on the walkthrough's very next keypress the `▸` walked four rows with no note. The question is the cursor's, the movement key's own test (#213, #219): where the trail has one row both keys refuse and the key goes; anywhere else it acts and stays. 111 rows in thirteen files restored; round twenty-nine's pin re-pointed to the cursor. Pinned on two-tools and fleet-hygiene at 152 and 220 with the trail-of-one side held. |
| 222 | Round eighty-two, the two-tools operator's second finding (2026-09-08) | **The unfold key yields where it cannot unfold.** On a conversation whose screen holds no tool result, Space answers `nothing to unfold on screen` whichever row is on top and at every width, while the reader's shed order ranked it above `/ search`, `r reply`, `a ask` and `enter attach` — 158 such frames. `unfoldKeyMoves` walks the drawn screen for a foldable row as `toggleFold` does; where none stands the key joins the stuck keys, taken only where a key that acts comes back, and under its own note it stays (#24). 32 rows in six files; one literal re-pointed. Pinned on the second-day and first-session readers at eighty. |
| 223 | Round eighty-two, the two-tools operator's third finding (2026-09-08) | **The walk key yields with no search to walk.** Until `/` has been entered, `n/N` answers `no search — / starts one` to both keys, and the row already knew the pair rides only with the search it walks — 372 such frames. `walkKeyStuck` adds the pair to the stuck keys where no query stands; `/ search` beside it still says how a walk begins. A trade brings back more than one key, so `keysActGained` now passes over stuck gains rather than refusing the whole trade on the first, and reads a key whichever form it wears. 22 rows in twelve files. Pinned on the two-tools reader. |
| 224 | Round eighty-three, the second-day operator's one thing (2026-09-08) | **The hairline runs beside the reply box.** `joinColumns` stops the deck's hairline where the longest column's content stops, and the reply box is composited afterwards, so a box whose last rows fall past every column's content stood with no `│` beside them — eleven of the box's fourteen rows at eighty on first-session, and the closing border's hairline lost on second-day at eighty and a hundred, while the same box kept it once the list grew a row (#108's lesson, unapplied). `joinColumnsBelow` takes a floor under the hairline: the box's last row. 86 rows in five files, every one a hairline cell gained, no content cell moved. Pinned on the first-session and second-day reply boxes. |
| 225 | Round eighty-three, the second-day operator's second finding (2026-09-08) | **A stuck key is tried again once another has yielded.** The yield pass judged each stuck key against the row as it stood and went by once: at eighty the reader's `[ ] turns` bought nothing beside `space unfold` at the row's head, and once the unfold key had yielded and bought `/ search` the twelve cells were `enter attach`, but the pass was over — five frames on first-session under a note not the key's own, while the same stand at a hundred shed it. The pass repeats while any yield is taken; a key that buys nothing against the final row is still refused (#193) and the spending order is unchanged. Five rows. Pinned on the first-session reader at eighty. Recorded: #219's two lines are now reached first by #221's block, so its pin bites only on both reverted together. |
| 226 | Round eighty-three, the two-tools operator's one thing (2026-09-08) | **The page key buys a stuck key's cells.** At 152 the frame `ctrl+u` produced under `at the start` kept `[ ] chapters` — both halves refuse, no drawn cell moves, colour on and off — and shed `ctrl+d/u half page`, the key that had just walked its cursor: `keysActGained` refused the page key as a gain on #42's and #51's rank argument, which is about rank and not about whether the key acts, and every level now drops the page key where it cannot move (#83, #200, #221), so a page key still on the row is a key that acts. One line; eleven rows in six files, rank unchanged. Pinned on the two-tools trail at 152 under `at the start`. |
| 227 | Round eighty-three, the two-tools operator's second finding (2026-09-08) | **The hide key yields where it cannot hide.** `x hide` was offered on board rows where `toggleHidden` can only refuse — `infra stays · it is asking`, no cell moves — while `a ask` and `/ search` were off the row, fifteen frames. `hideRefusal` is one sentence for the key and for the footer that offers it; `hideKeyStuck` joins the stuck keys where the selection cannot be hidden and is not hidden, taken only where a key that acts comes back; under any of `x`'s own notes the key stays (#24). 74 rows in eleven files; one golden re-baselined. Pinned on the two-tools board at 120. **Held**, the operator's third finding: the eighty-column help defining marks the scene never wears and not the tool words — the help fills all 24 rows there, the marks it defines are #17's, and a per-fleet glyph census is the design question #89 and #99 left; the row the operator would draw is recorded in the report. |
| 228 | Round eighty-three, the fleet-hygiene operator's one thing (2026-09-08) | **The trail's end is the last row the deck draws.** At 220 on many-idle, with the cursor on the last drawn trail row, `j` and `G` returned a byte-identical frame with an empty note: `TrailRows` counted seven rows where the panel drew six — a waypoint riding on its leg's row — so the index test read "not at the end", the move stepped onto the undrawn row and back, and the key drew nothing and said nothing. Both keys now ask the move whether it moved, the device `ctrl+d` already used (#24, #213, #221). Canonical corpus byte-identical under both colour profiles; seven off-canonical rows gain the owed note; the dead-press sweep goes eight to none. Two pins, one of them a general sweep of every Lv2 stand for a silent move key. |
| 229 | Round eighty-four, the two-tools operator's one thing (2026-09-08) | **A stuck key goes from a row already shed.** #210's gate — a stuck key yields only where a key that acts comes back — was written for the row where nothing is shed, so that a wide footer still names what `[` and `]` are (#193); at eighty the reader stood at 79 of 80 on `space unfold · [ ] turns · esc back · ? help · q quit`, both chapter halves refusing and moving nothing colour on or off, having already shed `/ search`, `n/N`, `r reply`, `a ask` and `enter attach` — and nothing could come back, since `enter attach` was one cell too wide and `a ask` ranked under it (#39). Where the row has already given up a key that acts, the reason is spent and the stuck key's cells are given up rather than spent: no key comes back, #39's rank is untouched, the help still teaches the key. A hundred rows in 22 files, every one a footer; round seventy-four's pin re-pointed from `n/N` to `h/l session`. Pinned on the two-tools reader at eighty and its list and trail at a hundred. |
| 230 | Round eighty-four, the second-day operator's one thing (2026-09-08) | **The attach refusal goes before the search key.** At eighty the second day's archive spent eighteen of 76 cells on `enter · no pane` — which answers `attach needs a pane` and moves no cell at any width — and named no way to search twelve finished sessions, eight of them behind `▾ 8 more below`, where `/` cuts the twelve to two. #206 applied its own rule, a refusal goes before a key that acts, to `a ask` only and left the refusal ranked below `/ search` and `n/N` in all three shed orders; #165 had read this harm on this row and shortened the refusal instead of moving its rank. The refusal now ranks before the search pair everywhere it appears. Ten rows in four files, each losing only the refusal and gaining `/ search`; one view pin re-pointed to the key the cells buy. Pinned on the second-day and few-ongoing archives at eighty. |
| 231 | Round eighty-four, the fleet-hygiene operator's one thing (2026-09-08) | **The reader's present is the end of the conversation.** On the reader's last page `G` returned a byte-identical frame with an empty note — `case "G"` threw away `scrollBy`'s answer that `ctrl+d` twelve lines up asks for — the dead key round one bans and the rule #228 folded one level up; silent on 27 canonical reader stands across five scenes while `j` and `ctrl+d` on the same page both said `end of the conversation`. `G` asks the page whether it moved and says the reader's own sentence for that end. Canonical corpus byte-identical; 132 off-canonical rows gain the owed note; the sweep goes 26 to none. Two pins, one a sweep of every reader stand for a silent page key. Recorded: #226's line is dead code since #229 subsumed it, and its pin no longer bites on its own. |
| 232 | Round eighty-four, the fleet-hygiene operator's second finding (2026-09-08) | **The attach refusal yields to the note that says it.** On the paneless session `enter` drew `attach needs a pane` twelve cells right of the row's own `enter · no pane` — the one stuck key whose note names it, since #165 gave the note that form so naming the key would buy a key back — and paid `/ search` and `x hide` at eighty for the repetition. Beside that note the clause joins the stuck keys, taken only where a key that acts comes back; every other stuck key stays under its own note because its note does not name it (#24, #57). Two canonical rows, 55 off-canonical. Pinned on the fleet-hygiene paneless session's `enter` at eighty with the no-note side held. |
| 233 | Round eighty-five, the two-tools operator's one thing (2026-09-08) | **The trace note leaves its destination to the row.** At 120 the board's `↪ sent to ⌁ dev:2.0` pointed at a pane the header and the card row the trace is about already drew character for character, and paid `a ask` for it; the code's stated reason — the note's destination clause is never on the row — is false on the board, where the card's tag draws the full target for a shared tmux session. Where the row draws the quote and the destination in the note's own form the note keeps only its verb (#128, #205). 27 rows in eighteen files, all footers, four gaining a key; nothing at 80 or 100 moves. Pinned on the two-tools board's `r`, `1` at 120 with both sides held. |
| 234 | Round eighty-five, the second-day operator's one thing (2026-09-08) | **The archive groups by project, not by the name its person gave.** `archiveGroups` bucketed on the session's name, which #79 defines as the typed `/rename`, so the one renamed session headed a group of its own at 220 and its directory's other session sat under a second heading 34 rows and six projects away — against #11's own rule, "archive groups by project directory", and the function's own comment. `projectOf` buckets on the directory whatever the row is called; the name still names the row, the header and the band (#79). 532 rows in five files, all second-day archive frames; one digit pin re-pointed as the archive's order shifts by one. Pinned at every width. Recorded: #225's repeat loop is now reached first by #229's `rowAlreadyShed`, so its pin no longer bites on its own, as #231 recorded for #226. |
| 235 | Round eighty-five, the fleet-hygiene operator's one thing (2026-09-08) | **The search walk says where it landed.** `n` and `N` pinned the page where every match was already drawn — on a conversation the reader draws whole they can never leave the top — and the key the row names beside `/ search` drew nothing and said nothing, the dead key round one bans: silent on 1,901 presses across all nine scenes, and no press of either key ever spoke. The walk asks the page whether it moved (#24, #228, #231) and where it did not says `the match is on screen`; under that note the pair stays, as the chapter keys stay under theirs (#223 from the other side). Canonical corpus byte-identical; 97 off-canonical rows gain the note; the sweep goes 1,901 to none. Two pins, one a sweep of every reader stand for a silent walk key. |
| 236 | Round eighty-five, the fleet-hygiene operator's second finding (2026-09-08) | **The search walk names the match it landed on.** #235 made the walk speak where it moved nothing; where it moved, the page went somewhere the person did not choose by hand and nothing on it named the match — 675 mute presses. `walkTo` reports which match it went to and the note says `match 3/7`, the turn note's own form without a quote (#20); the pair stays under that note too. Canonical corpus byte-identical; two pins, one a sweep of every reader stand for a walk that moves in silence. |
| 237 | Round eighty-six, the second-day operator's one thing (2026-09-08) | **The archive row keeps the name its person gave.** Since #234 made the bucket the project, the archive's row was the only one of #79's four places that no longer said the typed name: `3 ○ the checkout suite flakes on CI` under `webapp` while the header, the trail title and the search field all said `checkout-flake-hunt`, and under `/checkout-flake` the one row returned did not contain the query. A renamed session is the hidden live row's own case: it keeps its name before its prompt. 32 rows in five files, all second-day archive frames; #234's pin re-pointed to the name. Pinned at every width. |
| 238 | Round eighty-six, the two-tools operator's one thing (2026-09-08) | **The digit of the row you are on says so.** The walkthrough's last keypress on every two-tools file, `3` on the row `3` already selects, drew a frame byte-identical to the one before it with no note — 1,626 silent presses across all nine scenes, the dead key round one bans and #228, #231 and #235 each folded at a different key; round seventy-one had dropped it as a success under #30, and a success that draws nothing is still a key that says nothing. Where the digit names the row the deck is already on the note is `the session you are on`, the header naming the digit and the name at the same cells (#233); the ordinary price of a note (#36), cleared by the next keypress. Ten rows in ten files, all footers; the sweep goes 3,722 to none. Two pins, one a sweep of every self-digit press. Recorded: #237's price at eighty, `checkout-flake-hunt…` with the ask gone, the name being the handle the person typed. |
| 239 | Round eighty-six, the fleet-hygiene operator's one thing (2026-09-08) | **The search walk keeps its own place in the run.** #235 and #236 read the walk's place back off the page, and `clampScroll` pins the page at the last screenful, so once the rest of the run shared one screen every press recomputed the same match: six byte-identical frames on fleet-hygiene at eighty, 552 of the corpus's 588 search stands unable to walk their whole run, 2,082 matches never named. The walk keeps `walkRow`, steps one match a press, wraps, and names every landing `match 2/4` — nine cells, thirteen shorter than the sentence it replaces, so at eighty on many-idle the row keeps `A archive`; a fresh search or a cleared one resets the place. Canonical corpus byte-identical; 208 off-canonical rows change, 94 gaining a key and none losing one; runs cut short go 552 to none. One assertion of #235's pin widened. Two pins, the sweep under two seconds. |
| 240 | Round eighty-seven, the two-tools operator's one thing (2026-09-08) | **The search you typed opens on its first match.** `searchKey` zeroed the scroll and then took a step, and `walkStep`'s strictly-greater step walked past a match on row nought — the opening prompt the person themselves typed — so `/the` answered `match 2/3` with the matching prompt above the page: 2,556 of 5,422 fresh searches corpus-wide opened past match one, 532 with it off the page. Where the walk has no place yet the first press is a landing, not a step (#239): `landFirstMatch` opens on match one and says `match 1/3`. Canonical corpus byte-identical; 280 off-canonical footers move. Two pins, the sweep under seven seconds. |
| 241 | Round eighty-seven, the fleet-hygiene operator's one thing (2026-09-08) | **The reader's start key answers like its pair.** `g` set the scroll to nought and said nothing: on 522 of the corpus's 608 reader stands it moved no line, on 87 the frame came back byte for byte the same, while `k` and `ctrl+u` on the very same page both answered — the reader's last silent key, and the other end of #231's `G`. It asks the page whether it moved and says `start of the conversation`, the sentence `k` already gives for that end (#24, #228); the row it then draws is byte-identical at every stand to the row `k` draws there. Canonical corpus byte-identical; the sweep goes 522 to none. Two pins, the sweep in three seconds. |
| 242 | Round eighty-seven, the second-day operator's one thing (2026-09-08) | **The archive does not deny the live row it is drawing.** After a search the archive answered and the board did not, `A` opened an archive with no rows while the deck kept the live session selected — header naming it, trail drawing it, `enter attach` offered — yet `1` answered `no session 1`, where `x` one keypress away called it `1 hello`. #32's reason, the archive numbers as drawn, cannot cover a frame drawing no rows. Where the digit is the selected live session's the refusal names it and says where it is, `1 hello is live`, #57's form one branch below its hidden twin; the footer's `A fleet` is the way, so the note does not buy the key twice. Canonical corpus byte-identical; fifteen off-canonical footers. Pinned at five widths on the second-day scene. |
| 243 | Round eighty-eight, the second-day operator's one thing (2026-09-08) | **The fleet does not deny the digit its header is drawing.** Under a fleet query that matches nothing the list draws no row, so the selected live session's own digit found none and answered `no session 1` while the header drew `1 hello` three cells from the name and the trail beside it was that session's. That is #238's question: where the digit is the selected session's the note is `the session you are on`. Canonical corpus byte-identical; thirty off-canonical footers. Pinned at five widths on the second-day and first-session scenes. |
| 244 | Round eighty-eight, the fleet-hygiene operator's one thing (2026-09-08) | **The empty archive keeps the session it is drawing.** With a fleet search standing that left the archive with no rows, `A` swapped in a selection never set, `selectedIndex` fell to nought, and the header and trail title named the fleet's first row while the panel went on drawing the session the person left — and `enter attach` offered the first row's pane, a tmux write aimed at the wrong session: 65 of 85 empty-archive stands across six scenes. Where the archive answers nothing the selection stays the live session's, as the hidden branch already keeps the hidden one. Canonical corpus byte-identical; 218 off-canonical rows, one owed `enter · no pane` regained. Two pins. |
| 245 | Round eighty-eight, the two-tools operator's second finding (2026-09-08) | **The archive's drawn row is the session you are on.** #242 answered `2 api is live` to the board's digit pressed on a hidden live row the archive draws under its own `1`, naming a digit the frame does not; where the archive draws the row the answer is #238's, `the session you are on`, and #242's `N name is live` is kept for the archive that draws none. Pinned on the two-tools scene's `x`, `A`, `2` at three widths. |
| 246 | Round eighty-eight, the two-tools operator's one thing (2026-09-08) | **The help in the reader says what the grab key does.** Three levels deep, with `▲1 4m` in its own header, the help drew `g   grab the oldest ▲ needs-you and attach`, and `g` there is the start of the conversation (#241) — the help promising an action the key does not take, on all 608 reader stands; #40's rule for the board's `g` row at the one level where the key acts differently. The reader's row says `the start of the conversation, the other end of G; the grab is a level out`. Canonical corpus byte-identical; 135 off-canonical rows, every one the `g` row. Two pins, one a sweep of every help frame against the binding the frame carries. |
| 247 | Round eighty-nine, the fleet-hygiene operator's one thing (2026-09-08) | **A move on a view with no rows says so.** A standing query the view answered with nothing left `j` saying `the only live one` over `no session matches /zzqq` and a header counting `0 of 4`, and in the archive `the only session` — 364 of 400 empty-view stands counting a session the frame does not draw; the other 36, at trail level, already said `no leg to move to`. The move says what it could not do in the object the frame's own footer names: `no row to move to` on the list, `no column to move to` on the board; the band under a board miss is not a cursor's (#51). Canonical corpus byte-identical; 364 footers, no key lost or gained. Two pins. Recorded, not folded: `x` in the empty archive draws `2 harness is hidden · A, then x` on a frame that is already the archive and numbers that row `1`. |
| 248 | Round eighty-nine, the second-day operator's findings (2026-09-08) | **The header keeps the digit of the live row it is drawing.** In the archive the numbers are the archive's own (#32), but an archive drawing no row claims no number, and the live session the frame still selects, draws the trail of and offers `enter attach` for (#244) wore no digit in the header while its own refusal on that frame called it `1 hello is live` (#242). Where the archive draws no row the header keeps the live session's digit. 300 header stands off-canonical, corpus byte-identical. The operator's one thing this round — `the only session` on a list a search emptied — is #247, found by the fleet-hygiene operator the same round; the second-day pin, which asks only that no move on an empty view counts a session, stands beside #247's. |
| 249 | Round eighty-nine, the two-tools operator's one thing (2026-09-08) | **The archive keeps the last live session too.** On #244's own stand `x` in the archive hid the fleet's only live session, and the board came back `nothing live` and `○ all quiet` while its trail still drew `● scout thinking… for 40s`: `hideRefusal`'s `the live one stays` carried a `!archiveView` scope from when the count was the view's own list (round fifteen), and `liveCount` has counted the board ever since. The rule is the fleet's, not the view's. Canonical corpus byte-identical; fifteen off-canonical rows, four keys regained; one assertion of #242's pin re-pointed to many-idle, where a hide on the empty archive still lands. Two pins, one a sweep that no archive empties the live board. |
| 250 | Round eighty-nine, the two-tools operator's second finding (2026-09-08) | **The archive's hide note names no way back in.** In the archive the hide note said `A, then x` on the very frame whose footer names `x unhide` — #233's repetition, and #232's rule that a note yields to the key it names; at eighty the clause's eleven cells were what `x unhide` cost. In the archive the note is `2 api is hidden`. Twenty off-canonical footers, each gaining a key. Pinned on the two-tools archive at 80 and 120. |
| 251 | Round ninety, the two-tools operator's one thing (2026-09-08) | **No digit denies a live session the fleet numbers.** On a view that draws no row the deck answered a digit with `no session 1`, the sentence for a digit no session ever had, while that session was live and asking and the board one `esc` away drew `1 ▲ infra`: eighty footers in 35 files. Where the digit is the selected session's the deck already says so (#238, #243) and in an empty archive names it (#242); this is the same frame's other digits. The refusal names it and says what it is, `1 infra is live`, the way back already on the frame; where the archive draws rows the digits are its own (#32). Canonical corpus byte-identical; the note is three cells longer, so twenty of the eighty rows shed one clause at 80 and 100. Pinned on twenty cases with both other sides held. |
| 252 | The owner's look, round ninety (2026-09-08) | **The board keeps its order while the selection moves.** On a live fleet of two working columns over four calm ones, `j` along the calm band reshuffled the board on every press: the card just read jumped to the head of its band and the others slid, and the person could not tell what had moved or why. `boardKeys` appends the selected session to the owed so a digit always shows its trail (#16), and the pack read that list as the board's order, so a calm selected session led the calm band. The pack now takes the owed in the fleet's order and the calm after them in the fleet's order, the selection pulled forward only where it would be trimmed off the end, where it takes the last drawn slot as before (#90). Pinned on many-idle at 152 and 220: walking `j` along every drawn column changes the drawn order only by that last-slot rule. |
| 253 | Round ninety, the second-day operator's one thing (2026-09-08) | **The archive offers the reply key for the live row it selects.** On the empty archive the footer offered `enter attach` for the live session's pane and never named `r reply`, while `r` there opened the card and `1` sent; the archive draws and selects live rows — the hidden one, and the one an empty archive keeps (#244) — and for those the pane is real. #53 made which write a row is offered the pane's question; the archive's keymap had made it the view's. `r reply` joins the archive keymaps at every level, and the `no pane` test takes it off an archived row as before. 198 off-canonical footers gain the key, 21 canonical rows in six scenes; one assertion of #211's pin re-pointed. Pinned on the second-day archive. |
| 254 | Round ninety, the second-day operator's second finding (2026-09-08) | **The reply card keeps the digit of the live row it types into.** The card said `reply to hello` three rows under a header drawing `1 hello` — the same guard #248 fixed, at the card. Where the archive draws no row the card wears the live session's digit. Ten off-canonical rows, corpus byte-identical. Pinned at five widths. |
| 255 | Round ninety, the fleet-hygiene operator's one thing (2026-09-08) | **The board's band opens on its digit.** On `fleet-hygiene` at 120 the board stranded a recent band under the strip, numbered `5` to `9` on from the last column as #47 designed, and `5` answered `no session 5` fourteen rows under the row wearing it, while the same digit twenty columns narrower opened it. `recentRows` is shut to the board (#43) and lifted for the one call that draws the band; `openRecent` asked outside that call and saw none. The board records the band it drew, and the digit reads that and no other row: 60 of 60 band digits act, 272 digits no row wears are still refused, `A` comes back to the board it left. #51's landing number is untouched. Corpus byte-identical; pinned on the frame and as the rule over every scene and width. |
| 256 | Round ninety, the fleet-hygiene operator's second finding (2026-09-08) | **The archive's hide note wears the number the frame draws.** `x` in the archive said `2 harness is hidden` over a row drawn `▸1 ● harness` under a header reading `1 harness`: the note carried the board digit where the numbers are the archive's own (#32), the digit #245 took out of the refusal and #248 kept the header true to. The note takes the drawn number where the fleet gave one; on the board, where the row is gone the moment the key acts, the board digit stays. 130 of 180 archive-hide stands change, a digit for a digit, no key lost or gained; corpus byte-identical. Pinned at five widths and as the rule. |
| 257 | Round ninety-one, the two-tools operator's one thing (2026-09-08) | **The unhide note names which session came back.** On a fleet with two sessions called `api`, both hidden, `x` on one said `api is back on the board` under a header and a row that both drew the other `api`, the one still hidden: the session that came back is drawn nowhere on the frame that follows. The note takes the board digit, `2 api is back on the board`, the same digit the hide note spent one press earlier (#256) and the key that reaches it on the board (#16). Corpus byte-identical; 250 off-canonical footers, 23 shed one clause for the two cells (five of them `x unhide` at eighty, the ordinary price of a note, #36). Pinned at five widths under both profiles. |
| 258 | Round ninety-one, the two-tools operator's second finding (2026-09-08) | **The reply card wears the number the archive draws.** On an archive listing two hidden `api` rows, `r` on the row drawn `▸1 ● api` under a header reading `1 api` opened `reply to api`, no number, while the same card on the emptied archive wore the board digit (#254). The card reads the drawn row's number first, the header's own device (#248), and falls back to the board digit where #254 scoped it. Corpus byte-identical; 47 off-canonical card heads. Pinned at five widths. |
| 259 | Round ninety-one, the second-day operator's one thing (2026-09-08) | **The sent row does not keep a move that cannot move.** At eighty, a line sent from a list a search had emptied drew ` j/k move · ? help · q quit` beside `↪ sent "please continue" · to ⌁ main:0.0`: `j` there answers `no row to move to`, and the row named neither `enter attach`, the pane the note had just written to, nor `tab deeper`. The trace's destination clause shed the row before the stuck-key yield (#210, #213, #216) was taken, against the unyielded rank. The yield reaches that branch too, and the trade is taken only where the yielded row gives up nothing else it names: a key that cannot move buys a key that acts, never a key that acts another (#39's ranks stand). Corpus byte-identical; 106 off-canonical footers, 64 swap a dead move for a key that acts, 28 regain the destination's `to`. Pinned on four scenes at five widths under both profiles. |
| 260 | Round ninety-one, the fleet-hygiene operator's one thing (2026-09-08) | **A digit opens only a band row the frame drew.** #255 made the board's digit read the band the board drew; the list and the session view still asked `recentRows(9)`, what the band could hold, while drawing what fit. At eighty `fleet-hygiene`'s opening list drew four live rows and no band, and `5` to `9` each opened an archived session the frame never named, in an order it never showed; 46 such digits across the corpus. Each drawer records the band where it draws it, the frame resets it, and the digit reads that and no other row: 380 drawn band digits still act, the refusals #255 counted go from 272 to 290. Corpus byte-identical. Pinned on the frame and as the rule over every scene and width. |
| 261 | Round ninety-two, the fleet-hygiene operator's one thing (2026-09-08) | **The unhide note wears no number the frame gives another session.** #257 gave the unhide note the board digit; in an archive that still draws rows the numbers are positional (#32), and the row the session just left hands its number to the next one: `1 porter is back on the board` stood under a header reading `1 harness` over a row drawn `▸1 ● harness` that `1` opens — one digit, two sessions (#245, #256). 150 of 290 unhide notes across the corpus did it. The note wears the number the frame draws for this session where the archive still draws it, keeps the board digit where no drawn row wears it (the empty archive #257 holds), and where another row wears it spends no digit and names the pane instead where a namesake shares the tmux session (#53). Corpus byte-identical; 70 off-canonical footers, no key lost. Pinned on the frame and as the rule under both profiles. |
| 262 | Round ninety-two, the two-tools operator's one thing (2026-09-08) | **The archive's board card says its tool once.** On the archive's board (`A`, then `⇧tab board`, on no canonical frame) the card's second row drew `opencode · main` straight over its tag row `opencode · sonnet-4-5 · ⌁ dev:2.0`, and where the archived row had no model and no pane a whole row that was the word `claude` and nothing else, while the row above paid for it by shedding its verdict to a bare `✗` and clipping its branch. The column handed the tag down as a bool that could only say it draws the pane, so the row yielded the address (#64) and never the word. The row now takes the tag itself and yields the word too, keeping the branch and the verdict the tag cannot say; the archive list, drawing no tag, keeps its word (#53, #196). Corpus byte-identical; 37 off-canonical rows change and every one gains. Pinned on both scenes at three widths under both profiles. |
| 263 | Round ninety-two, the second-day operator's one thing (2026-09-08) | **The sent row in the archive does not keep a move that cannot move.** #259's rule was false one `A` away: at eighty a line sent from an archive a search had emptied drew ` j/k move · A fleet · ? help · q quit` beside its trace, and the yield was refused for width because the board's and the list's shed order carried no head form for `enter attach` — once `j/k move` had left the row's head, the separator-led fragment matched nothing and the key could not shed. The head forms the reader's list has carried since #56 and #200 join the board's and the list's, at the key's own rank. Corpus byte-identical; four off-canonical rows. Pinned at five widths under both profiles. Recorded, not folded: `the archive is already off the board` costs the eighty-column footer `tab deeper`, `a ask` and `/ search`; a nineteen-cell form such as `it is off the board` would buy them back — a wording call left to the panel. |
| 15 | The middle panel (2026-09-02, dogfood) | **Off by default; `m` brings the mirror back.** The deck is fleet + trail at Lv1 and Lv2, and the reader joins them at Lv3 — the reader is a *level*, not a panel. Two things decided it. The dogfooding happened under 110 columns, where the mirror never opened, and nobody missed it: the CLI it mirrors is one `Enter` away, in your own tmux. And the reason to want it — checking that a leg was classified right — is served better by the Lv2→Lv3 reader, which opens on exactly the tool calls the leg was built from, than by a glimpse of the CLI's last screenful. The columns go to the trail, which is where labels, reports and results were being truncated. `m` toggles the mirror at Lv1 (`-mirror` / `mirror = true` starts it on), and capture-pane only runs while it is on screen. The tripwire use of capture-pane (ARCHITECTURE §2.4) is untouched: detection never needed a panel. |
| 11 | Fleet grouping | **Live view groups by tmux session → window** (tmux's own order; amber floats within its group, header carries a `▲` echo; `elsewhere` for live-unmapped; headers dropped when degenerate). **Archive groups by project directory, newest first** — "I start sessions in the respective directory." `1`–`9` stay flat; `g` stays one key. |
